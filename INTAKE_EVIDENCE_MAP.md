# Intake & Onboarding — Evidence Map

Alle 105 Befunde aus 17 unabhaengigen Fachagenten-Audits. Begleitdokument zu [ONBOARDING_INTAKE_MASTERANALYSE.md](ONBOARDING_INTAKE_MASTERANALYSE.md).

**Beweisstufen:** BEWIESEN = Datei:Zeile / REST-Antwort / git zitiert · ABGELEITET = aus Code-Logik gefolgert, nicht ausgefuehrt · ANNAHME = mit den vorhandenen Zugaengen nicht pruefbar.

**Umgebung aller REST-Proben:** Produktion `dngycrrhbnwdohbftpzq`, anon-Key, ausschliesslich lesend. Stand 2026-08-29.

---

## C · Technische Evidence Map

105 Befunde aus 17 unabhaengigen Audits. 82 bewiesen, 20 abgeleitet, 3 Annahme.

Umgebung aller REST-Proben: Produktion (`dngycrrhbnwdohbftpzq`), anon-Key, nur lesend.


### P0 — 35 Befunde

| # | Befund | Stufe | Quelle | Aufwand | Auswirkung |
|---|---|---|---|---|---|
| P0.1 | **Neuer Flow hat keinen manuellen Ausweg — bei toter Edge Function ist die Jobaufnahme eine Endlosschleife** | BEWIESEN | Frontend/A11y | S | Ein Kunde, der über das Dashboard eine Stelle anlegen will, kann es aktuell gar nicht — er landet nach jedem Versuch wieder im leeren Textfeld. Die Fehlermeldung („bitte mehr Text/Details angeben") suggeriert, er müsse es nur besser versuch |
| P0.2 | **„Später weiter" verspricht Fortsetzbarkeit und löscht in Produktion das komplette Briefing — still** | BEWIESEN | Frontend/A11y | M | Antworten auf bis zu ~30 Briefing-Fragen, die Flexibilitätsmatrix, die Contracting-Konditionen und der Reveal-Descriptor sind nach „Später weiter" weg. Der Nutzer merkt es erst Tage später beim Fortsetzen — und sieht dann ein leeres Briefin |
| P0.3 | **Kein Autosave, kein Verlassen-Schutz: Escape oder ein Klick auf den Overlay verwirft die gesamte Sitzung** | BEWIESEN | Frontend/A11y | M | Ein versehentlicher Escape-Druck (z. B. um ein Select zu schließen, das gar nicht offen ist) oder ein Klick neben das Fenster löscht 15-25 Minuten Briefing-Arbeit ohne Rückfrage und ohne Undo. In einem Modal, in dem der Nutzer permanent mit |
| P0.4 | **Contracting-Stellen gehen komplett ohne Vergütungsangabe raus — Tagessatz wird garantiert verworfen** | BEWIESEN | Frontend/A11y | S | Jede über den neuen Flow angelegte Contracting-Stelle geht ohne Tagessatz, ohne Laufzeit und ohne Auslastung an die Recruiter — also ohne die drei Angaben, die im Contracting über die Ansprechbarkeit eines Kandidaten entscheiden. Der Recrui |
| P0.5 | **Drei Hard-Kill-Felder werden in einen JSONB-Blob geschrieben statt in die vorhandenen Spalten** | BEWIESEN | Datenmodell | S | Sprach-Kill, Praesenz-Kill und Zertifikats-Kill feuern nie. Ein Kandidat ohne Deutsch C1 auf einer Stelle mit Pflicht-C1 wird nicht ausgeschlossen. Und das bleibt auch NACH der Hybrid-Migration kaputt, weil intake_payload kein Matcher-Konsu |
| P0.6 | **Der Studio-Pfad persistiert `industry` ueberhaupt nicht** | BEWIESEN | Datenmodell | S | calculateIndustryScore gibt fuer alle Studio-Jobs konstant 75 zurueck (v3-1:1430) — Branchenpassung ist als Signal tot. Das Recruiter-Exposé (format-job-for-recruiters) verliert die Branche, obwohl recruiter_jobs_view sie ungated ausliefern |
| P0.7 | **„Spaeter weiter" loescht in Produktion das gesamte beantwortete Briefing** | BEWIESEN | Datenmodell | M | Der Kunde beantwortet bis zu 34 Fragen, klickt „Spaeter weiter", kommt zurueck — und der Fortschrittsbalken steht wieder bei 0, alle Antworten sind weg. Der Delegations-Flow (handleDelegate :435-454) uebergibt dem Kollegen konsequenterweise |
| P0.8 | **job_skill_requirements: typisierte Tiefe wird verworfen, und zwei Schreiber streiten um dieselben Zeilen** | BEWIESEN | Datenmodell | M | Die vom Kunden bewusst gesetzte Muss/Kann-Trennung ist weder stabil noch rekonstruierbar. min_years — das einzige Feld, mit dem sich „3 Jahre SAP FI" von „mal gesehen" trennen liesse — wird trotz vorhandener Spalte weggeworfen. Bei Backfill |
| P0.9 | **Die Flexibilitaetsmatrix erreicht kein Scoring — auch nicht nach der Migration** | BEWIESEN | Datenmodell | M | Ein als „flexibel" markiertes Muss killt den Kandidaten genauso hart wie ein „fix". Bei 5 Muss-Kriterien reicht das Fehlen von 3 flexiblen, um unter die 0.40-Schwelle zu fallen — der Kandidat verschwindet, obwohl der Kunde diese drei expliz |
| P0.10 | **Recruiter unterzeichnen live einen Vertrag mit einer fremden/fiktiven Gesellschaft ("MatchHub GmbH, Musterstraße 1, 10115 Berlin")** | BEWIESEN | Recht | M | Kein wirksamer Vertrag zwischen Bluewater & Bridge GmbH und dem Recruiter: Vertragspartner falsch bezeichnet, Erklärung an eine Nicht-Partei gerichtet. Bei jedem Auszahlungsstreit fehlt die Anspruchsgrundlage gegen den Recruiter (Umgehungsv |
| P0.11 | **Die einzige echte Kandidaten-Einwilligung ist produktiv nicht erfassbar — und zwei Pfade heben sie ohne den Kandidaten auf** | BEWIESEN | Recht | L | Offenlegung von Klarnamen, Kontaktdaten und CV an das Kundenunternehmen ohne nachweisbare Rechtsgrundlage. Art. 6 Abs. 1 lit. a i.V.m. Art. 7 Abs. 1 DSGVO (Nachweispflicht) nicht erfüllbar; die Datenschutzerklärung ist in einem tragenden Pu |
| P0.12 | **Betroffenenrechte-Selbstbedienung (Export/Löschung) ist eine tote UI — die Datenschutzerklärung behauptet das Gegenteil** | BEWIESEN | Recht | M | Art. 12 Abs. 3, 15, 17, 20 DSGVO: Fristen (1 Monat) laufen, die zugesagte Funktion existiert nicht. Eine Beschwerde beim BayLDA (in Ziffer 21 selbst als zuständig benannt) trifft auf eine schriftlich zugesagte, faktisch fehlende Funktion —  |
| P0.13 | **Vier divergierende AGB-Fassungen, keine Versionierung, akzeptierter Text wird nicht archiviert** | BEWIESEN | Recht | M | Einbeziehungs- und Beweisproblem: gegenüber Unternehmern ist die Einbeziehung zwar erleichtert (§ 310 Abs. 1 BGB), aber Matchunt kann nicht belegen, WELCHER Text wann zugegangen ist. Die Klauseln, auf die es ankommt (Umgehungsverbot § 11, F |
| P0.14 | **Honorar: Kunde stimmt "standardmäßig 20%" zu, der Admin setzt danach einseitig 15-30% — ohne Zustimmung und ohne Mitteilung** | BEWIESEN | Recht | L | Bei 24% statt 20% auf 90.000 EUR sind das 3.600 EUR pro Platzierung, die auf keiner Einigung beruhen. Rechtlich: einseitige Leistungsbestimmung ohne vertragliche Grundlage (§ 315 BGB greift nur, wenn ein Bestimmungsrecht vereinbart wurde —  |
| P0.15 | **Reihenfolge invertiert: Vertragsunterschrift vor jedem Produktwert — Time-to-Value = 7 Klicks bis zum ersten Artefakt, 3–5 Tage bis zum ersten Nutzen** | BEWIESEN | Conversion/UX | L | Klassischer Commitment-vor-Wert-Fehler: Reaktanz und Abbruch genau an Schritt 1 (AGB) und Schritt 2 (Unterschrift). Wer abbricht, hinterlässt einen Account ohne Firmendaten, ohne Job und ohne Reaktivierungsanker. Ein HR-Leiter mit Prokura-B |
| P0.16 | **Das gesamte Briefing erreicht den Recruiter nie — 34 Fragen ohne Empfänger, mit ausdrücklichem Gegenteil-Versprechen im UI** | BEWIESEN | Conversion/UX | M | Der teuerste Aufwand im gesamten Funnel ist folgenlos. Der Kunde merkt es erst, wenn die ersten Kandidaten kommen und offensichtlich nichts vom Briefing berücksichtigt wurde — also genau an dem Punkt, an dem er über Weiternutzung entscheide |
| P0.17 | **Alle drei Import-Wege sind in Produktion 404 — der Nutzer bekommt zwei Fehler-Toasts, und der zweite gibt ihm die Schuld** | BEWIESEN | Conversion/UX | S | Der Nutzer glaubt, sein Input sei unzureichend, und probiert es zwei- bis dreimal mit mehr Text — dieselbe 404. Danach ist das zentrale Differenzierungsversprechen ('60 Sekunden dank KI') verbrannt, und zwar im ersten Kontakt mit dem Produk |
| P0.18 | **Der Preis erscheint erst NACH der Registrierung — und widerspricht den echten AGB** | BEWIESEN | Conversion/UX | S | Preis-Reveal nach dem Commitment ist die klassischste Vertrauensfalle im B2B-Funnel und erzeugt genau an der Unterschrift den Abbruch, den man vermeiden wollte. Wer trotzdem unterschreibt und später /agb liest, findet eine abweichende Aussa |
| P0.19 | **Kunde kann jobs.status selbst auf 'published' setzen — die Matchunt-Freigabe ist reine Frontend-Konvention** | ABGELEITET | Backend/Security | S | Ein authentifizierter Kunde setzt per PATCH /rest/v1/jobs?id=eq.<X> {"status":"published"} seine Stelle live: sofort für alle Recruiter sichtbar, ohne Matchunt-Review, ohne fee_percentage/recruiter_fee_percentage (=> Provisionsanspruch ohne |
| P0.20 | **„Entwurf fortsetzen" überschreibt client_id — stille Eigentumsübernahme fremder Stellen** | ABGELEITET | Backend/Security | S | Org-owner/admin/hr, die per can_access_job ohnehin alle Org-Jobs sehen, reißen beim Weiterbearbeiten die Eigentümerschaft des Entwurfs an sich. Für Matchunt-Admins ist es mandantenübergreifend: NeueStelleBar.tsx:38-43 zeigt ihnen die 2 neue |
| P0.21 | **client_verifications ist selbst-schreibbar (KYC/Vertrag), und der neue Intake-Pfad prüft die Verifizierung überhaupt nicht mehr** | BEWIESEN | Backend/Security | M | PATCH /rest/v1/client_verifications?client_id=eq.<self> {"kyc_status":"verified","contract_signed":true,"terms_accepted":true,"kyc_verified_by":"<beliebig>"} macht jeden Account voll verifiziert — ohne Vertrag, ohne Handelsregister-/USt-Prü |
| P0.22 | **Es gibt keinen einzigen Codepfad, der eine Organisation anlegt — das gesamte Mandantenmodell ist unerreichbar** | BEWIESEN | Architektur | M | Für neue Kunden existieren Team, Rollen (hr/hiring_manager/viewer), Job-Scoping, interne Freigabe und Audit-Log nicht — nicht als Fehler, sondern als stille Abwesenheit. Eine komplette Migration, 5 SECURITY-DEFINER-Funktionen und 3 Edge Fun |
| P0.23 | **Team-Einladung ist produktiv tot: validate-invite nicht deployt, Fallback-Policy zuvor gelöscht** | BEWIESEN | Architektur | S | Ein Kunde kann faktisch niemanden in sein Team holen. Die Rollen hr, hiring_manager, viewer und finance sind in Produktion nicht instanziierbar — und alles, was darauf aufbaut (interne Intake-Freigabe, Job-Scoping, JobTeam in src/components |
| P0.24 | **Das Freigabe-Gate existiert nur in der UI — RLS erlaubt dem Kunden Selbst-Publish samt eigener Fee** | ABGELEITET | Architektur | M | Der einzige kommerzielle Kontrollpunkt der Plattform — Matchunt legt die Konditionen fest, bevor Recruiter arbeiten — ist umgehbar. Ein Kunde kann eine ungeprüfte Stelle mit selbstgewählter Recruiter-Fee in den Marktplatz stellen; Recruiter |
| P0.25 | **Die interne Kundenfreigabe ist über die Job-Detailseite produktiv unerreichbar — der Fallback ignoriert das Freigabe-Ziel** | BEWIESEN | Architektur | S | Der Freigabe-Schalter in der Team-Verwaltung (src/pages/organization/TeamManagement.tsx:82-86) verspricht eine Kontrolle, die es auf dem Hauptweg nicht gibt — ein Kunde, der Compliance-Freigabe aktiviert, glaubt sie zu haben. Da der zweite  |
| P0.26 | **'filled' ist ein Zustand ohne Heimat — das Umsatzsignal 'über Matchunt besetzt' wird ersatzlos verworfen** | BEWIESEN | Architektur | M | Matchunt erfährt nie aus dem System, dass eine Stelle über die Plattform besetzt wurde; Rechnungsstellung und Erfolgsquote sind nicht datengetrieben rekonstruierbar. Im Admin-Backlog erscheinen besetzte Stellen als 'Entwurf' und verfälschen |
| P0.27 | **P0 Datenleck: outreach_emails, outreach_send_queue und outreach_rate_limits sind mit dem anon-Key aus dem offenen Internet lesbar** | BEWIESEN | RevOps | S | Meldepflichtige Verletzung des Schutzes personenbezogener Daten nach Art. 33 DSGVO (72-Stunden-Frist ab Kenntnis). Der anon-Key steckt im ausgelieferten Frontend-Bundle von matchunt.ai und ist damit fuer jeden Besucher extrahierbar; es brau |
| P0.28 | **P0 Schreibzugriff: Die Leak-Policies erlauben anonymen Nutzern zusaetzlich INSERT, UPDATE und DELETE** | ABGELEITET | RevOps | S | Heute nur latent, weil process-outreach-queue nicht deployed ist und noch nie eine Mail versendet wurde. Genau deshalb ist der Zeitpunkt jetzt guenstig. Sobald das Modul in Betrieb geht, wird daraus ein offenes Mail-Relay auf einer Domain,  |
| P0.29 | **Das Outreach-Modul ist in Produktion funktionslos: 7 von 8 Edge Functions liefern 404** | BEWIESEN | RevOps | M | Der gesamte im Repository liegende Aufwand (rund 30 Komponenten, ueber 400 KB Frontend-Code, 8 Edge Functions) erzeugt in Produktion exakt null Vertriebskontakte. Jede Roadmap-Diskussion ueber Kampagnen-Optimierung, Sequenz-Tuning oder Betr |
| P0.30 | **Es wurde noch nie eine E-Mail versendet; die Warteschlange scheitert seit Januar an einem ungueltigen Absenderfeld** | BEWIESEN | RevOps | S | Der einzige gemessene Funnel-Zustand ist ein Totalausfall. Alle Kennzahlen im Dashboard von OutreachSlim (Warm, In Kontakt, Deals) stehen zwangslaeufig auf null, was intern faelschlich als schwache Zielgruppe statt als technischer Defekt ge |
| P0.31 | **backend-security F4: die vorgeschlagene WITH-CHECK-Subquery legt jedes UPDATE auf jobs lahm** | ABGELEITET | Red Team | S | Wird diese Empfehlung als P1 mit Aufwand S eingeplant und umgesetzt, kann anschliessend KEIN Kunde und kein Team-Mitglied mehr irgendeine Stelle aendern - Entwuerfe speichern, einreichen, schliessen, alles tot. Das ist ein groesserer Produk |
| P0.32 | **datenmodell und frontend-a11y wollen draft_state bzw. Tagessatz in briefing_notes legen - genau das Feld, das der Studio bei jedem Speichern ueberschr** | BEWIESEN | Red Team | S | Umgesetzt entsteht ein Fehler, der schlimmer ist als der heutige stille Verlust: der Entwurf sieht gespeichert aus, der Ablehnungsgrund verschwindet, und die Job-Liste zeigt willkuerliche JSON-Fragmente als Ablehnungsgrund an. |
| P0.33 | **Der Studio zerstoert beim Ergaenzen eines zurueckgegebenen Jobs den Ablehnungsgrund - von keinem der vier Agenten gefunden** | BEWIESEN | Red Team | S | Der Kunde liest den Grund einmal, klickt auf Ergaenzen, speichert - und kann nie wieder nachsehen, was Matchunt beanstandet hat. Er reicht denselben Mangel erneut ein, Matchunt lehnt erneut ab. Auch der Support kann den Grund nicht rekonstr |
| P0.34 | **Recruiter koennen die Marge von Matchunt direkt auslesen - von keinem Agenten gefunden** | BEWIESEN | Red Team | S | Jeder Recruiter kann die Marge von Matchunt pro Stelle berechnen (Default 20 minus 15 = 5 Prozentpunkte) und ueber alle sichtbaren Jobs aggregieren. Das ist die sensibelste Zahl des Geschaeftsmodells und die direkte Grundlage fuer Nachverha |
| P0.35 | **Auch ohne Entwurfs-Speichern gehen 100 Prozent der KI-Briefing-Antworten verloren - die vier Audits beschreiben nur den Draft-Fall** | BEWIESEN | Red Team | S | Sobald intake-questions deployed wird - der offensichtliche naechste Schritt - erzeugt jede Jobaufnahme ein perfekt aussehendes Frontend-Erlebnis mit vollstaendig leerem Ergebnis. Das ist gefaehrlicher als der heutige Zustand, in dem der Fl |

### P1 — 42 Befunde

| # | Befund | Stufe | Quelle | Aufwand | Auswirkung |
|---|---|---|---|---|---|
| P1.1 | **Der Nutzer sieht bei jedem Import zwei Fehlermeldungen: eine englische Rohmeldung und eine, die ihm die Schuld gibt** | BEWIESEN | Frontend/A11y | S | Der Nutzer probiert es mehrfach mit immer mehr Text (der zweite Toast fordert genau das), lädt dabei ggf. mehrfach dieselbe PDF hoch und schließt daraus, dass die KI schlecht ist — statt dass ein Backend fehlt. Support-Tickets kommen mit de |
| P1.2 | **Tote primäre CTA am Ende des Briefings: „Weiter zur Übergabe" und „Fertig" tun nichts** | BEWIESEN | Frontend/A11y | S | Der Nutzer klickt am emotionalen Höhepunkt des Flows („Briefing steht") auf den offensichtlichsten Weiter-Button und es passiert nichts. Auf Mobil, wo die echte Übergabe-Leiste außerhalb des Viewports liegt (Befund 9), ist das ein harter Ab |
| P1.3 | **„PDF" und „Link" im Dashboard-Launcher machen nicht, was sie ankündigen — mit Text im Feld starten sie sogar den falschen Parser** | BEWIESEN | Frontend/A11y | S | Zwei von drei beworbenen Importwegen sind nicht nur backend-seitig tot, sondern schon im Frontend nicht erreichbar. Selbst nach Deploy der Edge Functions bliebe der PDF-Weg über das Dashboard unbenutzbar. Für den Nutzer wirkt das wie Willkü |
| P1.4 | **Der neue Flow ist für Screenreader und Tastatur unbedienbar — der alte war es nicht (Design-System-Regression)** | BEWIESEN | Frontend/A11y | M | Ein blinder oder tastaturabhängiger Nutzer kann das Profilformular nicht ausfüllen (er hört „Eingabefeld, leer" zehnmal hintereinander) und das Briefing praktisch nicht durcharbeiten. Der alte Flow war hier nachweislich besser — der Neubau  |
| P1.5 | **Auf dem Smartphone ist der Flow nicht sinnvoll abschließbar: 16-32px-Ziele, 12px-Text mit iOS-Zoom, kein Fortschritt, Briefing zwei Bildschirme tief** | BEWIESEN | Frontend/A11y | L | Der Flow ist mobil formal erreichbar, aber praktisch nicht abschließbar: Man kann Skills kaum entfernen (16px), verliert bei jedem Feldfokus die Fenstergeometrie, sieht keinen Fortschritt, muss an der kompletten Profil-Sektion vorbeiscrolle |
| P1.6 | **Freelance-Jobs haben in Produktion gar kein Preisfeld — der Gehaltsteil des Matchings ist blind** | BEWIESEN | Datenmodell | M | Jeder Freelancer „passt ins Budget", egal ob er 600 oder 1400 EUR/Tag aufruft. Die Salary-Constraint (Gewicht 0.12) und der Salary-Dealbreaker sind fuer den gesamten Contracting-Bestand neutralisiert. Auch nach der Hybrid-Migration bleibt d |
| P1.7 | **Der Recruiter sieht vom gesamten Briefing exakt null Felder** | BEWIESEN | Datenmodell | L | Der komplette Aufwand der Jobaufnahme hat null Wirkung auf die Recruiter-Arbeit. Recruiter arbeiten weiter mit Titel + Skill-Arrays. Zusaetzlich bleibt job_summary fuer neu angelegte Jobs dauerhaft leer, weil der Generator nicht deployed is |
| P1.8 | **Zwei Aufnahmepfade schreiben unterschiedliche Spalten fuer dieselben Fakten (Split-Brain)** | BEWIESEN | Datenmodell | M | Fuer alle Studio-Jobs zeigt das KI-Fit-Assessment „Remote: N/A" — der LLM bewertet Standort-/Modell-Passung ohne die Information. Fuer alle Legacy-Jobs ist das Briefing in intake_briefing, fuer neue in briefing_notes; jede Auswertung ueber  |
| P1.9 | **Der Parser liefert 26 Felder, der Studio uebernimmt 20 — sechs davon haetten eine vorhandene Zielspalte** | BEWIESEN | Datenmodell | M | Selbst wenn die Import-Functions wieder deployed sind, kommt nur die Haelfte der extrahierten Daten an. Der Erfahrungs-Score bleibt konstant 100 (jobMinYears=0, jobMaxYears=20 -> :1391 immer wahr), weil die einzige Quelle fuer experience_mi |
| P1.10 | **Falschaussage "Ihr Account ist jetzt vollständig verifiziert" bei kyc_status='in_review' — und das KYC-Gate ist ohnehin wirkungslos** | BEWIESEN | Recht | M | (1) Irreführende geschäftliche Handlung gegenüber einem Unternehmer, § 5 Abs. 1, Abs. 2 Nr. 1 UWG — Abmahnrisiko durch Wettbewerber. (2) Das "verifizierte Unternehmen"-Versprechen, mit dem Recruiter zur Mitarbeit bewegt werden, ist unbelegt |
| P1.11 | **"Digitale Unterschrift" als Textfeld ohne Dokument, ohne Zeitstempel-Kontext und ohne Vertretungsprüfung — Beweiswert nahe null** | BEWIESEN | Recht | M | Formell unschädlich: Der Nutzungs-/Mäklervertrag ist formfrei, eine einfache elektronische Signatur ist nach Art. 25 Abs. 1 eIDAS nicht wirkungslos. Praktisch aber: kein Anscheinsbeweis (§ 371a ZPO greift nur bei qualifizierter Signatur), f |
| P1.12 | **EU AI Act: Matchunt stuft sich selbst als Anbieter eines Hochrisiko-KI-Systems ein — die Anbieterartefakte existieren im Code nicht** | ABGELEITET | Recht | XL | Zwei Risiken gleichzeitig: (1) Aufsichtsrechtlich ist die Selbsteinstufung ein schriftliches Eingeständnis der Anbieterrolle ohne die zugehörigen Nachweise — bei einer Marktüberwachungsanfrage nach Art. 21 KI-VO muss binnen Frist technische |
| P1.13 | **DSGVO-Rollenverteilung widerspricht sich zwischen /datenschutz und /agb; weder AVV noch Art.-26-Vereinbarung existieren im Produkt** | BEWIESEN | Recht | L | Ein Betroffener bekommt auf die Frage "wer ist verantwortlich?" zwei verschiedene Antworten von derselben Firma — das allein trägt eine Beschwerde. Fehlt bei gemeinsamer Verantwortlichkeit die Vereinbarung nach Art. 26 Abs. 1 DSGVO bzw. bei |
| P1.14 | **KI-Jobaufnahme sendet ungefilterte Interna an OpenRouter — von der Datenschutzerklärung nicht gedeckt, ohne Redaktion und ohne Protokoll** | BEWIESEN | Recht | M | Verarbeitung ohne passende Transparenzinformation (Art. 13 DSGVO) und ohne Drittlandsprüfung für genau diesen Datenfluss (Art. 44 ff.). Geschäftlich schwerer: eine No-Go-Firmenliste oder ein Entscheidername in einem US-Gateway-Log ist ein V |
| P1.15 | **Der Verifikations-Banner nagt dauerhaft, die Erfolgsmeldung ist unwahr, und der CTA führt in eine Schleife mit leeren Feldern** | BEWIESEN | Conversion/UX | S | Ein rot-alarmierender Dauer-Banner an oberster Stelle des Dashboards erzeugt binnen zwei Sitzungen Alarm-Blindheit — danach werden auch echte Handlungsaufforderungen (Kandidat entscheiden, Interview bestätigen) überlesen. Zwischenzeitlich g |
| P1.16 | **Die 'Briefing-Reife' ist ein Motivations-Placebo: der Score steigt beim Überspringen und wird grün bei einer unbesetzbaren Stelle** | BEWIESEN | Conversion/UX | M | Ein Score, der beim Ausweichen steigt, trainiert genau das Ausweichen. Der Kunde optimiert auf die Zahl statt auf die Aussage, übergibt ein leeres Briefing im guten Glauben und macht später die Plattform für schlechte Kandidaten verantwortl |
| P1.17 | **Es gibt keinen Passwort-Reset — und der Wert kommt laut eigener Anzeige erst in der zweiten Sitzung** | BEWIESEN | Conversion/UX | S | Genau die Kohorte, die den Funnel vollständig durchlaufen hat und für die eine Benachrichtigung 'Erste Kandidaten sind da' auslöst, kann beim Rückkehrversuch scheitern und hat dann keinen anderen Weg als Support-Kontakt — der im Produkt nir |
| P1.18 | **Nach 'Konto erstellen' gibt es keinen 'E-Mail bestätigen'-Zustand, und der Bestätigungslink führt auf die Marketing-Startseite** | ABGELEITET | Conversion/UX | S | Der Nutzer wartet vor einem Formular, das so aussieht als sei nichts passiert, und versucht es oft ein zweites Mal — dann greift 'Diese E-Mail ist bereits registriert' (de.ts) und wirkt wie ein Fehler seinerseits. Wer die Mail findet, wird  |
| P1.19 | **RLS-Antipattern: WITH CHECK (can_edit_job(id)) validiert die ALTE Zeile — client_id und organization_id sind unkontrolliert schreibbar** | ABGELEITET | Backend/Security | S | Zwei Mandantenbrüche in einem: (1) client_id auf beliebige UUID umschreibbar (siehe Ownership-Finding, trivial ausnutzbar mit der eigenen UUID). (2) `UPDATE jobs SET organization_id = '<fremde Org-UUID>'` schiebt einen Job in einen fremden  |
| P1.20 | **notifications INSERT mit WITH CHECK (true) — jeder darf im Namen des Systems an jeden schreiben** | BEWIESEN | Backend/Security | S | Jeder authentifizierte Nutzer — insbesondere ein Recruiter — kann beliebigen bekannten User-UUIDs Notifications mit frei gewähltem type/title/message/related_id unterschieben. In-App-Phishing („Ihre Stelle ist live — bitte hier bestätigen") |
| P1.21 | **Alle Autorisierungs-Helfer sind ohne Login aufrufbar und akzeptieren eine fremde _user_id — Berechtigungs-Orakel** | BEWIESEN | Backend/Security | S | Wer UUIDs kennt (aus Submissions, job_collaborators, profiles der eigenen Org, alten Links), kann pre-auth den kompletten Mandantengraph abfragen: Wer ist Plattform-Admin (has_role(...,'admin')), wer gehört zu welcher Org und in welcher Rol |
| P1.22 | **Team-Einladungen sind produktiv tot: organization-invite fehlt, accept-invite ist offen und ungebremst deployed** | BEWIESEN | Backend/Security | M | (1) Kein Kunde kann heute jemanden einladen — damit sind Rollen (hr/hiring_manager/viewer), job_collaborators, die interne Intake-Freigabe (pending_client_approval) und „an Fachbereich geben" (JobIntakeStudio.tsx:707-731) faktisch unerreich |
| P1.23 | **Die schützende Policy hinter NeueStelleBar ist can_access_job — mandantenübergreifend dicht, innerhalb des Mandanten offen** | BEWIESEN | Backend/Security | S | Cross-Tenant ist sauber geschützt (Antwort auf Frage 2: nein, fremde Kunden-Entwürfe sind für normale Nutzer nicht sichtbar). Intra-Tenant nicht: owner/admin/hr bekommen die halbfertigen Entwürfe ihrer Hiring Manager angeboten — inklusive G |
| P1.24 | **Der neue Intake-Pfad hat keinen manuellen Ausweg — er ist strikt fragiler als der Pfad, den er ersetzen soll** | BEWIESEN | Architektur | S | Die Nachfolgeoberfläche kann ohne Edge Functions überhaupt keinen Job erzeugen, nicht einmal einen Entwurf. Das ist eine lokale Optimierung (KI-first, kein Formular-Ballast), die das Gesamtsystem fail-closed macht: der Ausfall EINER Edge Fu |
| P1.25 | **Der neue Intake-Pfad lässt das Verifizierungs-Gate fallen, und das Onboarding meldet einen Erfolg, den es nicht gibt** | BEWIESEN | Architektur | M | Vertragsannahme und KYC sind keine Kontrolle, sondern eine Zeremonie: der neue Weg fragt gar nicht, der alte fragt nach einem Wert, den der Kunde selbst gesetzt hat. Die falsche Erfolgsmeldung erzeugt zusätzlich Support-Last, weil der Kunde |
| P1.26 | **Delegation an den Fachbereich erzeugt Benachrichtigungen zu Jobs, die der Empfänger per RLS nicht öffnen kann** | ABGELEITET | Architektur | S | Das Feature 'an Fachbereich geben' produziert Sackgassen-Benachrichtigungen. Der Kunde erlebt es als Datenverlust ('mein Kollege sieht nichts'), und weil der Absender keinen Fehler sieht (der Insert gelingt), fällt es erst im Support auf. S |
| P1.27 | **Zwei Intake-Pfade schreiben das Briefing in zwei verschiedene Spalten — der Verbraucher liest nur eine davon** | BEWIESEN | Architektur | M | Ein über das Studio angelegter Job erzeugt für Recruiter die Zusammenfassung 'Kein Briefing vorhanden' — der teuerste Teil der Jobaufnahme kommt beim Empfänger nicht an. Jede spätere Auswertung ('wie vollständig sind unsere Briefings?') mus |
| P1.28 | **'Später weiter' und 'Entwurf fortsetzen' verlieren produktiv den kompletten Studio-Zustand** | BEWIESEN | Architektur | S | Die Fortsetzen-Chips (src/components/dashboard/NeueStelleBar.tsx:114-128) und der Button 'Später weiter' (JobIntakeStudio.tsx:522-525) versprechen Kontinuität, die der Speicher nicht halten kann: wer die Aufnahme unterbricht, beantwortet be |
| P1.29 | **Der Akquise-Link, auf dem der gesamte Ziel-Flow aufbaut, existiert nirgends im Code** | BEWIESEN | RevOps | L | Attribution ist derzeit nicht unvollstaendig, sondern unmoeglich. Jede Aussage ueber Conversion-Raten waere frei erfunden. Operativ bedeutet der Demo-CTA ausserdem, dass jeder Lead manuelle Vertriebszeit bindet, was das Modell bei kleinen u |
| P1.30 | **AdminOutreach.tsx mit 39,6 KB ist toter Code; die produktiv erreichbare Oberflaeche ist nur ein Bruchteil des Moduls** | BEWIESEN | RevOps | M | Jede Analyse und jede Schaetzung, die vom Funktionsumfang der Dateien ausgeht, ueberschaetzt das Modul massiv. Konkret fuer die Attribution: Kampagnen und Sequenzen sind zwar in der Datenbank modelliert, aber ohne Oberflaeche nicht pflegbar |
| P1.31 | **Keine Verkaeufer-Attribution: outreach_leads besitzt kein Eigentuemer-Feld** | BEWIESEN | RevOps | M | Die geforderte Kette Verkaeufer -> Akquise-Link -> Kontakt -> Unternehmen -> Deal beginnt mit einer Dimension, die das Datenmodell nicht kennt. Sobald mehr als eine Person Vertrieb macht, ist weder Verantwortlichkeit noch Erfolgsbeitrag zur |
| P1.32 | **Der Konversionszeitpunkt wird nirgends geschrieben: converted_at ist ein totes Feld** | BEWIESEN | RevOps | L | Das Ende der geforderten Kette fehlt genauso wie ihr Anfang. Ohne Verknuepfung laesst sich weder ein Umsatz einer Akquise-Anstrengung zuordnen noch eine Aussage darueber treffen, welche Zielgruppe, Kampagne oder Ansprache tatsaechlich zahle |
| P1.33 | **Sequenzen und Follow-ups sind modelliert, laufen aber nie: es existiert kein Scheduler** | BEWIESEN | RevOps | M | Follow-ups sind im B2B-Kaltkontakt der ueberwiegende Teil der Antworten. Ohne Automatik entfaellt dieser Anteil vollstaendig, und die verbleibende Erstkontaktquote wird faelschlich als Leistungsgrenze des Kanals interpretiert. Zusaetzlich i |
| P1.34 | **DSGVO: Tracking-Pixel ohne Einwilligung und Widerspruch nur als Fliesstext statt als Ein-Klick-Abmeldung** | BEWIESEN | RevOps | M | Bussgeldrisiko und Abmahnrisiko nach UWG, beides realistisch erst mit dem ersten echten Versand. Zusaetzlich ein handfestes Zustellbarkeitsproblem: Ohne List-Unsubscribe-Header stufen grosse Postfachanbieter Massenversand schlechter ein, wa |
| P1.35 | **platform_events kann anonyme Akquise-Ereignisse strukturell nicht aufnehmen, weil user_id NOT NULL ist** | BEWIESEN | RevOps | M | Ohne Anpassung wird das Event-Schema entweder unvollstaendig oder es entsteht eine zweite, ungesicherte Ereignistabelle daneben, was das Muster der zwei parallelen Generationen erneut wiederholt. Zusaetzlich ist mangels Analytics-Werkzeug d |
| P1.36 | **Fehlende Grenze zwischen CRM- und Produktdaten: die Anreicherungsfelder liegen bereits im falschen Objekt** | BEWIESEN | RevOps | L | Ohne klare Grenze entsteht bei jeder Attributionserweiterung ein neuer Kandidat fuer einen Reveal-Verstoss, weil Attribution ihrer Natur nach Verknuepfungen schafft. Operativ kommen widerspruechliche Firmenstammdaten und doppelte Pflege hin |
| P1.37 | **DARK PATTERN: Weiss-ich-nicht zaehlt als halbe Antwort und oeffnet das Einreichungs-Gate** | BEWIESEN | Red Team | S | Das Gate, das inhaltsleere Stellen von Matchunt fernhalten soll, ist durch reines Durchklicken passierbar - und der Kunde bekommt dabei ein gruenes Fortschrittssignal, das ihm sagt, er habe die halbe Arbeit erledigt. Fuer den Recruiter komm |
| P1.38 | **DARK PATTERN: der Erfolgsschirm zeigt einen ewig drehenden Spinner und zwei erfundene Zeitzusagen** | BEWIESEN | Red Team | S | Der Kunde bekommt an der Stelle, an der er am ehesten Vertrauen fasst, drei Signale, die keine Deckung haben - ein laufender Prozess, eine Bearbeitungszeit und eine Lieferzeit. Wird eine davon gerissen, ist es kein Missverstaendnis, sondern |
| P1.39 | **KI-THEATER: die Punktzahlen im Qualitaets-Check sind erfunden und die angezeigte Briefing-Reife existiert in zwei widersprechenden Fassungen** | BEWIESEN | Red Team | S | Der Kunde optimiert gegen eine Anzeige, die ihm falsche Gegenwerte nennt, und sieht anschliessend eine dritte Zahl auf der Detailseite. Sobald das jemandem auffaellt, ist nicht nur dieser Score entwertet, sondern jede Zahl im Produkt - inkl |
| P1.40 | **UNREALISTISCHE AUTOMATISIERUNG: eine vom LLM selbst vergebene Note ist das harte Einreichungs-Gate** | BEWIESEN | Red Team | M | Nach dem Deploy von intake-questions ersetzt eine nicht reproduzierbare Modellzahl die heutige deterministische Berechnung. Ein Kunde sieht seine Reife ohne eigenes Zutun sinken; ein zu grosszuegiges Modell schaltet das Gate frei, ein zu st |
| P1.41 | **Der AGG-Waechter schaltet sich genau dann ab, wenn die KI-Befragung funktioniert** | BEWIESEN | Red Team | S | Ein Diskriminierungs-Waechter, der genau im Zielzustand des Produkts nichts mehr prueft, ist keine Kontrolle, sondern ein Alibi. Ein Kunde kann heute schon 'keine Berufsrueckkehrerinnen' in die Muss-Kriterien schreiben und bekommt keinen Hi |
| P1.42 | **Widerspruch a11y gegen datenmodell zum stillen Speicherverlust - datenmodells Fassung wuerde den Studio komplett unbrauchbar machen** | ABGELEITET | Red Team | S | Umgesetzt waere der neue Intake-Pfad nicht mehr degradiert, sondern tot - und zwar sofort nach dem naechsten Lovable-Publish, ohne dass irgendeine Migration schiefgegangen waere. |

### P2 — 24 Befunde

| # | Befund | Stufe | Quelle | Aufwand | Auswirkung |
|---|---|---|---|---|---|
| P2.1 | **Künstliche Aufbau-Animation kostet ~2,3 s pro Job, lässt sich nicht überspringen und ignoriert prefers-reduced-motion** | BEWIESEN | Frontend/A11y | S | Beim ersten Job ist die Animation ein sinnvolles Vertrauenssignal („die KI hat wirklich gearbeitet"). Ab dem zweiten ist sie reine Reibung — und für einen Power-User, der zehn Stellen einstellt, 23 Sekunden erzwungene Wartezeit auf nichts.  |
| P2.2 | **Kein Code-Splitting: jeder eingeloggte Nutzer lädt 2,09 MB (495 kB gzip) JavaScript in einem Chunk** | BEWIESEN | Frontend/A11y | M | 495 kB gzip vor dem ersten Pixel — auf DACH-Mobilfunk (LTE, ~2-4 Mbit real) sind das mehrere Sekunden Parse+Execute zusätzlich zur Übertragung, auf Mid-Range-Android deutlich mehr. Das trifft genau die Nutzer, für die der neue Flow gebaut w |
| P2.3 | **Zwei parallele Design-Systeme und ein fehlender Verifizierungs-Gate im neuen Flow** | BEWIESEN | Frontend/A11y | L | Gleiche Aufgabe, zwei Oberflächen mit unterschiedlicher Schriftskala, unterschiedlichen Feldhöhen und unterschiedlichen Regeln — je nachdem, ob der Nutzer über das Dashboard oder über Suche/Shortcut einsteigt. Das ist teuer in Wartung (jede |
| P2.4 | **Vier Stellen erzeugen unstrukturierte KI-Schattenfelder statt typisierter Fakten** | BEWIESEN | Datenmodell | L | Der komplette Produktionsbestand hat null typisierte Briefing-Felder. Die red_list — die einzige Deanonymisierungs-Bremse — ist nicht maschinell durchsetzbar, weil sie keine Feldreferenz traegt; kein Redaktionslauf konsumiert sie. Die tensi |
| P2.5 | **Dubletten: 8 Faktenklassen ohne definierte Praezedenz zwischen jobs, company_profiles und job_skill_requirements** | BEWIESEN | Datenmodell | L | Sechs Job-Spalten fuer die Aussage „Arbeitsmodell", sechs Arrays plus eine Tabelle fuer „Anforderung". Aendert der Kunde sein Firmenprofil, bleiben alle bestehenden Jobs auf dem alten Stand — ohne dass irgendwo steht, dass der Job-Wert eine |
| P2.6 | **Kanonisches Zielmodell: acht Klassen statt einer flachen 90-Spalten-Tabelle** | ABGELEITET | Datenmodell | XL | Jede Erweiterung der Jobaufnahme landet per Default in einem Blob und ist damit wirkungslos. Ohne Klassenmodell wiederholt sich der Fehler bei jedem neuen Feld. |
| P2.7 | **Die consents-Tabelle kann keine Einwilligung nachweisen: Insert für jeden erlaubt, gespeichert wird die Selbstauskunft des Recruiters** | BEWIESEN | Recht | M | Art. 7 Abs. 1 i.V.m. Art. 5 Abs. 2 DSGVO: Wer sich auf Einwilligung stützt, muss sie nachweisen. Ein Recruiter-Häkchen ist kein Nachweis der Kandidateneinwilligung, sondern eine Zusicherung im Recruiter-Vertrag — und die steht ausgerechnet  |
| P2.8 | **Gast-Draft am Akquise-Link: Rechtsrahmen muss vor der ersten Zeile Code stehen, nicht danach** | ABGELEITET | Recht | M | Ohne vorab definierte Rechtsgrundlage entsteht ein Datenbestand ohne Löschkonzept — exakt der Fall, den Aufsichtsbehörden bei Lead-Formularen prüfen. Wird der Draft KI-verarbeitet (intake-questions), kommt der Drittlandtransfer aus Finding  |
| P2.9 | **Der Publish-Blocker wirft das komplett ausgefüllte Stellenformular weg** | BEWIESEN | Conversion/UX | S | Datenverlust nach maximalem Aufwand, ausgelöst durch einen Zustand, den der Nutzer beim Ausfüllen nicht sehen konnte. Das ist der Abbruch mit der höchsten Wahrscheinlichkeit, dass der Kunde nicht zurückkommt — und er hat objektiv recht dami |
| P2.10 | **Die erste Dashboard-Sitzung liefert null Nutzen und gratuliert dem Nutzer dafür** | BEWIESEN | Conversion/UX | M | Der Aufwand-Nutzen-Vergleich der ersten Sitzung ist negativ, und das Produkt bestätigt das visuell. Ohne einen Wertmoment in Sitzung 1 gibt es keinen Grund für Sitzung 2 — und genau in Sitzung 2 läge (siehe Passwort-Finding) ohnehin die näc |
| P2.11 | **Anrede bricht zwischen den ersten Screens, und das zentrale Verkaufswort wird im Produkt nie erklärt** | BEWIESEN | Conversion/UX | S | Der Anredebruch signalisiert ein zusammengesetztes, nicht durchgestaltetes Produkt — in einem Kontext, in dem der Kunde gerade einen Vertrag unterschrieben hat, ist das ein konkretes Vertrauensrisiko. Der unerklärte Jargon kostet an der wic |
| P2.12 | **Die Mandantenzuordnung eines neuen Intakes ist implizit — drei widersprüchliche Definitionen von „meine Organisation"** | ABGELEITET | Backend/Security | M | Bei Mehrfachmitgliedschaft (Interim-HR, Konzernstruktur, Recruiter mit Agentur-Org) landet der Job im falschen Mandanten und ist damit für die owner/admin/hr des falschen Kunden vollständig lesbar. Zusätzlich zeigt „an Fachbereich geben" Mi |
| P2.13 | **Audit-Trail für Statuswechsel existiert, ist für Kunden aber strukturell leer und protokolliert die komplette Zeile** | BEWIESEN | Backend/Security | M | (1) Kein Kunden-Admin sieht je einen Job-Statuswechsel — der Nachweis „wer hat wann freigegeben / wer hat die Stelle geschlossen" ist für den Kunden nicht führbar, obwohl die Migration ihn als DSGVO-Feature ankündigt (Kommentar 202607101300 |
| P2.14 | **insertJobWithIntake ist nicht idempotent: getrennte Locks und fehlender Retry-Schutz erzeugen Doppel-Jobs** | ABGELEITET | Backend/Security | S | Klick auf „Später weiter" und unmittelbar danach auf „An Recruiter übergeben" (der zweite Button ist nicht gesperrt) erzeugt zwei INSERTs, weil beide draftJobId=null sehen: ein verwaister Entwurf plus eine Einreichung. Bei Timeout/Retry auf |
| P2.15 | **can_create_job wird durch den eigenen Backfill entwertet und ist bei _user_id IS NULL fail-open** | ABGELEITET | Backend/Security | S | Ein bewusst als viewer oder finance eingeladenes Mitglied darf trotzdem Jobs anlegen (INSERT-Policy 20260710130000:452-453 `WITH CHECK (auth.uid() = client_id AND public.can_create_job(auth.uid()))`). Die Rollentrennung, die das ganze Team- |
| P2.16 | **Das Rollenmodell widerspricht sich über vier Ebenen; Viewer und Finance sehen einen Weg, den die Datenbank ihnen verweigert** | ABGELEITET | Architektur | M | Die Berechtigungswahrheit liegt an vier Orten, die sich nicht kennen: CHECK-Constraint, SQL-Helper, TS-Typ, UI-Rendering. Jede Rollenänderung muss an allen vier Orten nachgezogen werden, Abweichungen fallen erst als Nutzerfehler auf. Für de |
| P2.17 | **resolveIntakeSubmitTarget entscheidet mandantenblind und kann eine andere Organisation benachrichtigen als die des Jobs** | ABGELEITET | Architektur | M | Im Mehr-Mandanten-Fall wartet ein Job auf die Freigabe von Organisation A, während er zu Organisation B gehört: Admin/HR von B sehen ihn ohne Benachrichtigung, A bekommt Benachrichtigungen zu einem Job, den sie per RLS nicht öffnen können.  |
| P2.18 | **Entwurf: Lifecycle, Eigentuemerschaft und CRM-Synchronisation entlang der Kette Verkaeufer bis veroeffentlichte Stelle** | ANNAHME | RevOps | L | Ein nicht eindeutiger Lifecycle macht jede Trichter-Auswertung angreifbar, weil dieselbe Firma je nach abgefragtem Feld in zwei verschiedenen Stufen steht. Bei einer spaeteren CRM-Einfuehrung waere eine Migration ohne definierte Uebergaenge |
| P2.19 | **Entwurf: vollstaendiges Event-Schema fuer den Akquise-Trichter mit Ausloeser und DSGVO-Bewertung** | ANNAHME | RevOps | L | Bei sauberer Umsetzung wird der Trichter erstmals messbar. Bei unsauberer Umsetzung entsteht ein zweites Datenschutzproblem derselben Art wie das Zaehlpixel aus Befund 10. |
| P2.20 | **Entwurf: North-Star-Metrik und Kennzahlenbaum mit Zielwerten und dem jeweils beweisenden Instrument** | ANNAHME | RevOps | M | Bei richtiger Leitgroesse steuert sich der Kanal ueber Qualitaet. Bei falscher Leitgroesse entsteht messbar viel Aktivitaet ohne Umsatz, und das Zustellbarkeitsproblem aus Befund 10 verschaerft sich. |
| P2.21 | **architekt F1 ist falsch: es gibt sehr wohl einen Codepfad, der eine Organisation anlegt** | BEWIESEN | Red Team | S | Ein als P0/BEWIESEN gefuehrter Befund - das gesamte Mandantenmodell sei unerreichbar - traegt nicht. Wer danach priorisiert, baut einen Signup-Trigger fuer ein Problem, das nur eine Reihenfolge-Frage ist. Der Restbefund ist real, aber klein |
| P2.22 | **OVERENGINEERING: recruiter_brief mit Redaktionspipeline fuer ein Briefing, dessen einziger Leser 404 liefert** | ABGELEITET | Red Team | S | Ein L-Ticket mit neuer Spalte, neuer Klassifikationslogik und neuer View-Version bindet Wochen, um eine Hypothese zu testen, die eine S-Massnahme in einem Tag beantwortet - und schafft dabei eine zweite Quelle fuer Reveal-Lecks, die dauerha |
| P2.23 | **FRAGEN OHNE WIRKUNG: vacancy_reason, reports_to, hiring_urgency und usps aendern heute nachweislich nichts** | BEWIESEN | Red Team | S | Der Kunde beantwortet Fragen zum Vakanzgrund, zur Berichtslinie und zu den Verkaufsargumenten - genau die Fragen, die einen Intake von einem Formular unterscheiden - und keine davon veraendert irgendeine Entscheidung. Das ist der teuerste T |
| P2.24 | **Kleinere Beleg-Ungenauigkeiten, die die Priorisierung verzerren** | BEWIESEN | Red Team | S | Fuer sich harmlos, in der Summe teuer: wenn zwei von fuenf gepruefeten BEWIESEN-Belegen bei genauer Lektuere ueberzeichnen, verliert die gesamte Liste ihre Verhandlungskraft gegenueber Entwicklung und Geschaeftsfuehrung. Der Kern beider Fin |

### P3 — 4 Befunde

| # | Befund | Stufe | Quelle | Aufwand | Auswirkung |
|---|---|---|---|---|---|
| P3.1 | **types.ts ist gegenueber den Migrationen stale — der Compiler kann keinen dieser Fehler fangen** | BEWIESEN | Datenmodell | S | Weder das Schreiben in nicht-existierende Spalten (intake_payload) noch das Lesen nicht-existierender Felder (companyProfile.excluded_companies :156, companyProfile.company_size :160, job.work_model in v3-1:1029/1466, job.must_have_skills : |
| P3.2 | **DSGVO-Selbstbedienung ist tot — genau der Bereich, den ein HR-Leiter vor der Freigabe prüft** | BEWIESEN | Conversion/UX | S | Ein HR-Leiter oder Datenschutzbeauftragter, der vor der internen Freigabe die Betroffenenrechte durchklickt, erhält als Antwort einen Fehler — und schließt daraus zu Recht auf den Reifegrad der Datenschutzumsetzung insgesamt. Das trifft nic |
| P3.3 | **Gast-Draft am Token: sicherheitstechnische Mindestanforderungen, die der heutige Code nicht erfüllt** | ABGELEITET | Backend/Security | L | Würde man den Gast-Draft naiv über eine jobs-Policy bauen, wäre jeder Draft von jedem beanspruchbar bzw. jeder fremde Job per client_id-Umschreibung übernehmbar (siehe WITH-CHECK-Finding). Ohne Rate-Limit wäre der Token-Endpunkt zudem enume |
| P3.4 | **OVERENGINEERING: Sicherheitsspezifikation fuer ein Feature, das es nicht gibt, waehrend der Hauptpfad 404 liefert** | ABGELEITET | Red Team | S | Roadmap-Verzerrung: die teuerste Position der gesamten Sammlung hat null Kundennutzen, solange der eingeloggte Kunde keine Stelle anlegen kann. |

### Belege und Empfehlungen im Detail


**P0.1 · Neuer Flow hat keinen manuellen Ausweg — bei toter Edge Function ist die Jobaufnahme eine Endlosschleife** — `BEWIESEN` · Frontend/A11y · Aufwand S
- *Beleg:* src/components/dashboard/JobIntakeStudio.tsx:238-243 (`startBuild`: `if (!job || !job.title) { toast.error(...); setStage('input'); return; }`); JobIntakeStudio.tsx:535-565 (Input-Stage kennt nur „Profil bauen", „PDF ablegen", „Link" — kein manueller Einstieg); Gegenstück im ALTEN Flow: src/pages/dashboard/CreateJob.tsx:911-923 (Button „Manuell erstellen") und CreateJob.tsx:986-1000 (Fallback-Karte „Import abgebrochen oder fehlgeschlagen" mit zwei Auswegen). Live-Probe: parse-job-url → HTTP 404 NOT_FOUND_FUNCTION_B
- *Problem:* Alle drei Eingänge des neuen Studios (Text, Link, PDF) laufen über Edge Functions, die produktiv 404 liefern. `startBuild` schickt den Nutzer dann nach `stage='input'` zurück — genau dorthin, wo er herkam. Es gibt keinen Zustand, in dem der Nutzer `built` ohne KI-Antwort erreicht: `setBuilt` wird ausschließlich in `startBuild` (Zeile 244) und bei Entwurfs-Hydration (Zeile 292) gesetzt. Der alte Flow hatte für exakt d
- *Empfehlung:* Sofort (ohne Deploy anderer Teile): In der Input-Stage einen dritten Button „Ohne KI manuell anlegen" ergänzen, der `startBuild({...EMPTY_BUILT, title: text.trim() || 'Neue Stelle'})` aufruft und direkt nach `stage='built'` springt (Animation überspringen). Zusätzlich in `startBuild` zwischen „KI nicht erreichbar" und „KI hat nichts erkannt" unterscheiden (die Hooks liefern bereits `error`) und im ersten Fall statt d
- *Akzeptanzkriterium:* Mit deaktiviertem Netzwerk/404 auf parse-job-url erreicht ein Nutzer aus der Input-Stage in höchstens zwei Klicks die `built`-Stage und kann die Stelle einreichen; kein Zustand des Studios ist ohne sichtbaren Vorwärts-Pfad.

**P0.2 · „Später weiter" verspricht Fortsetzbarkeit und löscht in Produktion das komplette Briefing — still** — `BEWIESEN` · Frontend/A11y · Aufwand M
- *Beleg:* src/components/dashboard/JobIntakeStudio.tsx:390-394 (`draft_state: { type, built, answers, freelance, flexibility, revealSetup, dyn }` liegt INNERHALB von `intake_payload`); src/lib/intakeCapture.ts:16-26 (`intake_payload` steht in `EXTENDED_COLUMNS`) und :45-57 (bei fehlender Spalte zweiter Insert nur mit `base`); JobIntakeStudio.tsx:424 (`const { data, error } = await persist('draft')` — `extendedPersisted` wird verworfen) und :432 (`toast.success('Entwurf gespeichert — über „Stelle ausschreiben" jederzeit forts
- *Problem:* `intake_payload` existiert in Produktion nicht (Kontext, Migration 20260619120000 nicht angewandt). intakeCapture fällt korrekt auf einen Insert ohne Extended-Felder zurück — damit verschwindet `draft_state` restlos. Beim Fortsetzen ist `r.intake_payload` `undefined`, alle `ds?.…`-Zugriffe greifen auf die Defaults. Der Nutzer bekommt trotzdem einen grünen Erfolgs-Toast mit explizitem Fortsetzungs-Versprechen.
- *Empfehlung:* Zwei Stufen. (a) Sofort: `extendedPersisted` in `handleSaveDraft`/`handleSubmit` auswerten und bei `false` statt `toast.success` einen `toast.warning` mit klarem Text zeigen („Stammdaten gespeichert — das Briefing konnte noch nicht abgelegt werden, bitte in dieser Sitzung abschließen"), plus den „Später weiter"-Button deaktivieren/mit Hinweis versehen. (b) Richtig: `draft_state` zusätzlich in `briefing_notes` (existi
- *Akzeptanzkriterium:* Wenn `extendedPersisted === false`, erscheint keine Erfolgsmeldung mit Fortsetzungs-Versprechen; ein gespeicherter und wieder geöffneter Entwurf zeigt entweder alle vorherigen Antworten oder eine explizite Warnung, dass sie nicht gespeichert wurden.

**P0.3 · Kein Autosave, kein Verlassen-Schutz: Escape oder ein Klick auf den Overlay verwirft die gesamte Sitzung** — `BEWIESEN` · Frontend/A11y · Aufwand M
- *Beleg:* src/components/dashboard/JobIntakeStudio.tsx:497-499 (`<Dialog open onOpenChange>` + `<DialogContent className="…">` — weder `onEscapeKeyDown` noch `onInteractOutside`/`onPointerDownOutside` gesetzt); src/components/ui/dialog.tsx:36-49 (Radix-Defaults werden unverändert durchgereicht, also Escape und Outside-Click schließen); in JobIntakeStudio.tsx existiert kein `setInterval`, kein `beforeunload`-Listener und kein Aufruf von `persist` außer in `handleSaveDraft` (:421), `handleDelegate` (:435) und `handleSubmit` (:
- *Problem:* Der gesamte Zustand des Studios lebt ausschließlich in React-State. Der einzige Persistenzpfad ist der Button „Später weiter" — und der ist in Produktion wirkungslos (Befund 2). Radix schließt den Dialog bei Escape und bei jedem Klick auf die dunkle Fläche; der `onOpenChange`-Handler (NeueStelleBar.tsx:144-147) schließt kommentarlos.
- *Empfehlung:* `onEscapeKeyDown` und `onInteractOutside` an `DialogContent` abfangen, sobald `stage === 'built'` und `built?.title` gesetzt ist, und stattdessen eine Bestätigung („Entwurf verwerfen?" / „Später weiter") zeigen. Parallel einen debounced Autosave (z. B. 20 s nach der letzten Änderung) auf `persist('draft')` legen, sobald ein Titel existiert. Beides ist unabhängig von der Migration umsetzbar, sobald Befund 2(a) das Spe
- *Akzeptanzkriterium:* Bei gefülltem Profil führen Escape und Outside-Click zu einer Rückfrage statt zum sofortigen Schließen; nach 30 s Inaktivität mit gesetztem Titel existiert eine Draft-Zeile in `jobs`.

**P0.4 · Contracting-Stellen gehen komplett ohne Vergütungsangabe raus — Tagessatz wird garantiert verworfen** — `BEWIESEN` · Frontend/A11y · Aufwand S
- *Beleg:* src/components/dashboard/JobIntakeStudio.tsx:342-343 (`salary_min: isFreelance ? null : …`, `salary_max: isFreelance ? null : …`); JobIntakeStudio.tsx:382 (`contracting: isFreelance ? freelance : null` — ausschließlich innerhalb von `intake_payload`); src/components/dashboard/intake/types.ts:24 (Kommentar: „Contracting-Fork: Tagessatz statt Gehalt (landet in intake_payload.contracting)"); src/lib/intakeCapture.ts:16-26 (`intake_payload` = Extended-Spalte, produktiv nicht vorhanden); Eingabemaske: src/components/das
- *Problem:* Für `type='freelance'` werden `salary_min`/`salary_max` bewusst genullt, weil der Tagessatz „stattdessen" in `intake_payload.contracting` landen soll. Diese Spalte existiert nicht — der Fallback-Insert schreibt nur `base`. Ergebnis: der Job-Datensatz enthält an keiner Stelle eine Zahl zur Vergütung. Zusätzlich zeigt die Studio-Faktenliste bei Freelance ohnehin `'noch ergänzen'` (JobIntakeStudio.tsx:195), der Nutzer h
- *Empfehlung:* Bis zur Migration `freelance.dayRateMin/dayRateMax` zusätzlich in die existierenden Spalten `salary_min`/`salary_max` schreiben (mit `employment_type='freelance'` als Interpretationsschlüssel) oder — sauberer — die Konditionen in den ebenfalls existierenden `briefing_notes`-Text serialisieren. Die harte Nullung in Zeile 342-343 darf erst greifen, wenn ein alternatives Zielfeld nachweislich existiert.
- *Akzeptanzkriterium:* Nach dem Einreichen einer Contracting-Stelle mit eingetragenem Tagessatz ist der Wert in der `jobs`-Zeile in mindestens einer produktiv existierenden Spalte wiederauffindbar.

**P0.5 · Drei Hard-Kill-Felder werden in einen JSONB-Blob geschrieben statt in die vorhandenen Spalten** — `BEWIESEN` · Datenmodell · Aufwand S
- *Beleg:* JobIntakeStudio.tsx:376-380 schreibt required_languages/required_certifications/onsite_required nach intake_payload.typed_extras. calculate-match-v3-1/index.ts:921 (job.required_languages), :944 (job.onsite_required), :953 (job.required_certifications) liest sie aus echten Spalten. REST: jobs?select=required_languages -> 200, required_certifications -> 200, onsite_required -> 200; jobs?select=intake_payload -> 400.
- *Problem:* Die KI normalisiert genau die drei Hard-Kill-Kriterien, die der Matcher als Spalten liest — und der Client legt sie in typed_extras eines JSONB-Felds ab, statt in die Spalten, die es in Produktion bereits gibt. Das ist keine Migrationsluecke, sondern ein falsch gewaehltes Ziel.
- *Empfehlung:* In JobIntakeStudio.buildRecord die drei Felder aus dyn.typedFields direkt in base schreiben: required_languages (jsonb [{code,minLevel}]), required_certifications (text[]), onsite_required (boolean). typed_extras ersatzlos streichen. onsite_required zusaetzlich aus built.remote_type==='onsite' ableiten, damit es auch ohne KI gesetzt ist.
- *Akzeptanzkriterium:* Nach Intake-Submit liefert jobs?select=required_languages,required_certifications,onsite_required fuer den Job nicht-leere Werte; ein Testkandidat ohne die geforderte Sprache erhaelt vom Matcher killed=true, category='language'.

**P0.6 · Der Studio-Pfad persistiert `industry` ueberhaupt nicht** — `BEWIESEN` · Datenmodell · Aufwand S
- *Beleg:* JobIntakeStudio.tsx:332-354 (buildRecord base) enthaelt kein industry-Feld; auch extended (:356-396) nicht. Verwendet wird built.industry an :151 (Prefill aus Firmenprofil), :165 und :247 (Reveal-Descriptor), :198 (UI-Fact), :220 (KI-Draft). CreateJob.tsx:639 schreibt industry. Konsumenten: calculate-match-v3-1:1428 calculateIndustryScore, format-job-for-recruiters (job.industry), recruiter_jobs_view Migration 20260725120000:53.
- *Problem:* Die Branche wird extrahiert, aus dem Firmenprofil gemerged, dem Briefing-Modell uebergeben und im UI angezeigt — aber nie in die Zeile geschrieben. Sie ueberlebt nur als Teilstring im Freitext-Descriptor der Reveal-Huelle.
- *Empfehlung:* industry: built.industry || null in buildRecord.base aufnehmen. Gleichzeitig company_size_band, funding_stage, tech_environment und team_size ergaenzen — alle vier existieren als Spalte (REST 200), stehen in recruiter_jobs_view und werden vom alten Pfad befuellt.
- *Akzeptanzkriterium:* Ein per Studio angelegter Job hat nach Submit jobs.industry gesetzt; recruiter_jobs_view liefert fuer diesen Job industry != null.

**P0.7 · „Spaeter weiter" loescht in Produktion das gesamte beantwortete Briefing** — `BEWIESEN` · Datenmodell · Aufwand M
- *Beleg:* JobIntakeStudio.tsx:392-394 legt draft_state (type, built, answers, freelance, flexibility, revealSetup, dyn) ausschliesslich in intake_payload ab. intakeCapture.ts:45-57: schlaegt der Insert mit den Extended-Spalten fehl, wird OHNE sie wiederholt. REST jobs?select=intake_payload -> 400. Beim Fortsetzen: NeueStelleBar.tsx:52 select('*'), JobIntakeStudio.tsx:289 `const ds = r.intake_payload?.draft_state` -> undefined, :303 setAnswers(ds?.answers ?? {}), :304 dyn zurueckgesetzt, :305 freelance EMPTY, :306 flexibility
- *Problem:* Der einzige Persistenzort fuer Antworten, Flexibilitaetsmatrix, Contracting-Konditionen und Reveal-Setup ist eine Spalte, die es nicht gibt. briefing_notes speichert zwar den serialisierten Text (:347), wird aber nie zurueck in `answers` geparst.
- *Empfehlung:* Kurzfristig: draft_state in die vorhandene Spalte briefing_notes als abgegrenzten JSON-Block anhaengen ODER die Hybrid-Migration anwenden. Mittelfristig: intakeCapture nicht mehr still degradieren lassen — bei fehlenden Spalten muss der Save mit sichtbarem Fehler abbrechen statt Erfolg zu melden (:53 console.warn -> harter Fehler + Toast).
- *Akzeptanzkriterium:* Entwurf mit 10 beantworteten Fragen speichern, Dialog schliessen, ueber „Stelle ausschreiben" fortsetzen: alle 10 Antworten und der Prozentwert sind wieder da.

**P0.8 · job_skill_requirements: typisierte Tiefe wird verworfen, und zwei Schreiber streiten um dieselben Zeilen** — `BEWIESEN` · Datenmodell · Aufwand M
- *Beleg:* intake-questions/index.ts:101-114 liefert min_years, proficiency, recency. JobIntakeStudio.tsx:414 mappt nur {job_id, skill_name, type, weight} — obwohl min_years/min_proficiency/recency_required existieren (REST 200; types.ts:4311-4314). :417 upsert(..., {onConflict:'job_id,skill_name', ignoreDuplicates:true}). Gegenspieler: normalize-job-requirements/index.ts:148 delete().eq('job_id',...) gefolgt von :153 insert — Function deployed (POST -> 400, nicht 404).
- *Problem:* Der Studio schreibt additiv-ignorierend (eine spaetere Umklassifizierung must->nice erreicht die Tabelle nie), der Normalizer loescht alles und ersetzt es durch KI-Ableitungen. Keine Spalte sagt, ob eine Zeile vom Kunden oder von einem Modell stammt, und keine sagt, mit welcher Confidence.
- *Empfehlung:* 1) persistSkillRequirements auf min_years/min_proficiency/recency_required erweitern. 2) ignoreDuplicates:false + Update-Semantik. 3) Spalten `source` ('client'|'ai_intake'|'ai_normalizer') und `confidence` (numeric) ergaenzen; der Normalizer darf nur Zeilen mit source LIKE 'ai_%' loeschen.
- *Akzeptanzkriterium:* Nach Intake stehen min_years-Werte in job_skill_requirements; ein anschliessender Normalizer-Lauf laesst client-Zeilen unveraendert.

**P0.9 · Die Flexibilitaetsmatrix erreicht kein Scoring — auch nicht nach der Migration** — `BEWIESEN` · Datenmodell · Aufwand M
- *Beleg:* ProfileSections.tsx:12-22 definiert FlexibilityMap (fix|negotiable|flexible je Muss-Kriterium), JobIntakeStudio.tsx:383 schreibt sie nach intake_payload.flexibility. calculate-match-v3-1:1197-1217 gewichtet jedes Must mit `req.weight || 1.0` (:1199) und :846 wirft bei mustHaveCoverage < 0.40 komplett raus. Einziger Leser heute: ClientJobDetail.tsx:392 raw.intake_payload?.flexibility (in Produktion immer undefined). job_skill_requirements hat keine flexibility-Spalte (REST 400).
- *Problem:* Der Kunde sagt explizit, welches Muss-Kriterium weich ist. Diese Aussage landet in einem Blob, den kein Matcher liest — obwohl job_skill_requirements.weight (numeric, existiert) exakt der dafuer gebaute Kanal waere.
- *Empfehlung:* flexibility beim Schreiben nach job_skill_requirements.weight uebersetzen (fix 1.0 / negotiable 0.6 / flexible 0.3). Coverage-Gate (v3-1:846) dann gegen die gewichtete Summe statt gegen die Zaehlung. Zusaetzlich eine Spalte job_skill_requirements.flexibility (text) fuer die Erklaerbarkeit im Recruiter-UI.
- *Akzeptanzkriterium:* Ein Job mit 3 flexiblen und 2 fixen Muss-Kriterien liefert fuer einen Kandidaten, der nur die 2 fixen erfuellt, mustHaveCoverage >= 0.40 und policy != 'hidden'.

**P0.10 · Recruiter unterzeichnen live einen Vertrag mit einer fremden/fiktiven Gesellschaft ("MatchHub GmbH, Musterstraße 1, 10115 Berlin")** — `BEWIESEN` · Recht · Aufwand M
- *Beleg:* src/pages/onboarding/RecruiterOnboarding.tsx:254 ("Verträge zwischen MatchHub GmbH"), :404 ("Zwischen MatchHub GmbH, Musterstraße 1, 10115 Berlin"), :294 ("Gerichtsstand ist Berlin"), :451 ("bestätigen Sie die rechtsgültige Unterzeichnung dieses Vertrags") — gegen src/pages/public/Impressum.tsx:32-34 (Bluewater & Bridge GmbH, Adlzreiterstraße 2, 80337 München) und src/pages/public/AGB.tsx:26-28. git log --date=short -- src/pages/onboarding/RecruiterOnboarding.tsx => ef1e24c 2025-12-11 (also im ausgelieferten Bundle
- *Problem:* Der Rahmenvertrag, den jeder Recruiter im Onboarding "unterschreibt", benennt eine andere juristische Person als den Plattformbetreiber, mit Musteradresse und abweichendem Gerichtsstand. Zusätzlich widersprechen sich Provisions- und Fälligkeitsregel diametral: RecruiterOnboarding.tsx:269 ("15% und 25%") und :275/:419 ("fällig nach erfolgreichem Abschluss der Probezeit") gegen AGB.tsx:324-326 ("fällig mit Zustandekomm
- *Empfehlung:* Recruiter-Onboarding sofort auf denselben, versionierten Vertragstext gegen Bluewater & Bridge GmbH umstellen (identisch zur /agb-Fassung, München als Gerichtsstand). Bereits "unterzeichnete" Recruiter-Verträge durch Anwalt bewerten lassen und per Nachtrag/Neuabschluss heilen. Provisionsfälligkeit auf EINE Regel vereinheitlichen (Vertragsunterzeichnung vs. Probezeitende ist eine wirtschaftliche Grundsatzentscheidung,
- *Akzeptanzkriterium:* Grep über src/ liefert 0 Treffer für "MatchHub" und "Musterstraße"; Recruiter- und Client-Onboarding referenzieren dieselbe versionierte Fassung; alle Bestands-Recruiter haben die neue Fassung erneut bestätigt (recruiter_verifications.terms_version = neue Versions-ID).

**P0.11 · Die einzige echte Kandidaten-Einwilligung ist produktiv nicht erfassbar — und zwei Pfade heben sie ohne den Kandidaten auf** — `BEWIESEN` · Recht · Aufwand L
- *Beleg:* REST-Probe (Fingerprint kontrolliert: assess-candidate-fit=400 deployed, send-email=500 deployed, nicht-existente-funktion-xyz=404, parse-job-url=404): POST /functions/v1/process-interview-response => 404 = NICHT deployed. Aufruferseite: src/pages/interview/InterviewResponsePage.tsx:202-209 (invoke 'process-interview-response' mit consentGiven/consentTextVersion), Consent-UI :495-517, CONSENT_TEXT_VERSION='2026-07-v1' :46. Serverseitige Consent-Prüfung/Protokoll: supabase/functions/process-interview-response/index.
- *Problem:* src/pages/public/Datenschutz.tsx:219-226 verspricht: identifizierende Daten werden "erst offengelegt, wenn hierfür eine Einwilligung der betroffenen Kandidatin ... vorliegt; die Freigabe wird protokolliert". Die einzige Stelle, an der der Kandidat selbst zustimmt, läuft gegen eine 404-Funktion. Was produktiv funktioniert, ist der Recruiter-Klick und der Admin-Override — beides erzeugt kein Einwilligungsdokument der b
- *Empfehlung:* (1) process-interview-response deployen und vorher die consent_meta-Spalte migrieren (index.ts:165-179 fängt das Fehlen sonst still ab). (2) respondToOptIn und adminOverride dürfen identity_unlocked NICHT mehr setzen; Reveal ausschließlich serverseitig über den kandidatenbestätigten Pfad. (3) adminOverride, falls betrieblich nötig, auf einen dokumentierten Ausnahmefall mit eigener Rechtsgrundlage und Vier-Augen-Proto
- *Akzeptanzkriterium:* submissions.identity_unlocked kann ausschließlich durch process-interview-response (oder einen gleichwertigen kandidatenbestätigten Endpunkt) auf true gesetzt werden — nachgewiesen per RLS/Trigger-Test; jeder Reveal-Datensatz trägt consent_confirmed_at + consent_meta.text_version; kein Client-Code s

**P0.12 · Betroffenenrechte-Selbstbedienung (Export/Löschung) ist eine tote UI — die Datenschutzerklärung behauptet das Gegenteil** — `BEWIESEN` · Recht · Aufwand M
- *Beleg:* REST-Probe: POST /functions/v1/gdpr-export => 404, /functions/v1/gdpr-deletion => 404 (nicht deployed; Fingerprint wie oben validiert). Aufrufer live erreichbar: src/components/gdpr/DataExportRequest.tsx:23 (invoke 'gdpr-export'), src/components/gdpr/DataDeletionRequest.tsx:39,:75,:108 (invoke 'gdpr-deletion'), eingebunden unter /dashboard/privacy und /recruiter/privacy (src/App.tsx:215-217, :302-304; src/pages/dashboard/DataPrivacy.tsx:36,:40). Gegenbehauptung: src/pages/public/Datenschutz.tsx:399-400 "Zur Wahrneh
- *Problem:* Nutzer klicken "Daten exportieren" / "Konto löschen" und bekommen einen Funktionsfehler. Es gibt keinen dokumentierten manuellen Ersatzprozess im Code; die Datenschutzerklärung verweist nur zusätzlich auf info@bluewater-bridge.de (Ziffer 18).
- *Empfehlung:* Entweder beide Funktionen deployen und einen End-to-End-Test dokumentieren, ODER bis dahin die UI ausblenden und die Datenschutzerklärung Ziffer 17 auf den tatsächlich gelebten Prozess (E-Mail-Postfach mit dokumentierter Fristenkontrolle, benannte verantwortliche Person) umformulieren. Nicht beides offen lassen. Zusätzlich einen Betroffenenanfragen-Eingang mit Fristen-Tracking etablieren (Art. 12 Abs. 3).
- *Akzeptanzkriterium:* Entweder: POST auf beide Funktionen liefert != 404 und ein Testnutzer erhält Export bzw. Löschbestätigung; oder: /dashboard/privacy und /recruiter/privacy zeigen keine nicht funktionierenden Buttons mehr und Datenschutz Ziffer 17 beschreibt den manuellen Weg mit Fristzusage.

**P0.13 · Vier divergierende AGB-Fassungen, keine Versionierung, akzeptierter Text wird nicht archiviert** — `BEWIESEN` · Recht · Aufwand M
- *Beleg:* Fassung A (Kunde, hardcodiert im Flow): src/pages/onboarding/ClientOnboarding.tsx:171-190. Fassung B (Recruiter, hardcodiert): src/pages/onboarding/RecruiterOnboarding.tsx:251-296. Fassung C (Repo-Stand /agb, § 1-§ 19 inkl. § 8a-8f, "Stand: Juni 2026"): src/pages/public/AGB.tsx:24-415. Fassung D (mutmaßlich live, Vorgänger): git show c66bce7:src/pages/public/AGB.tsx enthält "[Adresse]", "[Provisionshöhe/-modell einfügen]" und "Stand: [Datum]" und 0 Treffer für "KI-Verordnung"/"2024/1689"; git log -- src/pages/publi
- *Problem:* Der Kunde akzeptiert im Onboarding einen vier Absätze langen Text, der nirgends veröffentlicht, nicht verlinkt und nicht archiviert ist. Gespeichert wird nur der String '1.0', der auf kein identifizierbares Dokument zeigt (/agb trägt gar keine Versionsnummer, nur "Stand: Juni 2026"). Welche Fassung im Streitfall gilt, ist aus den Daten nicht rekonstruierbar. Ob die AI-Act-Klauseln § 8a-8f überhaupt live sind, ist off
- *Empfehlung:* Genau EINE AGB-Quelle: /agb bekommt eine harte Versions-ID (z. B. 'agb-2026-08-v3') im Text. Onboarding zeigt keinen eigenen Text mehr, sondern ein Vollzitat/iFrame derselben Quelle plus Pflicht-Link. Bei Annahme: Versions-ID, Zeitstempel, IP/User-Agent und ein unveränderlicher Snapshot (Storage-Objekt mit Hash) speichern — terms_version darf nie hardcodiert sein. Vorher per curl prüfen, welche Fassung auf matchunt.a
- *Akzeptanzkriterium:* client_verifications.terms_version enthält eine Versions-ID, zu der ein archivierter, per Hash referenzierter Textstand existiert; Onboarding und /agb zeigen nachweislich denselben Text; kein Rechtstext im Repo enthält mehr eckige Platzhalter; Academy-Link zeigt auf matchunt.ai.

**P0.14 · Honorar: Kunde stimmt "standardmäßig 20%" zu, der Admin setzt danach einseitig 15-30% — ohne Zustimmung und ohne Mitteilung** — `BEWIESEN` · Recht · Aufwand L
- *Beleg:* Zustimmungstext: src/pages/onboarding/ClientOnboarding.tsx:182-185 ("Die Vermittlungsgebühr beträgt standardmäßig 20% des Jahresgehalts ... fällig bei Unterzeichnung des Arbeitsvertrags"). Einseitige Festsetzung: src/components/admin/JobApprovalDialog.tsx:316-322 (Slider min=15, max=30) und :115-126 (update jobs.fee_percentage + status='published'). Die Publish-Benachrichtigung an den Kunden nennt die Fee nicht: :138-145. Anzeige beim Kunden erst nachträglich und rollenabhängig: src/pages/dashboard/ClientJobDetail.
- *Problem:* Es gibt keinen Punkt im Flow, an dem der Kunde die konkrete Fee annimmt. Der Admin setzt sie beim Freigeben, der Kunde erfährt sie allenfalls, wenn er später den Konditionen-Akkordeon öffnet — und Hiring Manager/Viewer im Team-Modell sehen sie gar nicht. "Vereinbart" ist die Fee damit nie.
- *Empfehlung:* Fee zum expliziten Vertragsschritt machen: Nach Admin-Festsetzung geht der Job in einen Status "Konditionen bestätigen"; der Kunde nimmt Prozentsatz, Bemessungsgrundlage (Zieljahresgehalt inkl./exkl. variabler Anteile), Fälligkeit und Rückerstattungsregel per protokolliertem Klick an; erst dann published. Annahme mit Zeitstempel + Nutzer + Betragsdefinition speichern. Alternativ: fee_percentage fest bei 20% und Abwei
- *Akzeptanzkriterium:* Kein Job erreicht status='published' ohne einen Datensatz "Konditionen angenommen" (Kunde, Zeitstempel, Prozentsatz, Bemessungsgrundlage, Fälligkeit); die Publish-Benachrichtigung nennt die vereinbarte Fee; die Fee ist für jede Client-Rolle einsehbar, die für Kosten haftet.

**P0.15 · Reihenfolge invertiert: Vertragsunterschrift vor jedem Produktwert — Time-to-Value = 7 Klicks bis zum ersten Artefakt, 3–5 Tage bis zum ersten Nutzen** — `BEWIESEN` · Conversion/UX · Aufwand L
- *Beleg:* src/pages/Auth.tsx:46-48 (nach Signup direkt navigate('/onboarding')); src/pages/onboarding/ClientOnboarding.tsx:18-23 (STEPS: terms → contract → company → complete); ClientOnboarding.tsx:225-227 ('Hiermit bestätige ich, dass ich berechtigt bin, im Namen meines Unternehmens Verträge abzuschließen'); Firmenname wird vorher nie erhoben (Auth.tsx:35-37 nur fullName/email/password); ClientOnboarding.tsx:259-282 Firmendaten explizit '(optional)' und ERST NACH der Unterschrift; src/hooks/useClientVerification.ts:99/122/1
- *Problem:* Gemessener Pfad Landing → erster gespeicherter Job: 1 Klick CTA + 3 Eingabefelder + 'Konto erstellen' + AGB-Scrollbox lesen + Checkbox + 'Weiter' + Namen ein zweites Mal tippen + 'Vertrag unterzeichnen' + 2 optionale Felder + 'Abschließen' + 'Zum Dashboard' = 7 Klicks und 4 Pflicht-Eingaben, geschätzt 90–150 Sekunden — bevor irgendetwas Produktartiges sichtbar wird. Der Anxiety-Peak (rechtsverbindliche Unterschrift) 
- *Empfehlung:* Reihenfolge umdrehen: (1) Firmen-Domain abfragen und daraus ein company_profiles anlegen (senkt später auch die Briefing-Last, weil JobIntakeStudio.tsx:122-136 genau das liest), (2) Job anlegen lassen — das ist der erste sichtbare Wert, (3) AGB + Unterschrift erst als Gate direkt vor 'An Recruiter übergeben', dort mit Kontext ('Damit beauftragen Sie die Suche für DIESE Stelle'). Onboarding-Headline statt 'Account-Ver
- *Akzeptanzkriterium:* Ein neu registrierter Kunde kann einen Job-Entwurf anlegen und speichern, ohne vorher AGB akzeptiert oder unterschrieben zu haben; die Unterschriftsseite zeigt den zuvor erfassten Firmennamen im Bestätigungstext; company_profiles existiert für 100 % der Accounts, die das Onboarding abschließen.

**P0.16 · Das gesamte Briefing erreicht den Recruiter nie — 34 Fragen ohne Empfänger, mit ausdrücklichem Gegenteil-Versprechen im UI** — `BEWIESEN` · Conversion/UX · Aufwand M
- *Beleg:* supabase/migrations/20260725120000_recruiter_jobs_view_hardening.sql:23-24 ('Zu C) gehören insbesondere client_id, … briefing_notes und intake_briefing') und :98-101 ('-- briefing_notes, intake_briefing, … bewusst NICHT Teil dieser View') und COMMENT :119-121; REST-Probe: GET /rest/v1/recruiter_jobs_view?select=id&limit=1 → HTTP 200 (View ist in PRODUKTION live); supabase/functions/format-job-for-recruiters/index.ts:53 select('*') aber Prompt :65-107 listet nur Titel/Branche/Standort/Remote/Anstellung/Erfahrung/Geh
- *Problem:* Der Kunde beantwortet bis zu 34 Fragen (IntakeBriefing zeigt 'Frage 1 von 34', IntakeBriefing.tsx:418). Das Ergebnis landet in jobs.briefing_notes. Diese Spalte ist aus recruiter_jobs_view bewusst entfernt, description/requirements sind bis zum Reveal NULL (Migration :93-94), und der einzige Kanal, der etwas Redigiertes an Recruiter liefert (format-job-for-recruiters → formatted_content), liest das Briefing gar nicht
- *Empfehlung:* Sofort (S): format-job-for-recruiters-Prompt um einen Block 'BRIEFING DES KUNDEN (ANONYMISIEREN)' mit job.briefing_notes, vacancy_reason, hiring_urgency, company_culture erweitern — dort läuft die Anonymisierungs-Instruktion (index.ts:68-76) ohnehin schon. Danach (M): entpersonalisierte Briefing-Ableitungen als eigene Spalten in recruiter_jobs_view aufnehmen (Verhandelbarkeit je Muss-Kriterium, Suchpriorität, Grund d
- *Akzeptanzkriterium:* Ein Recruiter, der einen frisch freigegebenen Job in seiner Ansicht öffnet, sieht mindestens drei inhaltliche Aussagen, die ausschließlich aus dem Briefing stammen und nicht aus Titel/Skills/Gehalt ableitbar sind — ohne dass company_revealed=true ist. Zusätzlich: Der Kunde kann im Studio eine 'Recru

**P0.17 · Alle drei Import-Wege sind in Produktion 404 — der Nutzer bekommt zwei Fehler-Toasts, und der zweite gibt ihm die Schuld** — `BEWIESEN` · Conversion/UX · Aufwand S
- *Beleg:* REST-Probe (Fingerprint validiert: calculate-match-v3-1 → 400, assess-candidate-fit → 400, calculate-scores → 400, format-job-for-recruiters → 400, Kontrolle 'definitely-not-a-function' → 404): parse-job-url → 404, parse-job-pdf → 404, intake-questions → 404; src/hooks/useJobParsing.ts:88 — parseJobText ruft ebenfalls 'parse-job-url' auf, es existiert gar keine eigene Text-Function; useJobParsing.ts:75 und :105 zeigen Toast 1 mit der rohen fnError.message; src/components/dashboard/JobIntakeStudio.tsx:240 zeigt Toas
- *Problem:* Bei jedem Import-Versuch feuern zwei Toasts gleichzeitig: erst die technische Rohmeldung des Edge-Function-Clients (englisch, für den Nutzer bedeutungslos), dann eine deutsche Meldung, die einen reinen Serverausfall als Eingabefehler des Nutzers umdeutet und ihn auffordert, MEHR zu tippen. Beim PDF-Weg ist die Zumutung doppelt: Die Datei wurde erfolgreich hochgeladen, der Nutzer bekommt trotzdem 'bitte mehr Text/Deta
- *Empfehlung:* 1) parse-job-url deployen — repariert Text UND Link in einem Schritt (es gibt keine separate Text-Function). 2) Bis dahin: Health-Probe beim Öffnen; sind die Functions nicht erreichbar, Import-Kacheln/-Buttons ausblenden statt sie ins Leere laufen zu lassen, und die Subline auf 'Felder direkt ausfüllen' ändern. 3) Toast-Kaskade beenden: rohe fnError.message nie an den Nutzer. 4) Textalternative für JobIntakeStudio.ts
- *Akzeptanzkriterium:* Bei nicht erreichbarem Parser sieht der Nutzer genau einen Hinweis, dieser nennt die Serverseite als Ursache, bietet einen funktionierenden manuellen Weg an, und keine PDF landet mehr im Bucket. Kein englischsprachiger Roh-Fehlertext erreicht die UI.

**P0.18 · Der Preis erscheint erst NACH der Registrierung — und widerspricht den echten AGB** — `BEWIESEN` · Conversion/UX · Aufwand S
- *Beleg:* src/i18n/locales/de.ts:186 CTA 'Job kostenlos ausschreiben', :492 'Keine Kreditkarte erforderlich • Kostenlose Registrierung', :493-503 Pricing-Block ohne jede Zahl; grep über src/components/landing/PricingSection.tsx nach '%|Prozent|Gebühr' → 0 Treffer; grep über src/i18n/locales/de.ts nach '20%' → 0 Treffer; src/pages/onboarding/ClientOnboarding.tsx:183 'Die Vermittlungsgebühr beträgt standardmäßig 20% des Jahresgehalts des vermittelten Kandidaten'; src/pages/public/AGB.tsx:292-293 'Die konkrete Höhe und das Mode
- *Problem:* Drei Preisaussagen, die sich gegenseitig widersprechen: Die Landingpage nennt keine Zahl und wirbt mit 'kostenlos'. Die Onboarding-Box nennt 20 % fix. Die verlinkte, produktive AGB sagt, es gebe keinen festen Satz und der Satz werde vor Prozessbeginn in der Plattform ausgewiesen — was nirgends passiert. Zusätzlich akzeptiert der Nutzer 'Version 1.0', während die publizierte AGB 'Stand: Juni 2026' trägt; die 150-Wort-
- *Empfehlung:* 1) Zahl in die PricingSection heben, bevor jemand ein Konto anlegt — 'Erfolgshonorar: 20 % des Bruttojahresgehalts, fällig bei Vertragsunterzeichnung des Kandidaten. Keine Fixkosten, kein Retainer.' 2) CTA von 'Job kostenlos ausschreiben' auf 'Stelle ausschreiben — zahlen nur bei Einstellung' ändern (hält das Versprechen, ohne 'kostenlos' zu behaupten). 3) Onboarding-Box entweder aus /agb generieren oder durch eine e
- *Akzeptanzkriterium:* Der Prozentsatz ist auf der Landingpage sichtbar, bevor der Signup-Screen erreicht wird; die im Onboarding gezeigte Zusammenfassung und /agb enthalten keine widersprüchlichen Preisaussagen mehr; terms_version entspricht dem Stand des publizierten Texts.

**P0.19 · Kunde kann jobs.status selbst auf 'published' setzen — die Matchunt-Freigabe ist reine Frontend-Konvention** — `ABGELEITET` · Backend/Security · Aufwand S
- *Beleg:* supabase/migrations/20260710130000_client_team_foundation.sql:446-449 `CREATE POLICY "Client team can update their jobs" ON public.jobs FOR UPDATE USING (public.can_edit_job(id)) WITH CHECK (public.can_edit_job(id));` — keine Spalten-Einschränkung. supabase/migrations/20260616104941_...:136-138 `recruiter_jobs_view ... WHERE public.has_role(auth.uid(),'recruiter') AND j.status = 'published'` — Status ist das EINZIGE Sichtbarkeits-Gate für Recruiter. Migrations-Kommentar 20260710130000:41-42: „jobs.status ist TEXT o
- *Problem:* Es gibt weder Trigger, CHECK-Constraint noch Policy-Bedingung, die den Statusübergang einschränkt. Der Admin-Publish in src/components/admin/JobApprovalDialog.tsx:115-124 (status='published' + fee_percentage + recruiter_fee_percentage + approved_by) ist damit nur eine von zwei möglichen Quellen für 'published'.
- *Empfehlung:* BEFORE UPDATE OR INSERT Trigger `jobs_status_guard` auf public.jobs: Übergang nach 'published' nur wenn `public.has_role(auth.uid(),'admin')` ODER `current_setting('role')='service_role'`; für Clients Whitelist draft -> pending_client_approval -> pending_approval sowie published -> paused/closed. Zusätzlich CHECK-Constraint auf die erlaubte Statusmenge, damit Tippfehler-Status keine Zombie-Jobs erzeugen.
- *Akzeptanzkriterium:* Als eingeloggter Client-User liefert PATCH auf jobs.status='published' einen Fehler (42501/RAISE), der Job bleibt auf pending_approval; pgTAP-Test deckt alle erlaubten und alle verbotenen Übergänge ab.

**P0.20 · „Entwurf fortsetzen" überschreibt client_id — stille Eigentumsübernahme fremder Stellen** — `ABGELEITET` · Backend/Security · Aufwand S
- *Beleg:* src/components/dashboard/NeueStelleBar.tsx:51-58 `resumeDraft` lädt JEDE Job-Zeile per `.select('*').eq('id', id)` ohne Ownership-Prüfung. src/components/dashboard/JobIntakeStudio.tsx:287-290 setzt `setDraftJobId(initialDraft.id)`. src/components/dashboard/JobIntakeStudio.tsx:332-333 `const base = { client_id: user?.id, ... }` — unbedingt, auch im Update-Fall. src/components/dashboard/JobIntakeStudio.tsx:404-406 `draftJobId ? updateJobWithIntake(draftJobId, rec.base, rec.extended) : insertJobWithIntake(...)`. src/l
- *Problem:* Der Speicherpfad kennt keinen Unterschied zwischen „ich lege an" und „ich bearbeite den Entwurf eines Kollegen". Jeder Klick auf „Später weiter" oder „An Recruiter übergeben" schreibt client_id auf den aktuell eingeloggten User um. Die UPDATE-Policy verhindert das nicht (siehe Finding zum WITH-CHECK-Antipattern).
- *Empfehlung:* `client_id` (und `organization_id`) aus `buildRecord` in JobIntakeStudio.tsx:332ff entfernen und nur im INSERT-Pfad setzen (intakeCapture.ts: getrennte `base`-Objekte für insert/update). Zusätzlich serverseitig absichern: UPDATE-Policy um `WITH CHECK (client_id = (SELECT j.client_id FROM jobs j WHERE j.id = jobs.id))` bzw. einen BEFORE-UPDATE-Trigger `NEW.client_id := OLD.client_id` ergänzen. NeueStelleBar-Query auf 
- *Akzeptanzkriterium:* Nach dem Fortsetzen und Speichern eines fremden Entwurfs ist jobs.client_id unverändert; ein direkter PATCH auf client_id wird von der DB abgelehnt.

**P0.21 · client_verifications ist selbst-schreibbar (KYC/Vertrag), und der neue Intake-Pfad prüft die Verifizierung überhaupt nicht mehr** — `BEWIESEN` · Backend/Security · Aufwand M
- *Beleg:* supabase/migrations/20251204204702_...:43-44 `CREATE POLICY "Clients can update own verification" ON public.client_verifications FOR UPDATE USING (auth.uid() = client_id);` — kein WITH CHECK, keine Spalten-Whitelist. grep über alle Migrationen: nur diese eine CREATE POLICY, kein späteres DROP/Tightening. src/hooks/useClientVerification.ts:169-174 leitet `isFullyVerified` und `canPublishJobs` ausschließlich aus terms_accepted/contract_signed/kyc_status ab. src/pages/dashboard/CreateJob.tsx:589-593 blockt Publish ohn
- *Problem:* Die Verifizierungs-Statusfelder sind gleichzeitig Autorisierungsentscheidung und vom Betroffenen frei beschreibbar. Der alte Pfad hatte wenigstens einen Frontend-Check, der neue Studio-Pfad hat ihn ersatzlos verloren.
- *Empfehlung:* UPDATE-Policy auf eine Spalten-Whitelist reduzieren: Kunde darf nur terms_accepted/terms_accepted_at/terms_version/digital_signature/company_registration_number/vat_id setzen; kyc_status, kyc_verified_at, kyc_verified_by, contract_signed ausschließlich per Trigger `RAISE`-geschützt bzw. Admin/Service-Role. Konkret: BEFORE UPDATE Trigger, der bei nicht-Admin `NEW.kyc_status := OLD.kyc_status` etc. erzwingt. Parallel `
- *Akzeptanzkriterium:* PATCH auf kyc_status durch den Kunden selbst ändert den Wert nicht (oder wirft); pgTAP-Test für jede geschützte Spalte.

**P0.22 · Es gibt keinen einzigen Codepfad, der eine Organisation anlegt — das gesamte Mandantenmodell ist unerreichbar** — `BEWIESEN` · Architektur · Aufwand M
- *Beleg:* src/hooks/useOrganization.ts:69 definiert createOrganization, Zeile 137 exportiert es; `grep -rn createOrganization src/` liefert NUR diese zwei Treffer, keinen Aufrufer. src/pages/onboarding/ClientOnboarding.tsx:67-109 schreibt ausschließlich client_verifications (über src/hooks/useClientVerification.ts:97/121/148). Organisationen entstehen nur im Backfill von supabase/migrations/20260710130000_client_team_foundation.sql (Abschnitt 6, Zeilen 355-365).
- *Problem:* Jeder Kunde, der sich nach dem Backfill registriert, hat keine organizations- und keine organization_members-Zeile. Der Trigger job_set_organization (Migration 20260710130000:370-386) sucht die Org über organization_members, findet nichts, also bleibt jobs.organization_id NULL. Jeder Job dieses Kunden fällt damit dauerhaft in den Legacy-Zweig von can_access_job (Migration:157-177: `organization_id IS NULL AND client_
- *Empfehlung:* Organisation beim Client-Signup serverseitig erzwingen: DB-Trigger auf user_roles/profiles (role='client') legt idempotent organizations(type='client', owner_id) + organization_members(role='owner', status='active') an. Zusätzlich einmaliger Backfill für alle seit dem 10.07.2026 registrierten Client-User. Erst danach ist jobs.organization_id ein verlässlicher Mandantenschlüssel.
- *Akzeptanzkriterium:* Für jeden User mit role='client' existiert genau eine organizations-Zeile mit ihm als owner_id und eine aktive organization_members-Zeile; ein unmittelbar nach der Registrierung angelegter Job hat organization_id NOT NULL.

**P0.23 · Team-Einladung ist produktiv tot: validate-invite nicht deployt, Fallback-Policy zuvor gelöscht** — `BEWIESEN` · Architektur · Aufwand S
- *Beleg:* POST https://dngycrrhbnwdohbftpzq.supabase.co/functions/v1/validate-invite → 404 {"code":"NOT_FOUND_FUNCTION_BLOB"}; Gegenprobe accept-invite → 400 (deployt), team-data → 401 (deployt). Aufrufer: src/pages/organization/AcceptInvite.tsx:22 → src/hooks/useOrganizationInvites.ts:144 `supabase.functions.invoke('validate-invite')`. supabase/migrations/20260710130000_client_team_foundation.sql:78 löscht `DROP POLICY "Anyone can view invite by token" ON public.organization_invites` — ein Client-seitiger Fallback existiert
- *Problem:* Die öffentliche Route /invite/:token (src/App.tsx:473) kann den Token nicht auflösen. Der Eingeladene sieht nie ein gültiges Invite, also wird accept-invite nie erreicht, also entsteht nie eine organization_members-Zeile und nie ein job_collaborators-Eintrag (supabase/functions/accept-invite/index.ts:166).
- *Empfehlung:* validate-invite deployen und den Pfad end-to-end verifizieren (Invite anlegen → /invite/:token → annehmen → organization_members + job_collaborators prüfen). Deploy-Gate einführen: CI-Check, der jede in src/ per functions.invoke() referenzierte Funktion gegen die deployte Liste abgleicht — die hier gefundenen 404er sind alle vom selben Typ.
- *Akzeptanzkriterium:* POST /functions/v1/validate-invite antwortet mit 4xx statt 404, und ein Testinvite lässt sich über /invite/:token bis zur aktiven Mitgliedschaft durchspielen.

**P0.24 · Das Freigabe-Gate existiert nur in der UI — RLS erlaubt dem Kunden Selbst-Publish samt eigener Fee** — `ABGELEITET` · Architektur · Aufwand M
- *Beleg:* supabase/migrations/20260710130000_client_team_foundation.sql:445-449: `CREATE POLICY "Client team can update their jobs" ON public.jobs FOR UPDATE USING (public.can_edit_job(id)) WITH CHECK (public.can_edit_job(id))` — keine Spalten- und keine Statusbeschränkung. Kein BEFORE-UPDATE-Statusguard auf public.jobs (grep über alle Migrationen; der einzige BEFORE UPDATE ist 20251204171610:317 = updated_at). Marktplatz filtert allein auf den Status: supabase/migrations/20260725120000_recruiter_jobs_view_hardening.sql:113 
- *Problem:* Jeder Kunde mit can_edit_job kann per PATCH /rest/v1/jobs?id=eq.<id> direkt status='published', fee_percentage, recruiter_fee_percentage, approved_at und approved_by setzen. Die Admin-Prüfung ist damit reine Höflichkeit. Eine Ebene tiefer dasselbe: can_edit_job (Migration:181-201) schließt hiring_manager mit Collaborator-Eintrag ein — er kann seinen eigenen Intake von pending_client_approval auf pending_approval hebe
- *Empfehlung:* Statuswechsel aus der Client-UPDATE-Policy herausnehmen: BEFORE-UPDATE-Trigger auf jobs, der für Nicht-Admins nur draft→pending_approval|pending_client_approval, pending_*→draft und published→closed|filled zulässt und NEW.fee_percentage/recruiter_fee_percentage/approved_at/approved_by/status='published' auf OLD zurücksetzt. pending_client_approval→pending_approval zusätzlich nur, wenn get_org_role(organization_id) IN
- *Akzeptanzkriterium:* Ein PATCH als Kunden-Token mit status='published' bzw. geändertem fee_percentage schlägt fehl oder lässt die Werte unverändert; ein hiring_manager kann seinen eigenen Job nicht von pending_client_approval nach pending_approval bewegen.

**P0.25 · Die interne Kundenfreigabe ist über die Job-Detailseite produktiv unerreichbar — der Fallback ignoriert das Freigabe-Ziel** — `BEWIESEN` · Architektur · Aufwand S
- *Beleg:* src/pages/dashboard/ClientJobDetail.tsx:311-316: erst `.update({ status: target.status, rejection_reason: null, rejected_at: null, client_approval_note: null })`, dann `if (error && isMissingColumnError(error)) { ... .update({ status: 'pending_approval' }) }`. REST-Probe: jobs.rejection_reason und jobs.rejected_at → 400 {"code":"42703","message":"column jobs.rejection_reason does not exist"}, während jobs.client_approval_note → 200.
- *Problem:* Weil rejection_reason produktiv fehlt, schlägt der erste UPDATE IMMER fehl und der Fallback greift IMMER — er setzt hart 'pending_approval' statt target.status. Jede erneute Einreichung eines zurückgegebenen Jobs geht damit direkt an Matchunt, unabhängig von organizations.settings.intake_approval_required. Zusätzlich leert der Fallback client_approval_note nicht: die alte interne Rückgabenotiz bleibt stehen und ersch
- *Empfehlung:* Migration 20260710120000_job_review_feedback nachziehen (rein additiv). Unabhängig davon den Fallback korrigieren: `target.status` setzen statt hart 'pending_approval', und client_approval_note leeren. Grundregel: isMissingColumnError-Fallbacks dürfen nie die fachliche Entscheidung ändern, nur Zusatzfelder weglassen.
- *Akzeptanzkriterium:* jobs.rejection_reason liefert per REST 200; ein hiring_manager in einer Org mit intake_approval_required=true landet beim erneuten Einreichen nachweislich auf status='pending_client_approval', und die alte client_approval_note ist geleert.

**P0.26 · 'filled' ist ein Zustand ohne Heimat — das Umsatzsignal 'über Matchunt besetzt' wird ersatzlos verworfen** — `BEWIESEN` · Architektur · Aufwand M
- *Beleg:* src/pages/dashboard/ClientJobDetail.tsx:225-226: `const newStatus = reason === 'filled_via_matchunt' || reason === 'filled_elsewhere' ? 'filled' : 'closed'`. `grep -rn "'filled'" supabase/` → 0 Treffer. REST: jobs.closed_reason und jobs.closed_at → 400/42703 (Migration 20260716120000 nicht angewandt) → der Fallback ClientJobDetail.tsx:236-242 speichert NUR den Status. Admin: src/pages/admin/AdminJobs.tsx:110-119 kennt in getStatusBadge nur draft/pending_approval/published/closed/paused und fällt für Unbekanntes auf
- *Problem:* Der Kunde beantwortet beim Schließen genau die Frage, an der die Vermittlungsgebühr hängt — 'über Matchunt besetzt' vs. 'anderweitig besetzt' — und die Antwort wird weggeworfen. Übrig bleibt ein Statuswert, den die Datenbank nicht kennt und den der Admin als 'Entwurf' sieht.
- *Empfehlung:* Zwei Dinge trennen: (1) Migration 20260716120000 nachziehen, damit closed_reason/closed_at persistieren. (2) 'filled' entweder überall einführen (Badge, Filter, Select in AdminJobs, Kennzahlen, Placement-/Invoice-Trigger) oder streichen und status='closed' + closed_reason als einzige Wahrheit führen — letzteres ist die kleinere Angriffsfläche, weil jobs.status nirgends per CHECK abgesichert ist.
- *Akzeptanzkriterium:* Nach dem Schließen mit 'filled_via_matchunt' ist der Grund in der Datenbank lesbar, die Stelle erscheint im Admin mit korrektem eigenem Badge und taucht in einer Auswertung 'über Matchunt besetzt' auf.

**P0.27 · P0 Datenleck: outreach_emails, outreach_send_queue und outreach_rate_limits sind mit dem anon-Key aus dem offenen Internet lesbar** — `BEWIESEN` · RevOps · Aufwand S
- *Beleg:* supabase/migrations/20251216022724_b67fba63-f3d8-4b02-833a-e57eb3b0c33a.sql:327-331 `CREATE POLICY "System can manage outreach_emails" ON outreach_emails FOR ALL USING (true) WITH CHECK (true);` (identisch fuer outreach_send_queue:330-331, outreach_conversations:333-334, outreach_messages, outreach_sequences) und 20251216032024_1fc7a6dc.sql:124-125 `CREATE POLICY "System can manage rate limits" ON public.outreach_rate_limits FOR ALL USING (true);`. REST-Probe mit anon-Key aus .env: GET /rest/v1/outreach_emails -> H
- *Problem:* Postgres verknuepft RLS-Policies derselben Tabelle mit ODER. Die restriktive Admin-Policy (`Admin full access on outreach_emails` mit has_role(auth.uid(),'admin'), Zeile 308-309) wird durch die danebenstehende permissive `USING (true)`-Policy vollstaendig entwertet. Die vermutete Absicht war, dem Service-Role-Key Zugriff zu geben; der Service-Role-Key umgeht RLS aber ohnehin, die Policy ist also nicht nur gefaehrlich
- *Empfehlung:* Sofort und vor jeder anderen Massnahme: alle `USING (true)`-Policies auf outreach_emails, outreach_send_queue, outreach_conversations, outreach_messages, outreach_sequences und outreach_rate_limits per DROP POLICY entfernen. Kein Ersatz noetig, da der Service-Role-Key RLS grundsaetzlich umgeht und die Edge Functions ausschliesslich mit diesem Key arbeiten (belegt in process-outreach-queue/index.ts und track-outreach-
- *Akzeptanzkriterium:* `curl -H "apikey: $ANON" "$URL/rest/v1/outreach_emails?select=id"` liefert eine leere Liste statt 11 Zeilen; identisch fuer outreach_send_queue und outreach_rate_limits. Ein als Admin eingeloggter Client liest die Zeilen weiterhin. Ein grep nach `USING (true)` ueber alle Migrationen liefert nur noch

**P0.28 · P0 Schreibzugriff: Die Leak-Policies erlauben anonymen Nutzern zusaetzlich INSERT, UPDATE und DELETE** — `ABGELEITET` · RevOps · Aufwand S
- *Beleg:* supabase/migrations/20251216022724_b67fba63-f3d8-4b02-833a-e57eb3b0c33a.sql:327-328 `FOR ALL USING (true) WITH CHECK (true)`. `FOR ALL` umfasst SELECT, INSERT, UPDATE und DELETE; `WITH CHECK (true)` akzeptiert jede geschriebene Zeile. Nicht per REST getestet, da schreibende Requests im Auftrag ausdruecklich untersagt sind.
- *Problem:* Wenn schon SELECT nachweislich fuer anon durchgeht, gilt dieselbe Policy-Zeile fuer die schreibenden Operationen. Ein anonymer Angreifer koennte damit Zeilen in outreach_send_queue einstellen und, sobald process-outreach-queue deployed wird, den Versand beliebiger Inhalte ueber die verifizierte Absenderdomain von Matchunt ausloesen. Ebenso liesse sich outreach_rate_limits.max_count hochsetzen und damit die Versandbre
- *Empfehlung:* Wird durch das DROP POLICY aus Befund 1 vollstaendig mitgeloest, ist also kein eigener Arbeitsschritt. Separat aufgefuehrt, damit die Dringlichkeit nicht als reines Lese-Leck fehleingeschaetzt und die Behebung nach dem geplanten Deployment eingeplant wird. Reihenfolge ist zwingend: erst Policies entfernen, dann Edge Functions deployen. Zusaetzlich absichern, indem der Queue-Insert ausschliesslich ueber eine SECURITY 
- *Akzeptanzkriterium:* Ein POST auf /rest/v1/outreach_send_queue mit anon-Key wird mit HTTP 401 oder 403 abgelehnt. Der Test wird einmalig vom Eigentuemer selbst ausgefuehrt, nicht durch die Analyse. Das Deployment von process-outreach-queue erfolgt nachweislich erst nach diesem Nachweis.

**P0.29 · Das Outreach-Modul ist in Produktion funktionslos: 7 von 8 Edge Functions liefern 404** — `BEWIESEN` · RevOps · Aufwand M
- *Beleg:* POST-Probe gegen https://dngycrrhbnwdohbftpzq.supabase.co/functions/v1/<name> mit anon-Key, Fingerprint 404=nicht deployed: generate-outreach-email 404, process-outreach-queue 404, track-outreach-engagement 404, crawl-career-page 404, crawl-career-pages-bulk 404, crawl-company-data 404, enrich-company-from-domain 404. Einzige Ausnahme: import-outreach-leads 500 (deployed, aber Fehler bei leerem Body, was bei {} erwartbar ist).
- *Problem:* Jede wertschoepfende Funktion des Moduls haengt an einer nicht deployten Funktion. Ohne generate-outreach-email entsteht kein Mail-Text, ohne process-outreach-queue wird nichts versendet, ohne track-outreach-engagement wird kein Open und kein Klick erfasst, ohne crawl-career-page und enrich-company-from-domain gibt es keine Anreicherung und damit keine Personalisierungsgrundlage. Uebrig bleibt der reine Lead-Import.
- *Empfehlung:* Nicht alles deployen, sondern bewusst einen Minimalpfad waehlen. Empfohlene Reihenfolge nach Behebung von Befund 1 und 2: (1) generate-outreach-email und process-outreach-queue, weil sie den Kernkreislauf Erzeugen-Freigeben-Versenden schliessen; (2) track-outreach-engagement, aber erst nach der Einwilligungs-Klaerung aus Befund 10; (3) enrich-company-from-domain und crawl-career-page nur, wenn die Anreicherung nachwe
- *Akzeptanzkriterium:* Die POST-Probe liefert fuer generate-outreach-email und process-outreach-queue nicht mehr 404. Ein Testversand mit test_mode = true an eine eigene Adresse kommt an und traegt den Betreff-Praefix [TEST] (belegt in process-outreach-queue/index.ts:278).

**P0.30 · Es wurde noch nie eine E-Mail versendet; die Warteschlange scheitert seit Januar an einem ungueltigen Absenderfeld** — `BEWIESEN` · RevOps · Aufwand S
- *Beleg:* REST-Probe /rest/v1/outreach_emails?select=status,sent_at,opened_at,open_count,clicked_at liefert alle 11 Zeilen mit sent_at=null, opened_at=null, open_count=0, clicked_at=null; Status ausschliesslich review (6x), approved (3x), rejected (2x). /rest/v1/outreach_send_queue: 3 Zeilen, status "pending", attempts 2 von max_attempts 3, error_message woertlich "Invalid `from` field. The email address needs to follow the `email@example.com` or `Name <email@example.com>` format.", scheduled_at 2026-01-13. Erzeugender Code:
- *Problem:* Zwei Ursachen ueberlagern sich. Erstens ist die Funktion seit dem letzten Deploy nicht mehr in Produktion (404), die Queue wird also gar nicht abgearbeitet. Zweitens war schon beim letzten erfolgreichen Lauf im Januar 2026 das Absenderfeld ungueltig, weil outreach_campaigns.sender_email offenbar keine valide Adresse enthaelt und die Funktion den Wert ungeprueft in das Resend-Template interpoliert. Ein Beleg dafuer, d
- *Empfehlung:* sender_email in outreach_campaigns mit einer Datenbank-Constraint gegen ein E-Mail-Muster absichern und im CampaignDialog clientseitig validieren, bevor eine Kampagne aktiviert werden kann. In process-outreach-queue vor dem Resend-Aufruf eine Vorabpruefung ergaenzen, die bei ungueltigem Absender die gesamte Kampagne pausiert (is_paused = true) statt einzelne Queue-Eintraege scheitern zu lassen; ein Konfigurationsfehl
- *Akzeptanzkriterium:* Eine Kampagne mit ungueltigem sender_email laesst sich nicht aktivieren. Nach dem Deployment gilt fuer mindestens eine Testmail: outreach_emails.status = 'sent' und sent_at ist gesetzt. Die 3 Alt-Eintraege in outreach_send_queue mit scheduled_at aus Januar 2026 sind nicht mehr im Status pending.

**P0.31 · backend-security F4: die vorgeschlagene WITH-CHECK-Subquery legt jedes UPDATE auf jobs lahm** — `ABGELEITET` · Red Team · Aufwand S
- *Beleg:* Empfohlen wird WITH CHECK (public.can_edit_job(id) AND client_id = (SELECT client_id FROM jobs WHERE id = jobs.id) AND organization_id IS NOT DISTINCT FROM (SELECT organization_id FROM jobs WHERE id = jobs.id)). Innerhalb der Subquery verdeckt das innere jobs das aeussere: jobs.id loest auf die INNERE Relation auf, die Bedingung wird WHERE id = id, die Subquery liefert alle Zeilen. Zusaetzlich liest eine Policy auf jobs innerhalb ihrer eigenen Auswertung erneut aus jobs.
- *Problem:* Zwei unabhaengige Fehler in einer Zeile: (1) Namensverdeckung macht die Bedingung wirkungslos bzw. erzeugt 'more than one row returned by a subquery used as an expression'; (2) Postgres bricht mit 'infinite recursion detected in policy for relation jobs' ab. Nebenbei: die Begruendung des Findings (eine STABLE-Funktion sehe den Statement-Snapshot, also OLD) ist ein Nebenschauplatz - der eigentliche Defekt ist, dass de
- *Empfehlung:* Empfehlung von backend-security F4 verwerfen. Stattdessen die Variante von architekt F3 nehmen: EIN BEFORE-UPDATE-Trigger auf public.jobs, der fuer Nicht-Admins NEW.client_id, NEW.organization_id, NEW.fee_percentage, NEW.recruiter_fee_percentage, NEW.approved_by, NEW.approved_at auf OLD zurueckschreibt und Statusuebergaenge whitelistet. Ein Trigger loest F1, F2 und F4 von backend-security gleichzeitig - drei getrennt
- *Akzeptanzkriterium:* Nach Deploy des Triggers laufen die bestehenden Client-Flows (Entwurf speichern, einreichen, schliessen) unveraendert durch; PATCH auf status=published, client_id oder fee_percentage laesst die Werte unveraendert.

**P0.32 · datenmodell und frontend-a11y wollen draft_state bzw. Tagessatz in briefing_notes legen - genau das Feld, das der Studio bei jedem Speichern ueberschreibt** — `BEWIESEN` · Red Team · Aufwand S
- *Beleg:* src/components/dashboard/JobIntakeStudio.tsx:347 schreibt bedingungslos briefing_notes: serializeBriefing(answers) || null. src/components/admin/JobApprovalDialog.tsx:182 stellt dem Feld '[ABGELEHNT] reason' voran. src/pages/dashboard/JobsList.tsx:59 und src/pages/dashboard/ClientJobDetail.tsx:280 parsen dieses Praefix per startsWith wieder heraus. serializeBriefing (src/components/dashboard/IntakeBriefing.tsx:320-329) liefert bei leerem answers den leeren String, also null.
- *Problem:* In briefing_notes liegen bereits drei Bedeutungen (Briefingtext, Ablehnungsgrund, freie Notiz aus BriefingNotesDialog.tsx:42). Ein JSON-Block als vierte Bedeutung zerstoert den startsWith-Parser und wird beim naechsten Studio-Save ohnehin ueberschrieben. Beide Agenten empfehlen die Ablage in einem Feld, dessen Schreiber sie selbst dokumentiert haben.
- *Empfehlung:* Beide Varianten verwerfen. Die einzige richtige Fassung steht bei architekt F10: eine rein additive Migration ALTER TABLE jobs ADD COLUMN draft_state jsonb - eine Zeile, kein Konflikt mit bestehenden Lesern, sofort deploybar ohne die grosse Hybrid-Migration. Tagessatz analog in salary_min/salary_max spiegeln (existierende Spalten), nicht in Freitext.
- *Akzeptanzkriterium:* briefing_notes enthaelt in keinem Datensatz JSON; ein gespeicherter Entwurf laesst sich mit allen Antworten fortsetzen; ein zurueckgegebener Job behaelt seinen Ablehnungsgrund.

**P0.33 · Der Studio zerstoert beim Ergaenzen eines zurueckgegebenen Jobs den Ablehnungsgrund - von keinem der vier Agenten gefunden** — `BEWIESEN` · Red Team · Aufwand S
- *Beleg:* Einstieg: src/pages/dashboard/JobsList.tsx:457 Button 'Ergaenzen & einreichen' -> openInStudio (Zeile 147). Hydrierung JobIntakeStudio.tsx:289 ds = r.intake_payload?.draft_state -> undefined (REST jobs?select=intake_payload -> 400), :303 setAnswers({}). Speichern: buildRecord :347 briefing_notes: serializeBriefing({}) || null = null, per updateJobWithIntake (src/lib/intakeCapture.ts:82) auf die Zeile geschrieben. Der Ablehnungsgrund existiert produktiv NUR dort: jobs.rejection_reason -> REST 400.
- *Problem:* Der einzige Weg, den der Kunde fuer 'zurueckgegeben' angeboten bekommt, loescht die Begruendung, wegen der er den Weg geht - inklusive des vorher tatsaechlich persistierten Briefingtexts. Danach faellt phase (ClientJobDetail.tsx:291) von 'returned' auf 'draft' zurueck und das Amberbanner mit dem Grund verschwindet.
- *Empfehlung:* Sofort und ohne Migration: in buildRecord briefing_notes nur schreiben, wenn serializeBriefing(answers) nicht leer ist, und ein vorhandenes [ABGELEHNT]-Praefix erhalten (Praefixzeile abtrennen, neuen Text anhaengen). Danach Migration 20260710120000_job_review_feedback nachziehen und rejection_reason als alleinige Wahrheit fuehren.
- *Akzeptanzkriterium:* Ein zurueckgegebener Job behaelt nach Oeffnen, Speichern und Schliessen im Studio seinen Ablehnungsgrund; das Amberbanner bleibt sichtbar.

**P0.34 · Recruiter koennen die Marge von Matchunt direkt auslesen - von keinem Agenten gefunden** — `BEWIESEN` · Red Team · Aufwand S
- *Beleg:* REST mit anon-Key: recruiter_jobs_view?select=fee_percentage -> 200 und recruiter_jobs_view?select=recruiter_fee_percentage -> 200. Quelle: supabase/migrations/20260725132524_a9e4cdb4-147d-46bd-8b0d-6f4093e27411.sql listet beide Spalten unmaskiert, GRANT SELECT an authenticated, WHERE has_role(auth.uid(),'recruiter'). Semantik: src/components/admin/JobApprovalDialog.tsx:310 Kommentar 'Fee Percentage (Client pays)', :76-77 Defaults 20 bzw. 15 Prozent. Zusaetzlich liefert recruiter_jobs_view?select=embedding -> 200.
- *Problem:* Die View ist gegen Firmenidentitaet gehaertet worden, aber die kommerziellen Konditionen beider Seiten stehen unmaskiert nebeneinander. Ein Recruiter braucht genau eine Abfrage, um zu sehen, was der Kunde zahlt und was er selbst bekommt.
- *Empfehlung:* fee_percentage aus recruiter_jobs_view entfernen (der Recruiter braucht nur seinen eigenen Satz). embedding ebenfalls entfernen - kein Frontend-Pfad liest ihn, das Matching laeuft serverseitig. Beides ist ein CREATE OR REPLACE VIEW ohne Frontend-Aenderung.
- *Akzeptanzkriterium:* recruiter_jobs_view?select=fee_percentage und ?select=embedding liefern je 400; das Recruiter-Frontend funktioniert unveraendert.

**P0.35 · Auch ohne Entwurfs-Speichern gehen 100 Prozent der KI-Briefing-Antworten verloren - die vier Audits beschreiben nur den Draft-Fall** — `BEWIESEN` · Red Team · Aufwand S
- *Beleg:* src/components/dashboard/IntakeBriefing.tsx:322 iteriert serializeBriefing ausschliesslich ueber die statische Konstante QUESTIONS. Die dynamischen Antworten liegen in dyn.answers und werden nur an zwei Stellen geschrieben: JobIntakeStudio.tsx:374 intake_payload.briefing_dynamic und :393 draft_state - beide innerhalb von intake_payload (REST jobs?select=intake_payload -> 400). Der Insert-Fallback src/lib/intakeCapture.ts:56 schreibt nur base; base enthaelt an briefingbezogenen Feldern allein briefing_notes (:347) u
- *Problem:* architekt F10, datenmodell F3 und frontend-a11y F2 beschreiben alle den Verlust beim Zwischenspeichern und Fortsetzen. Der Verlust ist unbedingt: auch wer in einer Sitzung durchgeht und direkt einreicht, liefert eine Zeile ab, in der keine einzige KI-Briefing-Antwort steht.
- *Empfehlung:* Reihenfolge festlegen: die additive Spalte (draft_state jsonb, besser gleich intake_payload jsonb aus 20260619120000) MUSS vor dem Deploy von intake-questions liegen. Bis dahin serializeBriefing um dyn.answers erweitern, damit wenigstens der Text in briefing_notes ankommt. Und intakeCapture darf den Verlust nicht mehr verschweigen (siehe naechster Punkt).
- *Akzeptanzkriterium:* Ein per Studio eingereichter Job enthaelt in einer produktiv existierenden Spalte den Volltext aller dynamischen Briefing-Antworten; die Parse-/Frage-Funktionen werden nicht vor dieser Spalte deployed.

**P1.1 · Der Nutzer sieht bei jedem Import zwei Fehlermeldungen: eine englische Rohmeldung und eine, die ihm die Schuld gibt** — `BEWIESEN` · Frontend/A11y · Aufwand S
- *Beleg:* node_modules/@supabase/functions-js/dist/module/types.js:62 (`super('Edge Function returned a non-2xx status code', 'FunctionsHttpError', context)`) und dist/module/FunctionsClient.js:128-129 (`if (!response.ok) throw new FunctionsHttpError(response)`); src/hooks/useJobParsing.ts:62-64 (`throw new Error(fnError.message)`) und :73-75 (`toast.error(errorMessage)`); danach src/components/dashboard/JobIntakeStudio.tsx:240 (`toast.error('Konnte keine Stelle erkennen – bitte mehr Text/Details angeben.')`); PDF-Variante: 
- *Problem:* Bei 404 liefert supabase-js einen generischen englischen Fehlertext, den beide Hooks ungefiltert in einen Toast schreiben. Direkt danach feuert `startBuild` einen zweiten Toast, der den Fehler dem Input des Nutzers zuschreibt. Der Nutzer bekommt also gleichzeitig eine unverständliche Systemmeldung und eine falsche Ursachenzuschreibung. Beim PDF-Weg wird die Datei zusätzlich vorher nach `job-documents/uploads/` hochge
- *Empfehlung:* In beiden Hooks den Fehler klassifizieren (`error.context?.status === 404` bzw. `FunctionsFetchError`) und einen deutschen, handlungsleitenden Text zurückgeben („Der KI-Import ist gerade nicht verfügbar — Sie können die Stelle manuell anlegen"). Den zweiten Toast in `startBuild` nur dann feuern, wenn die Function geantwortet hat, aber nichts erkannt wurde. Bei Fehlschlag der Analyse die hochgeladene Datei per `supaba
- *Akzeptanzkriterium:* Bei einem 404 der Import-Function erscheint genau ein deutscher Toast mit einem konkreten nächsten Schritt; in `job-documents/uploads/` liegt danach keine neue Datei.

**P1.2 · Tote primäre CTA am Ende des Briefings: „Weiter zur Übergabe" und „Fertig" tun nichts** — `BEWIESEN` · Frontend/A11y · Aufwand S
- *Beleg:* src/components/dashboard/JobIntakeStudio.tsx:632 (`onDone={() => undefined}`) → src/components/dashboard/intake/DynamicBriefing.tsx:142 (`onDone={onDone}` an das Fallback-`IntakeBriefing`) → src/components/dashboard/IntakeBriefing.tsx:379-380 (`<Button variant="hero" className="ml-2" onClick={onDone}>Weiter zur Übergabe</Button>`); zweiter toter Aufruf: DynamicBriefing.tsx:268 (`<Button size="sm" variant="outline" onClick={onDone}>Fertig</Button>`).
- *Problem:* `onDone` wird vom Studio als leere Funktion durchgereicht. In der Produktionsrealität (intake-questions = 404 → statisches Fallback-Briefing) erreicht jeder Nutzer, der das Briefing durcharbeitet, den Abschluss-Screen IntakeBriefing.tsx:369-382 mit einer `variant="hero"`-Schaltfläche — der visuell stärksten Schaltfläche des Design-Systems — die auf Klick nichts auslöst: kein Scroll, kein Stage-Wechsel, kein Fokuswech
- *Empfehlung:* `onDone` im Studio mit einer echten Aktion belegen: Fokus auf den Footer-Button „An Recruiter übergeben" setzen und ihn in den Viewport scrollen (`ref.current?.scrollIntoView({block:'nearest'}); ref.current?.focus()`). Alternativ die beiden Buttons entfernen, wenn kein Ziel existiert — eine Schaltfläche ohne Wirkung ist schlechter als keine.
- *Akzeptanzkriterium:* Ein Klick auf „Weiter zur Übergabe" bewegt Fokus und Viewport messbar auf die Übergabe-Aktion; kein Button im Flow hat einen No-Op-Handler.

**P1.3 · „PDF" und „Link" im Dashboard-Launcher machen nicht, was sie ankündigen — mit Text im Feld starten sie sogar den falschen Parser** — `BEWIESEN` · Frontend/A11y · Aufwand S
- *Beleg:* src/components/dashboard/NeueStelleBar.tsx:32 (`openStudio = (source) => setStudio({ open: true, text: seed, source, draft: null })`), :103 (`onClick={() => openStudio('pdf')}`), :106 (`onClick={() => openStudio('url')}`); src/components/dashboard/JobIntakeStudio.tsx:284 (`setShowUrl(initialSource === 'url')` — `'pdf'` wird nirgends ausgewertet; `fileRef.current?.click()` kommt in der Öffnen-Effect nicht vor) und :309-311 (`else if (initialText && initialText.trim()) { void buildFromText(initialText); }`).
- *Problem:* `initialSource='pdf'` hat keinerlei Wirkung: Der Dateidialog wird nicht geöffnet, der Nutzer landet im Textfeld. Schlimmer: Steht im Titel-Feld des Dashboards Text (der Normalfall — es ist das Haupt-Eingabefeld mit Placeholder „Jobtitel — oder Anzeige einfügen …"), greift Zeile 309 und startet sofort einen TEXT-Parse. Der Nutzer klickt „PDF", sieht die Bau-Animation für seinen Titeltext, bekommt den Fehler-Toast und 
- *Empfehlung:* In der Öffnen-Effect `initialSource` vollständig auswerten: bei `'pdf'` den Auto-Parse unterdrücken und `fileRef.current?.click()` in einem `requestAnimationFrame` nach dem Mount auslösen; bei `'url'` den Auto-Parse ebenfalls unterdrücken und stattdessen das URL-Feld fokussieren. Der Auto-Parse aus `initialText` darf nur bei `initialSource === 'text'` greifen.
- *Akzeptanzkriterium:* Klick auf „PDF" im Dashboard öffnet den Dateidialog, unabhängig davon, ob das Titelfeld gefüllt ist; Klick auf „Link" fokussiert das URL-Feld und startet keinen Text-Parse.

**P1.4 · Der neue Flow ist für Screenreader und Tastatur unbedienbar — der alte war es nicht (Design-System-Regression)** — `BEWIESEN` · Frontend/A11y · Aufwand M
- *Beleg:* KEINE Namen: src/components/dashboard/intake/ProfileSections.tsx:106-111 (Titel-Input), :116 (Standort), :118 (Select Remote), :125 (Select Level), :134 (Branche), :141-161 (4 Contracting-Inputs), :162-166 (Select Auslastung), :261-266 (Reveal-Descriptor), :269-276 (Select Reveal-Trigger) — durchgängig nur `placeholder`, kein `<Label htmlFor>`, kein `aria-label`, kein `aria-labelledby`. GEGENBEISPIEL alter Flow: src/pages/dashboard/CreateJob.tsx:1182-1196, :1209-1237 (`<Label htmlFor="title">` + `<Input id="title">
- *Problem:* Drei kumulative Verstöße. (1) WCAG 4.1.2/3.3.2: Zehn Formularfelder haben keinen zugänglichen Namen — Placeholder verschwinden beim Tippen und werden von mehreren Screenreadern gar nicht als Name gewertet. (2) WCAG 4.1.3: Jeder Zustandswechsel im Studio (Bauen läuft, Profil fertig, nächste Frage, Spannung erkannt, Reifegrad geändert) ist rein visuell — es gibt in beiden Flows keine einzige Live-Region. (3) Bei JEDER 
- *Empfehlung:* (a) Jedes Feld in ProfileSections mit einem sichtbaren `<Label>` oder mindestens `aria-label` versehen — die Section-Struktur (ProfileSections.tsx:36-45) bietet sich für `aria-labelledby` an. (b) Eine `<div role="status" aria-live="polite" className="sr-only">` in JobIntakeStudio, die Stage-Wechsel, „Profil gebaut, 7 Felder", die aktuelle Frage und Speicher-Ergebnisse ansagt. (c) `aria-pressed` an alle Chip- und Togg
- *Akzeptanzkriterium:* axe-core meldet für die `built`-Stage null Verstöße der Regeln `label`, `aria-input-field-name` und `select-name`; nach dem Beantworten einer Briefing-Frage liegt der Fokus auf der neuen Frage, nicht auf dem Dialog-Container.

**P1.5 · Auf dem Smartphone ist der Flow nicht sinnvoll abschließbar: 16-32px-Ziele, 12px-Text mit iOS-Zoom, kein Fortschritt, Briefing zwei Bildschirme tief** — `BEWIESEN` · Frontend/A11y · Aufwand L
- *Beleg:* Fenster: src/components/dashboard/JobIntakeStudio.tsx:499 (`h-[90vh] w-[96vw] max-w-5xl`, `overflow-hidden`), :533 (innerer Scroll-Container). Layout: :594 (`grid-cols-1 gap-5 lg:grid-cols-2` — zweispaltig erst ab 1024px, iPad-Portrait 768px bekommt ebenfalls eine Spalte) mit `ProfileSections` (:595) VOR dem Briefing (:626) und `QualityCheck` (:635) ganz unten. Fortschritt: :516 (`hidden items-center gap-2 md:flex`) — unter 768px gibt es KEINE Fortschrittsanzeige im Kopf. Trefferflächen: ProfileSections.tsx:61 (`<b
- *Problem:* Alle interaktiven Elemente der linken Spalte liegen zwischen 16px und 32px und damit unter der 44x44-CSS-Pixel-Schwelle (WCAG 2.5.5 AAA / 2.5.8 AA = 24px, die 16px-Löschknöpfe reißen auch die AA-Schwelle). Der `text-xs`-Override hebelt genau die Konvention aus, die shadcn im Input-Basisstil eingebaut hat, um den automatischen iOS-Safari-Zoom bei Feldern unter 16px zu verhindern — in einem `translate`-zentrierten `h-[
- *Empfehlung:* (a) Mobile-First-Overrides statt Desktop-Miniaturen: `h-8` → `h-10 md:h-8`, `text-xs` → `text-sm md:text-xs` an allen Feldern in ProfileSections und DynamicBriefing; die X-Löschknöpfe auf mindestens 24x24 (`p-1.5` + Icon 14px) bringen. (b) `grid-cols-2` auf `grid-cols-1 sm:grid-cols-2` umstellen. (c) Auf Mobil die Reihenfolge umdrehen (`flex flex-col-reverse lg:grid`) oder eine Tab-Leiste „Profil | Briefing | Reife" 
- *Akzeptanzkriterium:* Bei 390x844 mit aktiver Tastatur bleibt „An Recruiter übergeben" sichtbar; kein interaktives Element im `built`-Screen ist kleiner als 24x24 CSS-Pixel; kein Eingabefeld unter 16px Schriftgröße; die erste Briefing-Frage ist ohne Scrollen oder nach höchstens einem Tap erreichbar.

**P1.6 · Freelance-Jobs haben in Produktion gar kein Preisfeld — der Gehaltsteil des Matchings ist blind** — `BEWIESEN` · Datenmodell · Aufwand M
- *Beleg:* JobIntakeStudio.tsx:342-343 `salary_min: isFreelance ? null : ...`, `salary_max: isFreelance ? null : ...`. Tagessaetze gehen nach intake_payload.contracting (:382, Typ FreelanceTerms in intake/types.ts:26-32). REST jobs?select=intake_payload -> 400. Matcher: :983 `const jobSalaryMax = job.salary_max || 0` -> Dealbreaker bleibt 1.0; :1455 `if (candidateSalary > 0 && jobSalaryMax > 0)` -> salaryScore bleibt 100. Es gibt keine day_rate-Spalte (nicht in types.ts jobs Row, nicht in 20260619120000).
- *Problem:* Der Contracting-Fork ist im UI und im KI-Prompt sauber getrennt (intake-questions:165), aber im Datenmodell existiert er nicht: Tagessatz, Laufzeit, Auslastung und Verlaengerungsoption haben keine Zielspalte, und die vorhandenen Gehaltsspalten werden aktiv genullt.
- *Empfehlung:* Entweder day_rate_min/day_rate_max/engagement_months/utilization_days_per_week als Spalten ergaenzen und den Matcher auf `job.day_rate_max ?? job.salary_max` normalisieren — oder (billiger) den Tagessatz beim Speichern in salary_min/max spiegeln und employment_type='freelance' als Einheiten-Diskriminator nutzen. Die zweite Variante braucht null Migration.
- *Akzeptanzkriterium:* Ein Contracting-Job mit Tagessatz-Deckel 800 EUR erzeugt fuer einen Kandidaten mit 1200-EUR-Erwartung einen Salary-Dealbreaker < 1.0.

**P1.7 · Der Recruiter sieht vom gesamten Briefing exakt null Felder** — `BEWIESEN` · Datenmodell · Aufwand L
- *Beleg:* Migration 20260725120000_recruiter_jobs_view_hardening.sql:98-101 schliesst briefing_notes und intake_briefing bewusst aus. REST gegen die (deployte, company_revealed -> 200) View: intake_payload 400, briefing_notes 400, reveal_envelope 400, search_difficulty 400, unique_selling_points 400, vacancy_reason 400, reports_to 400, team_size 400, intake_completeness 400. Einzige Vorreveal-Textfassung: formatted_content aus format-job-for-recruiters — diese Function liest nur title/description/requirements/must_haves/nice
- *Problem:* Das Briefing wird erhoben, um „gezieltes blindes Sourcing" zu ermoeglichen (intake-questions:160), aber der Zielleser hat keinen Kanal dafuer. Was fuer den Recruiter gedacht war (Suchbild, Anti-Persona, Zielfirmen, Sell-Story), ist in derselben Spalte gelandet, die die Haertungsmigration als Identitaetsvektor klassifiziert und entfernt hat.
- *Empfehlung:* Ein neues, redaktionell getrenntes Feld einfuehren: jobs.recruiter_brief (jsonb) mit ausschliesslich nicht-identifizierenden Briefing-Bestandteilen (Suchbild, Anti-Persona, Skill-Tiefe, Sell-Argumente, Prozess) — befuellt aus der green_list-Logik, NIE aus red_list-Inhalten — und in recruiter_jobs_view unter Klasse A aufnehmen. briefing_notes bleibt client-privat.
- *Akzeptanzkriterium:* Ein Recruiter sieht fuer einen Studio-Job vor dem Reveal Suchbild und Muss-Tiefe; ein Diff gegen die red_list findet keinen Treffer.

**P1.8 · Zwei Aufnahmepfade schreiben unterschiedliche Spalten fuer dieselben Fakten (Split-Brain)** — `BEWIESEN` · Datenmodell · Aufwand M
- *Beleg:* CreateJob.tsx:627 schreibt remote_type UND :637 remote_policy; JobIntakeStudio.tsx:339 schreibt nur remote_type. _shared/fit-assessment.ts:29 buildJobSection liest `job.remote_policy` — genutzt von assess-candidate-fit (deployed, POST -> 400) und vom V4-Judge. Weiter nur in CreateJob: industry :639, company_size_band :640, funding_stage :641, tech_environment :642, team_size :644, company_culture :649, career_path :650, success_profile :651, daily_routine :652, trainable_skills :653, must_have_criteria :654, nice_t
- *Problem:* Dieselbe fachliche Aussage („Arbeitsmodell", „Briefingtext") hat je nach Aufnahmeweg eine andere Spalte. Es gibt keine Normalisierungsschicht, die das zusammenfuehrt — jeder Konsument waehlt eine der beiden und sieht die andere Haelfte des Bestands als NULL.
- *Empfehlung:* remote_policy im Studio mitschreiben (identischer Wert wie remote_type) und buildJobSection auf `job.remote_policy ?? job.remote_type` haerten. intake_briefing als deprecated markieren, briefing_notes als einzige Narrativ-Spalte fuehren und das Ablehnungsfeedback in die dafuer vorhandene Spalte client_approval_note (REST 200) umziehen.
- *Akzeptanzkriterium:* assess-candidate-fit liefert fuer einen Studio-Job einen Prompt mit gefuelltem Remote-Feld; briefing_notes enthaelt nach einer Admin-Ablehnung keinen [ABGELEHNT]-Praefix mehr.

**P1.9 · Der Parser liefert 26 Felder, der Studio uebernimmt 20 — sechs davon haetten eine vorhandene Zielspalte** — `BEWIESEN` · Datenmodell · Aufwand M
- *Beleg:* ParsedJobData (useJobParsing.ts:5-45) enthaelt team_size, team_avg_age, department_structure, core_hours, remote_days, overtime_policy, daily_routine, company_culture, benefits_extracted, career_path, hiring_deadline_weeks, company_size_estimate. fromData (JobIntakeStudio.tsx:35-46) uebernimmt davon nur reports_to, hiring_urgency, remote_days, vacancy_reason, unique_selling_points, industry — der Rest faellt weg. fromProfile (:48-58) verwirft soft_skills, experience_years_min/max, benefits, team_info, application_p
- *Problem:* Der Verlust passiert an der Typgrenze ParsedJobData -> BuiltJob, nicht erst beim Speichern. BuiltJob (intake/types.ts:3-22) hat nur 20 Felder und ist damit der eigentliche Flaschenhals der gesamten Aufnahme. Besonders bitter: fromProfile wirft experience_years_min/max weg — genau die Werte fuer experience_min/max, die der Matcher liest (v3-1:1388-1389) und die die Hybrid-Migration anlegt.
- *Empfehlung:* BuiltJob um experienceMin/experienceMax/teamSize/companyCulture/benefits/careerPath/coreHours/dailyRoutine erweitern und in buildRecord durchreichen. Saetze nicht mehr nach must_haves schreiben: Rohtext bleibt in requirements, must_haves nur noch aus normalisierten Skill-Tokens.
- *Akzeptanzkriterium:* Ein PDF-Import erzeugt einen Job mit gesetzten experience_min/experience_max; das Erfahrungs-Scoring liefert fuer einen unterqualifizierten Kandidaten < 100.

**P1.10 · Falschaussage "Ihr Account ist jetzt vollständig verifiziert" bei kyc_status='in_review' — und das KYC-Gate ist ohnehin wirkungslos** — `BEWIESEN` · Recht · Aufwand M
- *Beleg:* src/hooks/useClientVerification.ts:148-155 setzt beim Abschluss kyc_status:'in_review'. src/pages/onboarding/ClientOnboarding.tsx:99-109 springt danach auf Step 3, der :305-308 anzeigt: "Verifizierung abgeschlossen! Ihr Account ist jetzt vollständig verifiziert." Die interne Wahrheit widerspricht dem: useClientVerification.ts:169-171 isFullyVerified verlangt kyc_status==='verified'. Das produktive Gate ignoriert KYC komplett: :174 canPublishJobs = terms_accepted && contract_signed, verwendet in src/pages/dashboard/
- *Problem:* Der Kunde bekommt schriftlich bestätigt, ein Prüfverfahren sei abgeschlossen, das gerade erst begonnen hat — und es gibt im Code keinen Admin-Pfad, der 'in_review' jemals auf 'verified' hebt (kein Treffer für kyc_verified_by-Schreibvorgänge im Client-Code). Praktisch bleibt jeder Kunde dauerhaft 'in_review', während der Effekt-Gate ihn trotzdem publizieren lässt.
- *Empfehlung:* Text sofort auf die Wahrheit ändern: "Angaben eingereicht — wir prüfen innerhalb von X Werktagen". Entweder KYC ernst nehmen (Admin-Freigabe-UI bauen, canPublishJobs auf kyc_status==='verified' heben, Handelsregister/USt-ID verpflichtend + Formatprüfung) oder den KYC-Schritt ersatzlos streichen und nirgends mehr mit "verifiziert" werben. Halbe Lösung ist die schlechteste: sie erzeugt die Aussage ohne die Kontrolle.
- *Akzeptanzkriterium:* Kein UI-Text behauptet "vollständig verifiziert", solange kyc_status !== 'verified'; entweder existiert eine Admin-Freigabe, die kyc_status/kyc_verified_by setzt und canPublishJobs daran hängt, oder sämtliche "verifiziert"-Aussagen zu Kunden sind aus Produkt und Marketing entfernt.

**P1.11 · "Digitale Unterschrift" als Textfeld ohne Dokument, ohne Zeitstempel-Kontext und ohne Vertretungsprüfung — Beweiswert nahe null** — `BEWIESEN` · Recht · Aufwand M
- *Beleg:* src/pages/onboarding/ClientOnboarding.tsx:231-238 (Input, Placeholder "Max Mustermann", nur .trim()-Prüfung in :83-87), Vertragstext nur 2 Sätze im DOM :222-228 ("Hiermit bestätige ich, dass ich berechtigt bin ..."), nicht persistiert. Gespeichert wird ausschließlich src/hooks/useClientVerification.ts:121-128: contract_signed, contract_signed_at, digital_signature (Freitext). contract_pdf_url existiert als Spalte (supabase/migrations/20251204204702_...sql:17), wird aber nirgends geschrieben (grep über src/ und supa
- *Problem:* Gespeichert wird ein beliebiger Name-String. Nicht gespeichert werden: der unterzeichnete Text, dessen Version/Hash, IP, User-Agent, Bestätigungs-E-Mail. Die Vertretungsbefugnis wird lediglich behauptet, nicht geprüft (kein Abgleich mit der - optionalen und ungeprüften - Handelsregisternummer).
- *Empfehlung:* Kein neues Signaturprodukt nötig, aber Beweiskette schließen: Beim Klick den vollständigen Vertragstext als unveränderliches PDF/HTML mit Hash in Storage ablegen (contract_pdf_url füllen), zusammen mit Versions-ID, Zeitstempel, IP, User-Agent und der bestätigten E-Mail-Adresse; unmittelbar danach eine Bestätigungsmail mit dem Dokument an den Zeichner (Double-Opt-In-Charakter). Für Beträge oberhalb einer intern defini
- *Akzeptanzkriterium:* Zu jedem contract_signed=true existiert ein abrufbares Vertragsdokument mit Hash, Versions-ID, Zeitstempel, IP/User-Agent und der Bestätigungsmail an den Zeichner; contract_pdf_url ist nie null bei signierten Datensätzen.

**P1.12 · EU AI Act: Matchunt stuft sich selbst als Anbieter eines Hochrisiko-KI-Systems ein — die Anbieterartefakte existieren im Code nicht** — `ABGELEITET` · Recht · Aufwand XL
- *Beleg:* Selbsteinstufung: src/pages/public/AGB.tsx:129-138 (§ 8a Abs. 1/2: Anhang III Nr. 4 lit. a/b, "Matchunt ... nimmt insoweit die Rolle des Anbieters (Art. 3 Nr. 3 KI-Verordnung) ein") und src/pages/public/Datenschutz.tsx:314-319 (Ziffer 12a Abs. 1). Was im Code existiert: Modell-/Prompt-Versionierung und Persistenz der Bewertung in supabase/functions/assess-candidate-fit/index.ts:251-270 (model_used, prompt_version). Was fehlt (grep über supabase/ und src/): keine Gebrauchsanweisung/Instructions for Use für Betreiber
- *Problem:* Die Rechtstexte weisen Matchunt genau die Rolle zu, an der die schwersten Pflichten hängen (Art. 9-15, 16, 17, 18, 19, 43, 47, 48, 49, 72, 73 KI-VO). Gleichzeitig verlagern § 8b Abs. 6 und § 8e die Protokollierungs- und Freistellungslast auf den Kunden, obwohl die Aufbewahrung der automatisch erzeugten Protokolle nach Art. 19 KI-VO den Anbieter trifft und dem Kunden technisch keine Protokolle zur Verfügung stehen.
- *Empfehlung:* Anwaltlich zuerst die Einstufung selbst prüfen lassen, nicht die Umsetzung: Fällt die Fit-Bewertung tatsächlich unter Anhang III Nr. 4, oder greift die Filterausnahme nach Art. 6 Abs. 3 KI-VO (rein vorbereitende Aufgabe)? Wenn Hochrisiko bejaht wird: Mindestpaket priorisieren — Gebrauchsanweisung nach Art. 13 als abrufbares Dokument im Produkt, Betreiber-Protokollzugang (Art. 19/26 Abs. 6), technische Dokumentation n
- *Akzeptanzkriterium:* Anwaltlich dokumentierte Einstufungsentscheidung liegt vor; bei "Hochrisiko" existieren Gebrauchsanweisung, technische Dokumentation, Protokollzugang für Betreiber und ein Registrierungsnachweis; bei "nicht Hochrisiko" sind AGB § 8a-8f und Datenschutz Ziffer 12/12a entsprechend angepasst und die Bet

**P1.13 · DSGVO-Rollenverteilung widerspricht sich zwischen /datenschutz und /agb; weder AVV noch Art.-26-Vereinbarung existieren im Produkt** — `BEWIESEN` · Recht · Aufwand L
- *Beleg:* src/pages/public/Datenschutz.tsx:24-31 (Geltung ausdrücklich auch "für Kandidatinnen und Kandidaten, deren Daten über die Plattform verarbeitet werden") i.V.m. :35-45 ("Verantwortlicher im Sinne der DSGVO ist: Bluewater & Bridge GmbH") und :239-308 (Matchunt beschreibt Zweck, Mittel, Modellwahl der Fit-Bewertung selbst). Gegenteilige Zuweisung: src/pages/public/AGB.tsx:351-354 ("Für die von ihnen eingestellten Kandidatendaten sind die jeweiligen Recruiter bzw. Unternehmen datenschutzrechtlich verantwortlich; Matchu
- *Problem:* Zwei live ausgelieferte Rechtstexte behaupten Gegensätzliches über die zentrale Frage, wer für Kandidatendaten verantwortlich ist. Faktisch bestimmt Matchunt Zwecke und Mittel (Fit-Score, Ranking, Reveal-Regeln, Modellauswahl, Redaktionsstufe) und ist damit sehr wahrscheinlich Verantwortlicher bzw. gemeinsam Verantwortlicher — nicht Auftragsverarbeiter. Der in AGB § 14 Abs. 2 in Aussicht gestellte gesonderte Vertrag 
- *Empfehlung:* Rolle einmal anwaltlich festlegen und beide Texte darauf ausrichten. Bei (gemeinsamer) Verantwortlichkeit: Art.-26-Vereinbarung als Teil der Recruiter-/Kundenverträge, Wesentliches den Betroffenen zugänglich machen, und Art.-14-Information auf jeder kandidatenseitigen Token-Seite und in jeder Kandidaten-E-Mail verlinken. Bei Auftragsverarbeitung: AVV-Annahme in den Onboarding-Flow integrieren und protokollieren. AGB 
- *Akzeptanzkriterium:* /agb und /datenschutz benennen dieselbe Rolle; die passende Vereinbarung (Art. 26 oder Art. 28) wird im Onboarding protokolliert abgeschlossen; jede kandidatenseitige Seite und E-Mail enthält einen Link auf die Datenschutzerklärung.

**P1.14 · KI-Jobaufnahme sendet ungefilterte Interna an OpenRouter — von der Datenschutzerklärung nicht gedeckt, ohne Redaktion und ohne Protokoll** — `BEWIESEN` · Recht · Aufwand M
- *Beleg:* supabase/functions/intake-questions/index.ts:190-198 postet an https://openrouter.ai/api/v1/chat/completions, Modell google/gemini-2.5-flash. Übertragen wird der komplette Entwurf: :182-188 (JSON.stringify(job_draft) + alle bisherigen Freitext-Antworten). Der System-Prompt fordert aktiv sensible Inhalte an: :171 ("Aus welcher Rolle/welchen Firmen kommt der Ideal-Kandidat? Anti-Persona?"), Kapitel "Prozess & Entscheider" :47, Ausgabefelder target_companies/nogo_companies :97-98 und red_list :122. Keine PII-Redaktion
- *Problem:* Die Datenschutzerklärung beschreibt unter Ziffer 12 exakt eine KI-Verarbeitung — die Bewertung von Kandidaten über das Lovable AI Gateway. Die Jobaufnahme über OpenRouter ist eine zweite, inhaltlich andere Verarbeitung: sie überträgt Firmeninterna, Namen von Entscheidern (soweit der Kunde sie in Freitext nennt), Ziel- und No-Go-Firmenlisten sowie die red_list, also gerade die Angaben, die den Kunden de-anonymisieren 
- *Empfehlung:* (1) Datenschutzerklärung um die Intake-Verarbeitung ergänzen (Zweck, Empfänger OpenRouter, Kategorien, Drittland, Aufbewahrung) — das ist der schnelle Teil. (2) Vor dem Deploy von intake-questions eine Allowlist-Redaktion analog pii-redaction.ts einziehen, die Personennamen aus Freitextantworten entfernt, bevor der Prompt gebaut wird. (3) Minimalprotokoll je Aufruf persistieren: job_id, Zeitstempel, Modell, Prompt-Ve
- *Akzeptanzkriterium:* Datenschutzerklärung nennt die Jobaufnahme als eigene KI-Verarbeitung mit Empfänger und Drittlandgrundlage; intake-questions redigiert Personennamen vor dem Prompt und schreibt je Aufruf einen Protokolldatensatz; AVV und Retention-Zusage von OpenRouter liegen abgelegt vor.

**P1.15 · Der Verifikations-Banner nagt dauerhaft, die Erfolgsmeldung ist unwahr, und der CTA führt in eine Schleife mit leeren Feldern** — `BEWIESEN` · Conversion/UX · Aufwand S
- *Beleg:* src/pages/onboarding/ClientOnboarding.tsx:101 → src/hooks/useClientVerification.ts:153 setzt kyc_status:'in_review'; useClientVerification.ts:169-171 isFullyVerified verlangt kyc_status==='verified'; ClientOnboarding.tsx:305-307 zeigt trotzdem 'Verifizierung abgeschlossen!' / 'Ihr Account ist jetzt vollständig verifiziert'; src/components/verification/VerificationStatusBanner.tsx:15 blendet den Banner nur bei isFullyVerified aus; :23 progressValue=(currentStep/4)*100 → 100 % bei in_review; :56-57 Titel bleibt 'Veri
- *Problem:* Nach abgeschlossenem Onboarding sagt das Produkt dem Kunden zuerst 'vollständig verifiziert' (falsch), und ab dem nächsten Dashboard-Aufruf dauerhaft 'Verifikation erforderlich' bei 100 % Fortschritt. Klickt er den einzigen angebotenen Ausweg ('Fortfahren'), landet er wieder im Firmendaten-Formular — leer, obwohl er HRB und USt-ID bereits eingereicht hat. Er tippt sie erneut, bekommt 'Firmendaten eingereicht', und de
- *Empfehlung:* 1) Eigener, neutraler Zustand für in_review: kein Alert-Styling, kein CTA, Text: 'Unterlagen eingegangen — wir prüfen sie. Sie können bereits ausschreiben, wir melden uns.' 2) Erfolgsseite ehrlich: statt 'Ihr Account ist jetzt vollständig verifiziert' → 'Alles eingereicht. Ihre Angaben werden geprüft — Sie können sofort loslegen, die Prüfung läuft im Hintergrund.' 3) Firmendaten-Felder aus verification hydrieren; wen
- *Akzeptanzkriterium:* Ein Kunde mit kyc_status='in_review' sieht keinen Alarm-Banner und keinen Onboarding-CTA mehr; ein Rücksprung auf /onboarding zeigt die zuvor eingereichten HRB-/USt-Werte vorbefüllt; kein Screen behauptet 'vollständig verifiziert', solange kyc_status!=='verified'.

**P1.16 · Die 'Briefing-Reife' ist ein Motivations-Placebo: der Score steigt beim Überspringen und wird grün bei einer unbesetzbaren Stelle** — `BEWIESEN` · Conversion/UX · Aufwand M
- *Beleg:* src/components/dashboard/intake/QualityCheck.tsx:79 score = round(progressPct*0.6 + profilePts*0.4 - warnings.length*5); :83 Schwellen (>=80 'gut' grün, >=55 'mittel'); :66-73 profilePts = Titel 20 + Standort 15 + Gehalt 25 + mind. 1 Muss-Kriterium 20 + Descriptor 20; src/components/dashboard/IntakeBriefing.tsx:303 'a.unknown ? q.weight * 0.5 : q.weight' — 'Weiß ich nicht' zählt 50 %; :439 und :466 setzen unknown:true; :357-361 set() speichert die Antwort UND springt automatisch zur nächsten Frage; :315 openBriefin
- *Problem:* Zwei durchgerechnete Fälle. (A) Senior-Rolle in München, Gehaltsmaximum 65k, 10 Muss-Kriterien, alle Fragen beantwortet: profilePts 100, progressPct 100, 2 Warnungen → 60 + 40 − 10 = 90/100, grün, Label 'gut'. Marktseitig ist das eine kaum besetzbare Stelle; der Score sagt das Gegenteil, weil jede Warnung nur 5 Punkte kostet. (B) 34-mal 'Überspringen' (der Button springt selbst weiter, in etwa 15 Sekunden erledigt): 
- *Empfehlung:* 1) unknown = 0 Punkte, aber sichtbar ausweisen: 'Unbeantwortet: 12 von 34 — das erhöht Fehlvorschläge.' 2) Warnungen dürfen den Score nicht nur um 5 Punkte drücken: harte Marktkonflikte (Seniorität vs. Gehaltsband, >6 Muss-Kriterien) deckeln den Score bei 'mittel', statt ihn nur zu dämpfen. 3) Label ehrlich machen: nicht 'Briefing-Reife 90/100' sondern 'Was die Recruiter jetzt wissen: 12 von 20 besetzungsrelevanten A
- *Akzeptanzkriterium:* 34-maliges 'Überspringen' führt zu einem Score unter 40 und einem sichtbaren Hinweis auf die Zahl unbeantworteter Fragen; die Kombination Senior + Gehaltsmaximum unter Marktband + mehr als 6 Muss-Kriterien erreicht nie den grünen Bereich.

**P1.17 · Es gibt keinen Passwort-Reset — und der Wert kommt laut eigener Anzeige erst in der zweiten Sitzung** — `BEWIESEN` · Conversion/UX · Aufwand S
- *Beleg:* grep -rn 'resetPasswordForEmail|forgot|Passwort vergessen|reset-password' src/ → 0 Treffer; src/pages/Auth.tsx enthält im signin-Tab keinen entsprechenden Link (:245-268); src/pages/Auth.tsx:16 passwordSchema = min(6) ohne Stärkeanzeige, ohne Wiederholungsfeld, ohne Sichtbarkeits-Toggle; src/components/dashboard/JobIntakeStudio.tsx:668/674 'Erste Kandidaten · Ø 3–5 Tage'
- *Problem:* Der Nutzer legt in Sitzung 1 ein Konto an und erlebt dort nachweislich keinen Nutzen (der kommt laut Produktversprechen nach 3–5 Tagen). Die Sitzung, in der der Wert eintritt, ist die zweite — und für die gibt es keinen Wiederherstellungsweg, wenn das in 30 Sekunden erfundene 6-Zeichen-Passwort vergessen wurde. Es existiert kein Self-Service, keine Route, kein Link, keine Edge Function.
- *Empfehlung:* Reset-Flow ergänzen (supabase.auth.resetPasswordForEmail + /auth/reset-Route), Link 'Passwort vergessen?' unter das Passwortfeld im signin-Tab. Mindestlänge auf 10 Zeichen mit sichtbarer Stärkeanzeige, plus Sichtbarkeits-Toggle beim Anlegen. Microcopy statt stiller Regel: 'Mindestens 10 Zeichen — Ihre Kandidatendaten liegen dahinter.'
- *Akzeptanzkriterium:* Ein Nutzer kann sich ohne Support ein neues Passwort setzen; die Signup-Maske zeigt Passwortstärke und akzeptiert keine 6-Zeichen-Passwörter mehr.

**P1.18 · Nach 'Konto erstellen' gibt es keinen 'E-Mail bestätigen'-Zustand, und der Bestätigungslink führt auf die Marketing-Startseite** — `ABGELEITET` · Conversion/UX · Aufwand S
- *Beleg:* src/lib/auth.tsx:69 'const { error } = await supabase.auth.signUp(...)' — das data-Objekt wird verworfen, die App kann 'Session da' nicht von 'Bestätigungsmail verschickt' unterscheiden; src/lib/auth.tsx:64 redirectUrl = `${window.location.origin}/`; src/pages/Auth.tsx:104-109 behandelt Erfolg ausschließlich mit Toast + setJustSignedUp(true); Auth.tsx:43-53 leitet nur weiter, wenn `user && role` gesetzt sind — beides bleibt ohne Session null
- *Problem:* Ist die E-Mail-Bestätigung im Supabase-Projekt aktiv (mit den vorhandenen Zugängen nicht prüfbar — ANNAHME), endet die Registrierung in einem stummen Zustand: grüner Toast 'Konto erfolgreich erstellt!', danach unverändert das Registrierungsformular. Kein Hinweis auf den Posteingang, kein 'Mail erneut senden', keine Angabe an welche Adresse. Klickt der Nutzer später den Link in der Mail, landet er wegen emailRedirectT
- *Empfehlung:* 1) In signUp das data-Objekt zurückgeben und in Auth.tsx auf data.session prüfen. 2) Fehlt die Session, einen eigenen Bildschirm rendern statt eines Toasts: 'Fast geschafft. Wir haben eine Bestätigung an {email} geschickt — bitte den Link darin öffnen.' plus Buttons 'Mail erneut senden' und 'Andere Adresse verwenden'. 3) emailRedirectTo auf `${origin}/onboarding` setzen, damit der Klick im Mailclient direkt im Produk
- *Akzeptanzkriterium:* Nach dem Absenden des Signup-Formulars zeigt die Seite entweder das Onboarding (Session vorhanden) oder einen expliziten Bestätigungs-Bildschirm mit genannter E-Mail-Adresse; der Link aus der Bestätigungsmail öffnet /onboarding, nicht /.

**P1.19 · RLS-Antipattern: WITH CHECK (can_edit_job(id)) validiert die ALTE Zeile — client_id und organization_id sind unkontrolliert schreibbar** — `ABGELEITET` · Backend/Security · Aufwand S
- *Beleg:* supabase/migrations/20260710130000_client_team_foundation.sql:446-449 (USING und WITH CHECK sind identisch `public.can_edit_job(id)`). Definition 20260710130000:181-201: `can_edit_job` ist `LANGUAGE sql STABLE SECURITY DEFINER` und schlägt die Zeile per `SELECT ... FROM jobs j WHERE j.id = _job_id` nach. Eine STABLE-Funktion sieht den Snapshot zum Statement-Start, also OLD — der WITH-CHECK-Ausdruck referenziert KEINE einzige Spalte der neuen Zeile. Der einzige Trigger, der organization_id ableitet, ist BEFORE INSER
- *Problem:* WITH-CHECK-Ausdrücke müssen NEW-Spalten direkt prüfen. Ein Row-Lookup per id ist funktional ein zweites USING und lässt jede Spaltenänderung durch, solange man die Zeile vorher bearbeiten durfte.
- *Empfehlung:* WITH CHECK auf NEW-Spalten umstellen, z. B. `WITH CHECK (public.can_edit_job(id) AND client_id = (SELECT client_id FROM jobs WHERE id = jobs.id) AND organization_id IS NOT DISTINCT FROM (SELECT organization_id FROM jobs WHERE id = jobs.id))` — sauberer: einen BEFORE-UPDATE-Trigger, der client_id/organization_id für alle Nicht-Admins auf OLD zurücksetzt, und die identische Prüfung für submissions/interviews/offers, di
- *Akzeptanzkriterium:* PATCH jobs mit fremder organization_id ändert die Spalte nicht; pgTAP-Test „Job kann nicht in fremde Org verschoben werden".

**P1.20 · notifications INSERT mit WITH CHECK (true) — jeder darf im Namen des Systems an jeden schreiben** — `BEWIESEN` · Backend/Security · Aufwand S
- *Beleg:* supabase/migrations/20251204173818_...:29-31 `CREATE POLICY "System can insert notifications" ON public.notifications FOR INSERT WITH CHECK (true);`. grep über alle Migrationen nach `drop policy|revoke` für notifications: 0 Treffer — nie nachgeschärft. REST-Probe: notifications?select=* → 200 (Tabelle existiert, für anon RLS-gefiltert leer). Verwendet mit clientseitig gewähltem Empfänger in src/lib/intakeApproval.ts:71-81 und src/components/dashboard/JobIntakeStudio.tsx:441-448 (`user_id: memberUserId`).
- *Problem:* Es gibt keinen serverseitigen Absender-Zwang. Die Anwendung nutzt die Tabelle als Broadcast-Kanal und hat den Schreibschutz dafür komplett geopfert, statt die Fan-out-Inserts in eine Edge Function mit Service Role zu legen.
- *Empfehlung:* Policy ersetzen durch `WITH CHECK (false)` für authenticated/anon und alle Fan-outs (intakeApproval.notifyApproversOfIntake, notifyCreatorOfDecision, handleDelegate, JobApprovalDialog) in eine Edge Function `notify` mit Service Role verlagern, die Empfänger serverseitig aus der Org-Mitgliedschaft ableitet. Übergangsweise minimal: `WITH CHECK (public.is_org_member((SELECT organization_id FROM jobs WHERE id = related_i
- *Akzeptanzkriterium:* Direkter REST-INSERT in notifications durch einen normalen Nutzer schlägt fehl; die Freigabe-Benachrichtigungen kommen weiterhin an.

**P1.21 · Alle Autorisierungs-Helfer sind ohne Login aufrufbar und akzeptieren eine fremde _user_id — Berechtigungs-Orakel** — `BEWIESEN` · Backend/Security · Aufwand S
- *Beleg:* REST-Probe mit dem anon-Key aus .env, ohne Session, gegen https://dngycrrhbnwdohbftpzq.supabase.co/rest/v1/rpc/: `has_role {"_user_id":"000...","_role":"admin"}` -> 200 false; `can_access_job {"_job_id":...,"_user_id":...}` -> 200 false; `can_edit_job` -> 200 false; `get_org_role {"_org_id":...}` -> 200 null; `is_org_member` -> 200 false; `is_job_collaborator` -> 200 false; `org_intake_approval_required` -> 200 false; `can_create_job {}` -> 200 true. Die Migration erteilt EXECUTE nur an authenticated (2026071013000
- *Problem:* Die Funktionen sind SECURITY DEFINER, umgehen also die RLS der Basistabellen, und nehmen den zu prüfenden User als Parameter statt auf auth.uid() zu bestehen. Damit ist jede Berechtigungsfrage für JEDEN Dritten beantwortbar, ohne Session.
- *Empfehlung:* `REVOKE EXECUTE ON FUNCTION public.get_org_role(uuid,uuid), public.is_org_member(uuid,uuid), public.is_org_admin(uuid,uuid), public.is_job_collaborator(uuid,uuid), public.can_access_job(uuid,uuid), public.can_edit_job(uuid,uuid), public.can_create_job(uuid), public.org_intake_approval_required(uuid), public.has_role(uuid,app_role) FROM PUBLIC, anon;` — und die 2-arg-Overloads mit explizitem _user_id ganz entfernen bz
- *Akzeptanzkriterium:* rpc/has_role und rpc/can_access_job liefern mit dem anon-Key 401/403 statt 200; die RLS-Policies funktionieren unverändert (pgTAP grün).

**P1.22 · Team-Einladungen sind produktiv tot: organization-invite fehlt, accept-invite ist offen und ungebremst deployed** — `BEWIESEN` · Backend/Security · Aufwand M
- *Beleg:* Functions-Probe: POST /functions/v1/organization-invite -> 404 {"code":"NOT_FOUND_FUNCTION_BLOB"}; POST /functions/v1/accept-invite -> 400 {"error":"Token is required"} (also deployed). src/hooks/useOrganizationInvites.ts:65 ist der EINZIGE Erstellpfad (`supabase.functions.invoke('organization-invite')`), useOrganizationInvites.ts:104 der Annahmepfad. REST-Probe: organization_invites?select=token_hash / job_ids / revoked_at -> je 200 (Migration deployed). supabase/functions/accept-invite/index.ts:47-66 validiert to
- *Problem:* Halb ausgerollter Sicherheitspfad: die Schreibseite (Token erzeugen, hashen, Rolle + job_ids setzen) fehlt, die Leseseite (Token einlösen, inkl. serverseitiger Kontoanlage mit email_confirm) ist öffentlich erreichbar und ohne Bruteforce-Schutz.
- *Empfehlung:* organization-invite deployen (sonst die UI-Einstiegspunkte ausblenden, statt einen 404-Fehler zu zeigen) und accept-invite um ein Rate-Limit erweitern: Zähler pro IP und pro token-Präfix in einer Tabelle, Sperre nach 10 Fehlversuchen/Stunde, plus konstante Antwortzeit. Token-Entropie prüfen (>=128 bit).
- *Akzeptanzkriterium:* organization-invite antwortet mit 400 statt 404; 20 fehlerhafte accept-invite-Aufrufe von einer IP führen zu HTTP 429.

**P1.23 · Die schützende Policy hinter NeueStelleBar ist can_access_job — mandantenübergreifend dicht, innerhalb des Mandanten offen** — `BEWIESEN` · Backend/Security · Aufwand S
- *Beleg:* Die Query src/components/dashboard/NeueStelleBar.tsx:38-45 hat keinen client_id-Filter und hängt zu 100 % an: supabase/migrations/20260710130000_client_team_foundation.sql:442-443 `CREATE POLICY "Client team can view their jobs" ON public.jobs FOR SELECT USING (public.can_access_job(id));`. Die alte Policy `"Clients can manage their own jobs" FOR ALL USING (auth.uid() = client_id)` (20251204171610:191-192) wird in derselben Migration Zeile 439 gedroppt. Dass diese Migration in Produktion gelaufen ist, belegen die R
- *Problem:* Es existiert kein Client-seitiger Ownership-Filter; Kollegen-Entwürfe fallen nur deshalb nicht auf, weil `.limit(2)` greift. Das Feature heißt „meine offenen Entwürfe", liefert aber „die 2 neuesten Entwürfe, die ich sehen darf".
- *Empfehlung:* Query auf `.eq('client_id', user.id)` einschränken (bzw. auf job_collaborators.user_id joinen) und die Entwurfsliste explizit als „meine" kennzeichnen. Wenn Team-Entwürfe gewünscht sind, das als zweite, sichtbar getrennte Liste bauen — nie als implizites Nebenprodukt der RLS.
- *Akzeptanzkriterium:* Ein Org-Admin sieht in der Fortsetzen-Leiste ausschließlich seine eigenen Entwürfe; ein Test mit zwei Nutzern derselben Org belegt das.

**P1.24 · Der neue Intake-Pfad hat keinen manuellen Ausweg — er ist strikt fragiler als der Pfad, den er ersetzen soll** — `BEWIESEN` · Architektur · Aufwand S
- *Beleg:* src/components/dashboard/JobIntakeStudio.tsx:238-243: `startBuild` bricht bei `!job || !job.title` mit Toast ab und setzt stage='input'; alle drei Einstiege (Zeilen 253-267) laufen über jp.parseJobText/parseJobUrl bzw. pdf.uploadAndParsePdf. src/hooks/useJobParsing.ts:58 UND :88 rufen beide `functions.invoke('parse-job-url')`, src/hooks/useJobPdfParsing.ts:54/:84 ruft 'parse-job-pdf'. REST: parse-job-url → 404, parse-job-pdf → 404. Es gibt keinen anderen Weg nach stage='built' außer startBuild und der Draft-Hydrier
- *Problem:* Auch der reine Texteingabe-Weg ('Erzählen Sie uns von der Rolle') geht durch parse-job-url. Fällt die KI aus, gibt es kein Formular, in das der Kunde die Rolle eintippen kann — er landet in einer Schleife aus 'Konnte keine Stelle erkennen'. Der alte Pfad (src/pages/dashboard/CreateJob.tsx) hat dieses Formular.
- *Empfehlung:* Im Studio einen Zustand 'manuell anlegen' einbauen, der stage='built' mit einem leeren BuiltJob betritt (Titel als einziges Pflichtfeld) — ProfileSections und QualityCheck funktionieren bereits ohne KI. Und: erst abschalten, dann ersetzen — solange der alte Pfad live ist, keine weiteren Features exklusiv ins Studio legen.
- *Akzeptanzkriterium:* Bei abgeschalteten Parse-Funktionen lässt sich im Studio eine Stelle bis status='pending_approval' anlegen; ein Klick auf 'manuell' erreicht das Profil ohne einen einzigen Edge-Function-Aufruf.

**P1.25 · Der neue Intake-Pfad lässt das Verifizierungs-Gate fallen, und das Onboarding meldet einen Erfolg, den es nicht gibt** — `BEWIESEN` · Architektur · Aufwand M
- *Beleg:* Gate im alten Pfad: src/pages/dashboard/CreateJob.tsx:103 (useClientVerification) und :589-593 (`if (publish && !canPublishJobs) { ... navigate('/onboarding') }`). `grep -rn useClientVerification src/` findet KEINEN Treffer in JobIntakeStudio.tsx oder NeueStelleBar.tsx. src/pages/onboarding/ClientOnboarding.tsx:99-108 → src/hooks/useClientVerification.ts:148-155 setzt kyc_status='in_review', die UI springt auf Schritt 3 mit 'Verifizierung abgeschlossen!' (ClientOnboarding.tsx:305), während isFullyVerified (useClien
- *Problem:* Drei Ebenen widersprechen sich. (1) Der Nachfolgepfad kennt das Gate nicht — jeder unverifizierte Account kann über das Studio einreichen. (2) Der Kunde bekommt 'Ihr Account ist jetzt vollständig verifiziert' für einen Zustand, der auf 'in_review' steht; der Redirect in ClientOnboarding.tsx:60-65 feuert nie. (3) canPublishJobs = terms_accepted && contract_signed — beide Flags schreibt der Kunde selbst (useClientVerif
- *Empfehlung:* Das Gate serverseitig ziehen — in den Statuswechsel-Trigger aus Finding 3 integrieren (kein Übergang nach pending_approval ohne terms_accepted UND contract_signed) — und client_verifications für den Kunden nur lesbar machen; Schreiben ausschließlich über eine Edge Function, die Vertragstext und -version protokolliert. Onboarding-Schritt 3 ehrlich formulieren ('Unterlagen eingereicht — Prüfung läuft').
- *Akzeptanzkriterium:* Ein Account ohne unterzeichneten Vertrag bringt über KEINEN der beiden Intake-Pfade und auch nicht per direktem REST-Aufruf einen Job nach pending_approval; das Onboarding zeigt nach dem letzten Schritt 'in Prüfung' statt 'abgeschlossen'.

**P1.26 · Delegation an den Fachbereich erzeugt Benachrichtigungen zu Jobs, die der Empfänger per RLS nicht öffnen kann** — `ABGELEITET` · Architektur · Aufwand S
- *Beleg:* src/components/dashboard/JobIntakeStudio.tsx:435-454 (handleDelegate) speichert den Entwurf und schreibt eine notification an memberUserId — KEIN Insert in job_collaborators. `grep -rn job_collaborators src/` liefert nur src/integrations/supabase/types.ts (generierte Typen), keinen Schreibzugriff. can_access_job (supabase/migrations/20260710130000_client_team_foundation.sql:157-177) verlangt für hiring_manager und viewer zwingend is_job_collaborator. Collaborator-Zeilen entstehen nur über den Creator-Trigger (Migra
- *Problem:* Der Übergabe-Empfänger bekommt 'Jobaufnahme an Sie übergeben' inklusive related_id, aber SELECT auf den Job liefert 0 Zeilen. Auch die Fortsetzen-Chips (src/components/dashboard/NeueStelleBar.tsx:35-49, Query ohne client_id-Filter, rein RLS-getragen) zeigen ihm nichts. Für owner/admin/hr klappt es zufällig, weil deren Zugriff nicht über Collaborator läuft — ausgerechnet die Zielrolle des Features (hiring_manager) ist
- *Empfehlung:* handleDelegate muss vor der Notification einen job_collaborators-Eintrag (job_id, user_id, role='hiring_manager', added_by) schreiben; RLS dafür ergänzen (INSERT erlaubt, wenn can_edit_job(job_id) und der Ziel-User aktives Mitglied derselben Org ist). Parallel in JobTeam (src/components/client/JobDetailSections.tsx:355) eine Aktion 'vorhandenes Mitglied zu dieser Stelle hinzufügen' ergänzen — heute bietet die Kompone
- *Akzeptanzkriterium:* Nach 'an Fachbereich geben' kann der Empfänger den Entwurf über die Benachrichtigung und über die Fortsetzen-Chips öffnen und bearbeiten; ein Viewer kann ihn öffnen, aber nicht einreichen.

**P1.27 · Zwei Intake-Pfade schreiben das Briefing in zwei verschiedene Spalten — der Verbraucher liest nur eine davon** — `BEWIESEN` · Architektur · Aufwand M
- *Beleg:* Alter Pfad: src/pages/dashboard/CreateJob.tsx:648 `intake_briefing: briefingText`. Neuer Pfad: src/components/dashboard/JobIntakeStudio.tsx:347 `briefing_notes: serializeBriefing(answers)` — intake_briefing wird dort nie gesetzt (grep). Verbraucher: supabase/functions/generate-job-summary/index.ts:112 liest ausschließlich `job.intake_briefing || 'Kein Briefing vorhanden'`. REST: generate-job-summary → 404 (nicht deployt), obwohl src/components/admin/JobApprovalDialog.tsx:132-134 es beim Publish aufruft.
- *Problem:* Dasselbe fachliche Objekt — was der Kunde über die Rolle erzählt hat — liegt je nach Erstellungsweg in intake_briefing, briefing_notes oder intake_payload. Der einzige Konsument, der daraus die recruiter-taugliche Fassung baut, kennt nur den alten Ort. Verschärfend: src/components/admin/JobApprovalDialog.tsx:182 missbraucht briefing_notes im Ablehnungs-Fallback als Statusfeld ('[ABGELEHNT] …' wird vorangestellt), und
- *Empfehlung:* Einen kanonischen Speicherort festlegen (intake_payload, sobald die Hybrid-Migration liegt; bis dahin briefing_notes), alle Schreiber darauf ziehen, generate-job-summary darauf umstellen und deployen. Ablehnungsgründe nie in briefing_notes — dafür ist rejection_reason da (Finding 4).
- *Akzeptanzkriterium:* Ein über Studio und ein über CreateJob angelegter Job erzeugen beide eine nicht-leere job_summary; briefing_notes enthält in keinem Datensatz mehr die Zeichenkette '[ABGELEHNT]'.

**P1.28 · 'Später weiter' und 'Entwurf fortsetzen' verlieren produktiv den kompletten Studio-Zustand** — `BEWIESEN` · Architektur · Aufwand S
- *Beleg:* src/components/dashboard/JobIntakeStudio.tsx:392-394 legt draft_state ausschließlich innerhalb von intake_payload ab; REST: jobs.intake_payload → 400/42703. src/lib/intakeCapture.ts:45-57: bei fehlender Spalte wird der erweiterte Insert verworfen und nur `base` geschrieben (Warnung nur in der Konsole, Zeile 53). Beim Wiederaufnehmen liest JobIntakeStudio.tsx:289 `r.intake_payload?.draft_state` → undefined → Fallback auf die flachen Spalten (292-301).
- *Problem:* Alles, was das Studio über die Basisspalten hinaus erhebt — dynamische KI-Antworten (dyn), Flexibilitätsmatrix, Contracting-Konditionen, Reveal-Deskriptor und Reveal-Trigger, Skill-Requirement-Gewichte — existiert nur in intake_payload. In Produktion wird es beim Speichern verworfen und beim Fortsetzen durch Defaults ersetzt (revealSetup fällt auf 'after_first_interview', freelance auf EMPTY_FREELANCE zurück).
- *Empfehlung:* Bis die Hybrid-Migration liegt: draft_state in einer Spalte ablegen, die existiert — eine additive Migration für jobs.draft_state jsonb ist eine Zeile. Grundsätzlich muss intakeCapture einen Verlust sichtbar machen (Rueckgabe extendedPersisted wird heute von keinem Aufrufer ausgewertet), statt ihn nur in die Konsole zu schreiben.
- *Akzeptanzkriterium:* Ein Entwurf mit beantwortetem Briefing, gesetzter Flexibilität und geändertem Reveal-Trigger lässt sich schließen und wieder öffnen, ohne dass einer dieser Werte verloren geht.

**P1.29 · Der Akquise-Link, auf dem der gesamte Ziel-Flow aufbaut, existiert nirgends im Code** — `BEWIESEN` · RevOps · Aufwand L
- *Beleg:* grep -rnE "acquisition|akquise|referral|invite_token|tracking_link|utm_|short_link|/onboarding|signup_link" ueber src/components/outreach/, src/pages/admin/AdminOutreach.tsx, supabase/functions/generate-outreach-email/ und supabase/functions/process-outreach-queue/ liefert null Treffer. Ergaenzend: grep nach "matchunt|https?://|link|url" in generate-outreach-email/index.ts trifft nur die Deno-Imports (Zeile 1-2) und den Lovable-AI-Gateway-Endpunkt (Zeile 483). Der vorgeschriebene CTA laut gespeichertem generation_p
- *Problem:* Die Kette Verkaeufer -> Akquise-Link -> Kontakt -> Unternehmen -> Deal -> abgeschlossene Jobaufnahme bricht am ersten Glied. Das heutige Modul erzeugt Text, der um einen Rueckruf bittet. Damit gibt es keinen klickbaren Uebergang von der Kaltansprache in das Produkt, keinen Traeger fuer eine Attribution und keinen Zeitstempel, an dem ein Funnel-Schritt festgemacht werden koennte. Verschaerfend kommt hinzu, dass es lau
- *Empfehlung:* Eine Tabelle acquisition_links als alleinigen Traeger der Attribution einfuehren: id, token (unguessable, mindestens 128 Bit), owner_user_id als Verkaeufer, lead_id, company_id, campaign_id, sequence_step, purpose, expires_at, revoked_at, created_at, first_opened_at, converted_at. Die oeffentliche Route /a/:token reiht sich in die bestehenden Token-Routen (interview/select, offer/view, invite/:token, reference/:token
- *Akzeptanzkriterium:* Ein Verkaeufer erzeugt einen Link, oeffnet ihn im anonymen Browserfenster und landet ohne Login in der Jobaufnahme. In acquisition_links sind owner_user_id und first_opened_at gesetzt. Der Token ist nicht erratbar und laeuft nach Ablauf von expires_at mit HTTP 410 aus.

**P1.30 · AdminOutreach.tsx mit 39,6 KB ist toter Code; die produktiv erreichbare Oberflaeche ist nur ein Bruchteil des Moduls** — `BEWIESEN` · RevOps · Aufwand M
- *Beleg:* grep -rn "AdminOutreach" ueber src ohne Selbstreferenz liefert null Treffer. src/App.tsx:71 importiert ausschliesslich OutreachSlim; die Routen /admin/outreach (App.tsx:434-436) und /admin/outreach/company/:companyId (App.tsx:439) zeigen auf OutreachSlim. OutreachSlim.tsx umfasst 3122 Byte und bindet genau vier Views ein: CompanyListView, ContactListView, EmailWorkflow, InboxView. Zum Vergleich unerreichbar bleiben unter anderem SmartImportDialog.tsx (42 KB), LeadImportDialog.tsx (39 KB), LeadProfileDialog.tsx (26 
- *Problem:* Es existieren zwei Generationen des Moduls parallel: eine breite Admin-Oberflaeche mit Kampagnen, Sequenzen und Massenimport, und eine schlanke Vier-Tab-Ansicht, die sie abgeloest hat, ohne dass die alte entfernt wurde. Das ist dasselbe Muster wie bei der Jobaufnahme mit CreateJob.tsx gegen JobIntakeStudio.tsx. Da nur OutreachSlim geroutet ist, sind Kampagnenanlage und Sequenzsteuerung fuer Bediener faktisch nicht er
- *Empfehlung:* Erst entscheiden, dann aufraeumen. Empfehlung: OutreachSlim als kanonische Oberflaeche bestaetigen und AdminOutreach.tsx samt der ausschliesslich dort verwendeten Dialoge loeschen; ein einziger Kampagnen-Dialog wird nach OutreachSlim uebernommen, weil ohne campaign_id keine Kampagnen-Attribution moeglich ist. Vor dem Loeschen pro Komponente pruefen, welche Faehigkeit verloren geht, insbesondere beim Massenimport. Wic
- *Akzeptanzkriterium:* Genau eine Outreach-Oberflaeche ist geroutet. Ein Bediener kann darin eine Kampagne mit sender_email und sequence_steps anlegen. Ein grep nach nicht importierten Komponenten unter src/components/outreach/ liefert keine verwaisten Dateien mehr.

**P1.31 · Keine Verkaeufer-Attribution: outreach_leads besitzt kein Eigentuemer-Feld** — `BEWIESEN` · RevOps · Aufwand M
- *Beleg:* Vollstaendige Spaltenliste von outreach_leads aus src/integrations/supabase/types.ts (rund 90 Spalten, darunter created_by, lead_source, list_name, score, engagement_score, priority, segment, status, outreach_status). Es gibt kein owner_id, kein assigned_to, kein seller_id und kein account_executive. created_by ist die einzige Personenreferenz und dokumentiert den Importierenden, nicht den zustaendigen Verkaeufer.
- *Problem:* Wer einen Lead importiert, ist selten dieselbe Person, die ihn bearbeitet und abschliesst, insbesondere bei einem Massenimport per SmartImportDialog. Damit fehlt die Dimension, ohne die keine der geforderten Auswertungen funktioniert: keine Leistungsbetrachtung pro Verkaeufer, keine Gebietsaufteilung, keine Provisionsgrundlage und keine Wiedervorlagen-Zuordnung. Auch eine spaetere Zuordnung ueber die Kampagne hilft n
- *Empfehlung:* owner_user_id (UUID, Referenz auf auth.users) auf outreach_leads und auf outreach_companies ergaenzen, dazu owner_assigned_at und owner_previous_id fuer eine nachvollziehbare Uebergabe. Den Eigentuemer beim Import setzbar machen und in der Kontaktliste sichtbar filtern. Die Zuordnung gehoert bewusst an den Lead und die Firma, nicht an den Akquise-Link, weil ein Lead ueber mehrere Links und Kampagnen hinweg denselben 
- *Akzeptanzkriterium:* Jeder neu importierte Lead hat ein gesetztes owner_user_id. Die Kontaktliste laesst sich auf Meine Leads filtern. Eine Auswertung liefert Anzahl Leads, versendete Mails und Deals gruppiert nach Eigentuemer.

**P1.32 · Der Konversionszeitpunkt wird nirgends geschrieben: converted_at ist ein totes Feld** — `BEWIESEN` · RevOps · Aufwand L
- *Beleg:* outreach_leads.converted_at existiert (types.ts:6064, 6109, 6154). Ein grep nach converted_at und den Literalen 'converted' ueber src und supabase/functions findet ausserhalb der generierten types.ts nur lesende Verwendungen: BulkGenerateDialog.tsx:31 (Ausschlussfilter), LeadDetailSheet.tsx:206 (Badge-Farbe), AdminOutreach.tsx:110 und :433 (Badge und Auswahlfeld, beide in der nicht gerouteten Datei). Kein einziger Schreibzugriff, weder im Frontend noch in einer Edge Function. Zusaetzlich gibt es keinen Fremdschlues
- *Problem:* Die Konversion ist der Uebergang von CRM zu Produkt, und genau dieser Uebergang ist im Datenmodell nicht abgebildet. Selbst wenn ein angesprochenes Unternehmen sich registriert und eine Stelle einstellt, gibt es keine Verbindung zwischen dem outreach_lead und dem entstandenen company_profile oder job. Der einzige Weg, converted_at zu setzen, waere ein manuelles Auswahlfeld in einer Datei, die nicht geroutet ist.
- *Empfehlung:* Die Verknuepfung an genau einer Stelle herstellen und dort strikt halten: eine Tabelle acquisition_conversions mit lead_id, acquisition_link_id, company_profile_id, first_job_id, converted_at, attribution_model. Diese Tabelle ist die einzige erlaubte Bruecke zwischen CRM-Domaene (outreach_*) und Produkt-Domaene (company_profiles, jobs). Keine Fremdschluessel von outreach_leads direkt auf jobs und keine CRM-Spalten in
- *Akzeptanzkriterium:* Eine Abfrage beantwortet ohne manuelle Nacharbeit: Welche veroeffentlichten Stellen gehen auf welchen Lead, welchen Link und welchen Verkaeufer zurueck. Ein ueber einen Akquise-Link registriertes Testunternehmen erzeugt automatisch genau eine Zeile in acquisition_conversions.

**P1.33 · Sequenzen und Follow-ups sind modelliert, laufen aber nie: es existiert kein Scheduler** — `BEWIESEN` · RevOps · Aufwand M
- *Beleg:* outreach_campaigns.sequence_steps hat den Vorgabewert '[{"step":1,"day":0,"type":"initial"},{"step":2,"day":3,"type":"followup_1"},{"step":3,"day":7,"type":"followup_2"}]' (Migration 20251216022724:87). Die Tabelle outreach_sequences (ebenda:247-261) fuehrt current_step, next_email_at, status und pause_reason. Ein grep nach cron, pg_cron oder schedule in allen Migrationen mit Bezug zu outreach oder queue trifft ausschliesslich den Index idx_outreach_queue_pending (Zeile 276). Es gibt keinen pg_cron-Job und keinen a
- *Problem:* Die Warteschlange ist ein Ziehmodell ohne Zieher. process-outreach-queue muesste periodisch aufgerufen werden, wird es aber weder durch pg_cron noch durch einen externen Dienst. Ebenso wenig gibt es Code, der nach einem Versand next_email_at auf den naechsten Sequenzschritt setzt. Faktisch existiert nur der manuelle Erstkontakt, und auch der nur, wenn jemand die Funktion von Hand ausloest.
- *Empfehlung:* Nach dem Deployment einen pg_cron-Job einrichten, der process-outreach-queue in kurzem Takt aufruft, mit strikter Obergrenze pro Lauf und Beachtung von outreach_rate_limits. Den Sequenzfortschritt serverseitig in derselben Funktion setzen, unmittelbar nach erfolgreichem Versand, und bei einer eingehenden Antwort die Sequenz automatisch pausieren (status = paused, pause_reason = replied), was outreach_reply_classifica
- *Akzeptanzkriterium:* Ein Testlead durchlaeuft Schritt 1 bis 3 im konfigurierten Abstand ohne manuellen Eingriff. Eine simulierte Antwort setzt die Sequenz auf paused. Es steht kein Eintrag laenger als eine Stunde im Status pending, ohne dass eine Meldung ausgeloest wird.

**P1.34 · DSGVO: Tracking-Pixel ohne Einwilligung und Widerspruch nur als Fliesstext statt als Ein-Klick-Abmeldung** — `BEWIESEN` · RevOps · Aufwand M
- *Beleg:* supabase/functions/process-outreach-queue/index.ts:276 fuegt bedingungslos ein `<img src="${baseUrl}/functions/v1/track-outreach-engagement?type=open&eid=${email.id}" width="1" height="1" style="display:none;" />` in jede Mail ein; die Einbindung erfolgt in Zeile 298 ohne jede Pruefung auf eine Einwilligung. Der Widerspruchshinweis lautet woertlich "Wenn das nicht passt, kurze Antwort mit \"Stop\" genuegt." und ist als `<p class="optout">` mit font-size 11px und Farbe #999 formatiert. Im gesamten Versandpfad gibt e
- *Problem:* Drei getrennte Rechtsfragen ueberlagern sich. Erstens setzt das Zaehlpixel Informationen auf dem Endgeraet beziehungsweise liest sie aus und beruehrt damit die Einwilligungspflicht nach TDDDG; die deutschen Aufsichtsbehoerden bewerten Oeffnungsmessung ohne Einwilligung ueberwiegend als unzulaessig. Zweitens verlangt eine Abmeldung praktisch einen Antwortvorgang, was die Huerde deutlich ueber das nach Art. 21 Abs. 3 D
- *Empfehlung:* Vor dem ersten Versand drei Dinge umsetzen. Erstens das Zaehlpixel standardmaessig abschalten und nur nach dokumentierter Einwilligung aktivieren; die Oeffnungsrate ist als Steuerungsgroesse ohnehin unzuverlaessig geworden und laesst sich durch Antwortquote und Link-Klick ersetzen. Zweitens einen echten Abmeldelink mit eigenem Token ergaenzen, der ohne weitere Rueckfrage in outreach_suppression_list eintraegt, plus d
- *Akzeptanzkriterium:* Eine Testmail enthaelt kein Zaehlpixel, sofern keine Einwilligung vorliegt. Sie traegt den Header List-Unsubscribe. Ein Klick auf den Abmeldelink erzeugt ohne weitere Bestaetigung einen Eintrag in outreach_suppression_list, und ein anschliessender Versandversuch an dieselbe Adresse endet mit dem Sta

**P1.35 · platform_events kann anonyme Akquise-Ereignisse strukturell nicht aufnehmen, weil user_id NOT NULL ist** — `BEWIESEN` · RevOps · Aufwand M
- *Beleg:* supabase/migrations/20251204204702_2964446c-9063-4045-bdf7-cec2029a93cd.sql:59-66: `CREATE TABLE public.platform_events (... event_type TEXT NOT NULL, ... user_id UUID NOT NULL ...)`. RLS ebenda:86-95: "System can insert events", "Users can view own events" mit `auth.uid() = user_id`, "Admins can view all events". Die Tabelle wird bereits produktiv beschrieben, unter anderem aus src/hooks/useActionSession.ts:65 und :224 sowie aus schedule-interview, track-candidate-engagement und fraud-detection. REST-Probe mit ano
- *Problem:* platform_events waere das naheliegende Fundament fuer das geforderte Event-Schema, weil es existiert, produktiv genutzt wird und mit entity_type, entity_id, metadata, session_id und response_time_seconds bereits die richtigen Bausteine hat. Genau die Haelfte der geforderten Ereignisse entsteht aber, bevor ein Nutzerkonto existiert: Link geoeffnet, Intake gestartet, erste Frage beantwortet, Import fehlgeschlagen, Abbr
- *Empfehlung:* Eine eigene Tabelle acquisition_events fuehren statt platform_events aufzuweichen, weil die Zugriffsmodelle sich grundlegend unterscheiden: platform_events gehoert einem Nutzer, ein Akquise-Ereignis gehoert einem Link. Schema: id, event_name, occurred_at, acquisition_link_id, lead_id, company_id, owner_user_id, anonymous_id, user_id (nullable, wird beim Uebergang nachgetragen), session_id, properties JSONB, schema_ve
- *Akzeptanzkriterium:* Ein anonymer Linkaufruf erzeugt eine Zeile in acquisition_events mit gesetztem anonymous_id und leerem user_id. Nach der Registrierung tragen alle Ereignisse derselben Sitzung dieselbe user_id. Mit anon-Key ist die Tabelle nicht lesbar.

**P1.36 · Fehlende Grenze zwischen CRM- und Produktdaten: die Anreicherungsfelder liegen bereits im falschen Objekt** — `BEWIESEN` · RevOps · Aufwand L
- *Beleg:* outreach_leads vermengt drei Ebenen in einer Tabelle: Kontaktperson (contact_name, contact_email, contact_role, first_name, last_name, seniority, department, mobile_phone, direct_phone, office_phone, personal_linkedin_url, education), Unternehmen (company_name, company_domain, company_headcount, company_technologies, company_industries, hq_address_line, hq_city, revenue_range, founding_year) und Vertriebszustand (status, outreach_status, score, engagement_score, priority, last_contacted_at, converted_at). Parallel 
- *Problem:* Es gibt heute mindestens vier Orte fuer Unternehmensdaten: outreach_leads mit rund 30 Firmenspalten, outreach_companies, company_profiles und company_intelligence. Welcher davon fuehrend ist, ist nicht definiert. Ein Firmenname, der im CRM korrigiert wird, erreicht das Produkt nicht und umgekehrt. Fuer Matchunt ist das besonders heikel, weil Firmendaten hier nicht neutral sind: Der Kunde ist gegenueber Recruitern bis
- *Empfehlung:* Die Grenze explizit festlegen und technisch erzwingen. CRM-Domaene: outreach_companies als einziger fuehrender Ort fuer Interessenten-Firmendaten, outreach_leads reduziert auf die Kontaktperson mit company_id als Verweis, alle Firmenspalten dort schrittweise ausgebaut. Produkt-Domaene: company_profiles als einziger fuehrender Ort fuer Kundendaten. Einzige erlaubte Verbindung ist die Tabelle acquisition_conversions au
- *Akzeptanzkriterium:* Jede Firmeninformation hat genau einen definierten fuehrenden Ort, dokumentiert. Keine Recruiter-sichtbare View liest aus einer outreach_-Tabelle. Ein CRM-seitiger Firmenname erscheint in keiner Ansicht, in der der Kunde anonym bleiben muss.

**P1.37 · DARK PATTERN: Weiss-ich-nicht zaehlt als halbe Antwort und oeffnet das Einreichungs-Gate** — `BEWIESEN` · Red Team · Aufwand S
- *Beleg:* src/components/dashboard/IntakeBriefing.tsx:300-304: addressed += a.unknown ? q.weight * 0.5 : q.weight. Der Prozentwert wird JobIntakeStudio.tsx:118-119 zu progressPct und :352 als intake_completeness persistiert. Gate: src/pages/dashboard/ClientJobDetail.tsx:299 reife = raw.intake_completeness ?? 0 und :304 canSubmit = missing.length === 0 && reife >= 30.
- *Problem:* Wer jede Frage mit 'Weiss ich nicht' beantwortet, erreicht rechnerisch 50 Prozent und liegt damit weit ueber der 30-Prozent-Schwelle. Der Balken misst Interaktion, nicht Information, und ist trotzdem als Qualitaetszahl beschriftet (Briefing-Reife, ClientJobDetail.tsx:511 reife/100).
- *Empfehlung:* unknown mit 0 gewichten und stattdessen separat ausweisen (z. B. '12 von 30 Fragen beantwortet, 6 bewusst offen'). Das Gate nicht auf einen Prozentwert legen, sondern auf eine Pflichtliste (Muss-Kriterien, Verguetung, Standort/Modell, Reveal-Descriptor) - vier boolesche Bedingungen sind ehrlicher und nicht manipulierbar.
- *Akzeptanzkriterium:* Ein Briefing, in dem jede Frage mit Weiss-ich-nicht beantwortet wurde, erreicht 0 Prozent und laesst sich nicht einreichen.

**P1.38 · DARK PATTERN: der Erfolgsschirm zeigt einen ewig drehenden Spinner und zwei erfundene Zeitzusagen** — `BEWIESEN` · Red Team · Aufwand S
- *Beleg:* src/components/dashboard/JobIntakeStudio.tsx:663-675 setzt fest { label: 'Pruefung durch Matchunt', hint: 'ca. 4 Std', active: true } und { label: 'Erste Kandidaten', hint: 'Oe 3-5 Tage' }; :677-681 rendert fuer active einen Loader2 mit animate-spin, sonst einen leeren Kreis. Die Werte sind Literale, es gibt keinen Datenzugriff in diesem Block. Ergaenzend src/components/dashboard/NeueStelleBar.tsx:132: 'Fertiges Profil in Sekunden' - waehrend parse-job-url, parse-job-pdf und intake-questions produktiv 404 liefern.
- *Problem:* Der Spinner suggeriert einen laufenden Vorgang; es laeuft nichts, er dreht sich, solange der Dialog offen ist. 'ca. 4 Std' und 'Oe 3-5 Tage' sind keine Messwerte, sondern Text - es gibt keine Tabelle, aus der sie stammen koennten, und keinen Ort, an dem sie gegen die Realitaet geprueft werden.
- *Empfehlung:* Spinner durch ein statisches Haekchen-/Punkt-Symbol ersetzen (der Schritt ist erledigt, nicht laufend). Zeitangaben entweder aus echten Daten ableiten (Median approved_at minus created_at der letzten 30 Jobs) oder ersatzlos streichen. Bis die Parse-Funktionen deployed sind, 'Fertiges Profil in Sekunden' aus NeueStelleBar entfernen.
- *Akzeptanzkriterium:* Auf dem Erfolgsschirm dreht sich nichts; jede angezeigte Zeitangabe hat eine benennbare Datenquelle oder steht nicht da.

**P1.39 · KI-THEATER: die Punktzahlen im Qualitaets-Check sind erfunden und die angezeigte Briefing-Reife existiert in zwei widersprechenden Fassungen** — `BEWIESEN` · Red Team · Aufwand S
- *Beleg:* src/components/dashboard/intake/QualityCheck.tsx: score = round(progressPct*0.6 + profilePts*0.4 - warnings.length*5). Gehaltsband setzt profilePts += 25, also +10 auf den Score - der Hebeltext sagt 'Gehaltsband angeben: +6 P'. revealDescriptor setzt profilePts += 20, also +8 - der Text sagt 'Anonymen Firmen-Descriptor setzen: +4 P'. Zweite Fassung: JobIntakeStudio.tsx:352 persistiert intake_completeness = progressPct (NICHT score); ClientJobDetail.tsx:299/511 zeigt genau diesen Wert unter derselben Ueberschrift Br
- *Problem:* Die Hebel behaupten eine Praezision, die die Formel nicht hergibt - zwei von drei Zahlen sind um Faktor 1,7 bis 2 daneben. Und derselbe Kunde sieht im Studio eine Zahl (score) und auf der Job-Seite eine andere (progressPct), beide beschriftet Briefing-Reife.
- *Empfehlung:* Die Hebelbetraege aus derselben Funktion berechnen, die den Score bildet (Delta-Simulation statt Literal), oder die Punktzahlen weglassen und nur die Reihenfolge zeigen. Eine Score-Funktion in einer gemeinsamen Datei, von Studio und ClientJobDetail identisch aufgerufen, und genau dieser Wert nach intake_completeness.
- *Akzeptanzkriterium:* Studio und Job-Detailseite zeigen fuer denselben Job dieselbe Zahl; ein genannter Hebel erhoeht den Score nach Umsetzung um exakt den genannten Betrag.

**P1.40 · UNREALISTISCHE AUTOMATISIERUNG: eine vom LLM selbst vergebene Note ist das harte Einreichungs-Gate** — `BEWIESEN` · Red Team · Aufwand M
- *Beleg:* supabase/functions/intake-questions/index.ts:71 definiert weighted_completeness als integer im Antwortschema, :139 als required; :246 uebernimmt es ungeprueft (nur clamp 0-100). Regel :177: 'FERTIG IST FERTIG: weighted_completeness >= 85 und keine Pflichtluecken -> next_questions = []'. Frontend: src/components/dashboard/intake/DynamicBriefing.tsx:106 completeness = data.weighted_completeness; JobIntakeStudio.tsx:119 progressPct = dyn.available ? dyn.completeness : staticProg.pct; :352 Persistenz als intake_complet
- *Problem:* Das Modell entscheidet selbst, wann sein eigenes Interview vollstaendig ist, und diese Selbstbenotung ist gleichzeitig die einzige Kennzahl, die persistiert wird, und die Bedingung fuer den Uebergang in die Matchunt-Pruefung. Es gibt keine Gegenprobe gegen eine Pflichtfeldliste. Weil der Aufruf zustandslos ist, kann derselbe Antwortstand bei zwei Aufrufen zwei verschiedene Noten bekommen - der Balken kann fallen.
- *Empfehlung:* weighted_completeness als Beratungssignal behalten, aber nicht persistieren und nicht als Gate verwenden. intake_completeness deterministisch aus einer Pflichtfeldliste im Frontend berechnen (Muss-Kriterien, Verguetung, Standort/Modell, Reveal-Descriptor, K.-o.-Kriterien) - dieselbe Funktion fuer beide Briefing-Modi. Zusaetzlich Monotonie erzwingen: der angezeigte Wert darf innerhalb einer Sitzung nie fallen.
- *Akzeptanzkriterium:* Zwei aufeinanderfolgende Aufrufe mit identischem Antwortstand ergeben denselben angezeigten Reifegrad; das Einreichungs-Gate haengt an keiner Modellausgabe.

**P1.41 · Der AGG-Waechter schaltet sich genau dann ab, wenn die KI-Befragung funktioniert** — `BEWIESEN` · Red Team · Aufwand S
- *Beleg:* src/components/dashboard/intake/QualityCheck.tsx prueft AGG_PATTERN ausschliesslich gegen answerText(answers.exclusion_criteria) - eine einzelne Frage-ID aus dem STATISCHEN Katalog. Der Studio uebergibt QualityCheck answers={answers} (JobIntakeStudio.tsx:639), waehrend die dynamischen Antworten in dyn liegen (:626-633, fallback-Prop). Weder built.must_haves noch built.description/requirements werden geprueft. Der Sonderfall ist bereits sichtbar: :641 openQuestions={dyn.available ? [] : openBriefingQuestions(...)}.
- *Problem:* Sobald intake-questions deployed ist (dyn.available = true), landen alle Antworten in dyn.answers, und answers.exclusion_criteria bleibt leer. Der Regex laeuft weiter, findet aber nie etwas. Ausserdem ist das Freitextfeld mit dem hoechsten Risiko - must_haves und requirements - nie im Pruefumfang.
- *Empfehlung:* Den Regex ueber die gesamte einzureichende Nutzlast laufen lassen: must_haves, nice_to_haves, description, requirements, serializeBriefing(answers) und dyn.answers - eine Zeile Textkonkatenation statt einer Feld-ID. Treffer nicht nur als Warnung, sondern als expliziter Bestaetigungsdialog vor der Uebergabe.
- *Akzeptanzkriterium:* Ein AGG-verdaechtiger Begriff in must_haves oder in einer dynamischen Briefing-Antwort erzeugt eine Warnung; kein Einreichen ohne bewusste Bestaetigung.

**P1.42 · Widerspruch a11y gegen datenmodell zum stillen Speicherverlust - datenmodells Fassung wuerde den Studio komplett unbrauchbar machen** — `ABGELEITET` · Red Team · Aufwand S
- *Beleg:* frontend-a11y (Widerspruch 2 und Finding 2a) empfiehlt, extendedPersisted auszuwerten und bei false statt toast.success ein toast.warning zu zeigen. datenmodell (Finding 3) empfiehlt: 'bei fehlenden Spalten muss der Save mit sichtbarem Fehler abbrechen statt Erfolg zu melden (intakeCapture.ts:53 console.warn -> harter Fehler + Toast)'. Fakt: JobIntakeStudio.tsx:368-395 setzt intake_payload IMMER (source, captured_at, contract_type sind unbedingt), also ist cleanExtended in src/lib/intakeCapture.ts:41-45 nie leer, a
- *Problem:* Ein harter Abbruch bei fehlenden Extended-Spalten bedeutet in der heutigen Produktion: 100 Prozent aller Speicher- und Einreichvorgaenge scheitern. datenmodell hat den Ausloeser richtig beschrieben, aber nicht geprueft, wie oft er feuert.
- *Empfehlung:* Entscheidung: a11y-Fassung umsetzen (ehrlicher Warn-Toast plus deaktiviertes Spaeter-weiter-Versprechen), datenmodells harten Abbruch verwerfen. Der harte Abbruch ist erst zulaessig, NACHDEM die Spalte existiert - dann ist er allerdings sinnvoll, weil ein Fehlschlag dann echt ist.
- *Akzeptanzkriterium:* Bei fehlender Spalte speichert der Studio weiterhin die Stammdaten und meldet den Teilverlust sichtbar; kein Vorgang bricht komplett ab.
---

<a id="d2"></a>

## D.2 · Streichen, automatisch befuellen, spaeter fragen

**146 Streich-Empfehlungen** aus 8 Fachkatalogen, 59 unterschiedliche Fragen betroffen.

### Von mehreren Agenten unabhaengig zum Streichen empfohlen (starkes Signal)

| Frage-ID | Nennungen | Begruendungen |
|---|---|---|
| `CJ-27` | 8x | "Wie viele Personen entscheiden final?" — abstrakt, lädt zur sozial erwünschten Antwort "Nur ich" ein, und BEWIESEN geht die Antwort verloren (formData.decision_makers_count wird in handleSubmit Creat <br>— Antwort wird in handleSubmit nie verwendet (CreateJob.tsx:619-668) — reiner Datenverlust. Inhaltlich doppelt zu IB-29. Beides ersetzt durch UE-15 mit typisiertem Ziel. <br>— 'Wie viele Personen entscheiden final?' hat BEWIESEN kein Zielfeld: formData.decision_makers_count wird in CreateJob.tsx handleSubmit (619-668) nirgends verwendet, die Antwort geht beim Speichern verl |
| `CJ-30` | 8x | "Home Office Tage pro Woche?" — BEWIESEN wird formData.remote_days beim Speichern nie geschrieben, und die Angabe widerspricht CJ-17 (onsite_days_required) ohne jeden Abgleich. Drei Felder für dieselb <br>— formData.remote_days wird nirgends geschrieben (weder Spalte noch intake_payload) und widerspricht CJ-17 ohne Abgleich. Zwei Erhebungen desselben Sachverhalts, eine davon folgenlos. <br>— 'Home Office Tage pro Woche' geht BEWIESEN verloren (formData.remote_days wird in handleSubmit nie geschrieben) und widerspricht CJ-17 (onsite_days_required) ohne jeden Abgleich. Ersetzen durch IT-11, |
| `CJ-22` | 7x | "Hiring-Dringlichkeit" — BEWIESEN produktiv unerreichbar (Akkordeon rendert nur bei enrichmentData/industry/company_size_band/funding_stage, alle Quellen hängen an Edge Functions mit HTTP 404) und der <br>— Im PROD-Bundle nie sichtbar (Akkordeon-Bedingung CreateJob.tsx:1448 hängt an toten Edge Functions) und Wertekonflikt zu CJ-26 ('ASAP' gegen 'hot'). CJ-26 gewinnt, CJ-22 entfällt. <br>— 'Hiring-Dringlichkeit' im Akkordeon ist in PROD unerreichbar (Sichtbarkeit haengt an enrichmentData/industry/company_size_band/funding_stage, deren Quellen alle HTTP 404 liefern) und kollidiert im Wer |
| `CS-01` | 6x | "Logo hochladen (Coming soon)" — Button permanent deaktiviert, kein onClick, kein Zielfeld (BEWIESEN ClientSettings.tsx:232-238). Ein sichtbares nicht funktionierendes Element beschädigt die Glaubwürd <br>— Button permanent deaktiviert, kein onClick, company_profiles.logo_url wird nie gesetzt. Erweckt den Eindruck einer Eingabe, die es nicht gibt. <br>— 'Logo hochladen (Coming soon)' ist ein dauerhaft deaktivierter Button ohne onClick und ohne Zielfeld. Er erzeugt beim Kunden den Eindruck eines unfertigen Produkts, ohne je eine Entscheidung zu verbes |
| `CS-13` | 6x | "Team-Verwaltung kommt bald" — Platzhalter ohne Eingabefeld, während JobIntakeStudio.tsx:710-729 gleichzeitig auf "Einstellungen → Team" verweist. Ein Verweis auf ein nicht existierendes Ziel. Entfern <br>— Platzhalter ohne Eingabefeld, während JobIntakeStudio.tsx:710-729 aktiv auf 'Einstellungen → Team' verweist. Entweder bauen oder den Verweis entfernen — als Frage hat es keinen Platz. <br>— 'Team-Mitglieder (kommt bald)' ist ein Platzhalter ohne Eingabefeld und widerspricht aktiv JobIntakeStudio.tsx:710-729, das den Kunden auf 'Einstellungen -> Team' verweist. Entfernen oder den Verweis  |
| `CS-14` | 6x | company_awards — kein Eingabefeld im UI, wird aber bei jedem Speichern als null mitgeschrieben (BEWIESEN ClientSettings.tsx:48/141/164). Toter Ballast. <br>— company_awards wird bei jedem Speichern mitgeschrieben, aber von keinem Feld befüllt. Toter Payload-Schlüssel. <br>— company_awards wird bei jedem Speichern mitgeschrieben, ist aber BEWIESEN immer null, weil kein Formularfeld es fuellt. Entweder Feld ergaenzen oder aus dem Save-Payload entfernen; als Frage existiert |
| `IB-17` | 6x | "Tagessatz-Spanne" als Chips — Dublette zu PS-08/PS-09 (Tagessatz von/bis als Zahl). Zwei Erhebungen derselben Zahl in zwei Formaten, kein Abgleich. Die typisierte Zahlvariante behalten. <br>— Tagessatz-Spanne als Chips doppelt zu PS-08/PS-09 (Zahlenfelder). Bandbreite schlägt Chips beim Matching; die Chip-Variante entfällt. <br>— 'In welcher Tagessatz-Spanne bewegen wir uns' (Chips) doppelt PS-08/PS-09 (Tagessatz von/bis als Zahl). Beide landen heute nicht typisiert. Die Chips-Variante streichen, die Zahlenfelder behalten — si |
| `IB-23` | 6x | "Welche Auslastung benötigen Sie?" — Dublette zu PS-11 ("Auslastung"). <br>— Auslastung doppelt zu PS-11 (Tage pro Woche, typisiert). Die typisierte Variante bleibt. <br>— 'Welche Auslastung benoetigen Sie' doppelt PS-11 ('Auslastung', Select). Chips-Variante streichen. |
| `IB-07` | 5x | "Erste 30 Tage" (Gewicht 1) — bildet mit IB-08 (90 Tage) und IB-09 (12 Monate) drei Fragen auf derselben Zeitachse. Für Sourcing-Entscheidungen genügt der schärfste Punkt. IB-09 behalten, IB-07 streic <br>— Dritte Zeithorizont-Frage neben IB-05 (3 Monate) und IB-08 (90 Tage). Drei Fragen für einen Sachverhalt; UE-03 deckt ihn mit einer ab. <br>— 'Erste 30 Tage' liegt zwischen IB-05 (erste 3 Monate) und IB-08 (nach 90 Tagen) und liefert keine eigenständige Sourcing-Entscheidung. Drei Zeithorizonte sind Fragen-Inflation; 90 Tage und 12 Monate g |
| `IB-22` | 5x | "Wie lang ist das Projekt?" — Dublette zu PS-10 ("Dauer (Monate)"). Gleiches Prinzip: typisiertes Feld behalten, Chip-Variante streichen. <br>— Projektdauer doppelt zu PS-10 (Dauer in Monaten, numerisch). Die numerische Variante bleibt. <br>— 'Wie lang ist das Projekt' doppelt PS-10 ('Dauer (Monate)'). Chips-Variante streichen, Zahlenfeld behalten. |
| `IB-34` | 5x | Vierter Wertesatz für den Reveal-Zeitpunkt neben PS-17 (drei abweichende Werte). Zwei Antworten auf dieselbe Frage, keine davon persistiert. Vereinheitlicht in UE-14. <br>— 'Wann darf Ihre Identitaet freigegeben werden' hat vier Optionen, von denen zwei ('Nach NDA', 'Bei gegenseitigem Interesse') im Datenmodell BEWIESEN kein Gegenstueck haben — PS-17 kennt nur opt_in/aft <br>— Konflikt mit PS-17: vier abweichende Optionen gegen drei typisierte Werte, 'Nach NDA' und 'Bei gegenseitigem Interesse' haben maschinell kein Gegenstück. Den typisierten Select behalten, die Freitextv |
| `IB-29` | 4x | "Wer trifft am Ende die Einstellungsentscheidung?" — inhaltliche Dublette zu CJ-27, ebenfalls nur als Fließtext in briefing_notes. Zwei Fragen zur selben Sache mit unterschiedlichen Optionslisten erze <br>— Doppelung zu CJ-27, landet nur als Fließtext in briefing_notes. Ersetzt durch die Folgefrage 'Wer sagt am Ende ja oder nein?' in UE-15. <br>— Doppelung zu CJ-27 und EX-14, dabei nur vierstufiger Fließtext ohne Gremien- und Freigabeebene. Für Führungsmandate ist genau diese fehlende Ebene (Aufsichtsrat, Gesellschafter, Konzernmutter) der ent |
| `CS-10` | 4x | "Jahresumsatz" als Freitext im Abschnitt "Partner Facts für Recruiter" ("Diese Informationen werden Recruitern angezeigt", ClientSettings.tsx:343-345). Zusammen mit Mitarbeiterzahl, Gründungsjahr und  <br>— Jahresumsatz als Freitext, ausdrücklich für Recruiter sichtbar (ClientSettings.tsx:343-345). Zusammen mit Größe, Gründungsjahr und Alleinstellung identifiziert er die Firma — direkter Widerspruch zum  <br>— Jahresumsatz hat keinen Sourcing-Hebel – kein Kandidat wechselt deswegen – und erhöht zusammen mit Mitarbeiterzahl, Gründungsjahr und Branche das De-Anonymisierungsrisiko im Recruiter-sichtbaren Berei |
| `IB-19` | 4x | Bürotage als Chips, während CJ-17 sie als typisierten Slider erhebt und onsite_days_required füllt. Die Freitextvariante gewinnt nie und erzeugt nur einen zweiten, widersprechenden Wert. <br>— 'Wie viele Tage pro Woche vor Ort (fix)' ist eine Doppelung zu CJ-17 (Slider onsite_days_required) und landet nur in briefing_notes als Fliesstext, waehrend CJ-17 eine typisierte Spalte fuellt. Eine v <br>— Dublette zu CJ-17. Präsenztage sind bereits als jobs.onsite_days_required typisiert vorhanden (REST 200); eine zweite Freitext-/Chip-Erfassung erzeugt nur Widersprüche. |
| `PS-12` | 4x | Verlängerung als Checkbox (binär, Default true) gegen IB-24 (vierstufig, inklusive 'könnte in Festanstellung übergehen'). Die binäre Variante verliert Information und hat einen irreführenden Default. <br>— Binäre Verlängerungs-Checkbox dupliziert IB-24, das vierstufig und damit für die Kandidatenansprache aussagekräftiger ist ('könnte in Festanstellung übergehen' ist ein Wechselargument, 'true' nicht).  <br>— 'Verlaengerung moeglich' als Checkbox mit Default true (types.ts:39) ist doppelt schaedlich: sie doppelt IB-24, sie verliert die vierte Stufe 'koennte in Festanstellung uebergehen' — das staerkste Pit |
| `IB-13` | 4x | Nicht inhaltlich streichen, sondern in dieser Form: Sprache als Chip-Fließtext in briefing_notes ist für das Matching wirkungslos, obwohl der Matcher required_languages als Hard-Kill liest. Ersetzt du <br>— Ersetzt durch S-04. IB-13 speichert einen harten K.O. als Fließtext in briefing_notes und bedient das live existierende jobs.required_languages nicht – der Filter bleibt wirkungslos, obwohl der Matche <br>— Ersetzt durch EX-12. KORREKTUR zur Inventar-Begründung: Die Behauptung 'KEINE typisierte Sprachspalte, kein required_languages' ist falsch — jobs.required_languages existiert in PROD (REST 200), wird  |
| `CJ-21` | 3x | "Funding-Stage" — BEWIESEN in PROD nie sichtbar. Für den DACH-Mittelstand, der die Mehrheit der Kunden stellt, zudem meist bedeutungslos. Nur bei Startups anbieten, sonst gar nicht. <br>— 'Funding-Stage' ist in PROD unerreichbar (gleiche Sichtbarkeitsfalle wie CJ-19) und verbessert fuer IT-Rollen keine der zulaessigen Entscheidungen: sie beeinflusst weder Matching noch Ausschluss. Fuer <br>— Funding-Stage ist für die überwiegende Mehrheit der DACH-Mandate ohne Entscheidungswert und wirkt in Kombination mit Branche, Größe und Region de-anonymisierend. Zudem BEWIESEN produktiv unerreichbar  |
| `CJ-23` | 3x | "Tech-Stack (normalisiert)" — reine Anzeige ohne Eingabefeld, befüllbar nur durch enrich-job-data, BEWIESEN HTTP 404. Der Block rendert nie, die Spalte bleibt immer null. <br>— Keine Frage, sondern eine reine Anzeige, die ausschließlich enrich-job-data befüllt — Function in PROD 404, Spalte bleibt dauerhaft null. <br>— Reines Anzeigefeld ohne Eingabe; BEWIESEN nur über enrich-job-data befüllbar, die in PROD 404 liefert. jobs.tech_environment bleibt dauerhaft null. Tech-Stack gehört als Skill-Anforderung in K03/K04. |
| `CJ-15` | 3x | Zweites Feld für dieselbe Aussage wie CJ-08 (remote_type vs. remote_policy, unterschiedliche Wertelisten, kein Abgleich). remote_policy wird künftig aus remote_type plus onsite_days_required abgeleite <br>— remote_policy dupliziert CJ-08 remote_type mit abweichenden Werten. Der deployte Matcher liest job.remote_type (Zeile 1029, 1466), nicht remote_policy – also remote_type behalten und remote_policy str <br>— Dublette zu CJ-08 mit abweichendem Wertebereich. BEWIESEN: Der Live-Matcher liest ausschließlich job.remote_type (calculate-match-v3-1/index.ts:1029, 1466). remote_policy sollte aus remote_type abgele |
| `CS-11` | 3x | Gründungsjahr, gleiche Begründung wie CS-10: hoher De-Anonymisierungswert, kein Matching-Beitrag. Gehört allenfalls auf die Sperrliste in UE-13, nicht in ein Recruiter-Feld. <br>— Gründungsjahr trägt keine Suchentscheidung und ist zugleich ein starker Identifikationsanker im DACH-Mittelstand. Wenn Reifegrad relevant ist, deckt die Größenangabe das ausreichend ab. <br>— Gründungsjahr trägt keine Entscheidung, erhöht aber die Identifizierbarkeit im anonymen Zustand. Wenn überhaupt, gehört es in den kandidatensichtbaren Text nach dem Reveal. |
| `CJ-08` | 3x | 'Remote-Modell' (jobs.remote_type) und CJ-15 'Remote-Policy' (jobs.remote_policy) sind zwei Felder mit unterschiedlichen Wertebereichen fuer dieselbe Aussage, ohne Abgleich. Der Matcher liest remote_t <br>— Doppelung zu CJ-15 mit abweichendem Wertebereich (onsite/hybrid/remote gegen full_remote/hybrid/onsite_only), kein Abgleich zwischen beiden. Eine der beiden muss weg; für diese Rollenfamilie ist die A <br>— 'Remote-Modell' (jobs.remote_type) konkurriert mit CJ-15 'Remote-Policy' (jobs.remote_policy) — zwei Fragen, zwei Wertebereiche, dieselbe Aussage, kein Abgleich. Beide Spalten existieren in PROD und b |
| `IB-16` | 2x | "Wie weit geht das Gehalt nach oben?" — die Chips "Verhandelbar" und "Marktrate (beraten Sie uns)" (BEWIESEN IntakeBriefing.tsx:173) sind Nicht-Antworten, die volle Punktzahl bringen und dem Recruiter <br>— Gehaltsdecke als Chips. Inhaltlich richtig, aber unvollständig für Finance und ohne verwertbares Ziel. Es fehlen die zwei Punkte, die in Finance Prozesse platzen lassen: der variable Anteil (ist die g |
| `IB-24` | 2x | "Wie wahrscheinlich ist eine Verlängerung?" — Dublette zu PS-12, dort binär, hier vierstufig. Die vierstufige Variante ist inhaltlich besser; dann PS-12 streichen und IB-24 typisiert speichern. Auf ke <br>— 'Wie wahrscheinlich ist eine Verlaengerung' (vierstufig) doppelt PS-12 ('Verlaengerung moeglich', binaer). Die binaere Variante ist die schwaechere — hier ausnahmsweise PS-12 streichen und IB-24 behal |
| `CS-08` | 2x | Doppelerfassung der Umsatzsteuer-ID zu ON-04 (client_verifications.vat_id) ohne jeden Abgleich. Eine Quelle genügt; ON-04 bleibt. <br>— Doppelerfassung der USt-IdNr. zu ON-04 ohne jeden Abgleich. Eine Quelle festlegen (Vorschlag: client_verifications.vat_id bei der Verifizierung), im Firmenprofil nur anzeigen. |
| `IB-08` | 2x | Deckungsgleich mit IB-05 (90 Tage sind drei Monate). Zusammengeführt in UE-03. <br>— Wie IB-07: geht in der Folgefrage von K02 auf; separater Chip-Satz erzeugt drei nebeneinanderliegende Fließtexte in briefing_notes, die niemand auswertet. |
| `IB-10` | 2x | 'Zum wichtigsten Muss-Skill: wie tief muss er sitzen' fragt BEWIESEN nur nach EINEM Skill, unabhaengig davon, wie viele Muss-Kriterien existieren, und erzeugt keine Zeile in job_skill_requirements. Er <br>— Fragt nach der Tiefe von genau EINEM Muss-Skill, unabhängig davon, wie viele es gibt. Für Finance untauglich: Ein Bilanzbuchhalter-Profil hat mindestens drei unabhängige Tiefendimensionen (Abschlussar |

### Alle weiteren Streich-Empfehlungen

| Frage-ID | Agent | Begruendung |
|---|---|---|
| `IB-27` | interviewmethodik | Formulierung methodisch defekt: das einleitende "Ehrlich:" unterstellt, die bisherigen Antworten seien es nicht; die Frage verlangt eine Selbstanklage; und der Chip "Gibt keine nennenswerte" (BEWIESEN IntakeBriefing.tsx:236) ist eine Fluchttür, die mit voller  |
| `CJ-19` | interviewmethodik | "Industrie" — BEWIESEN in PROD nie sichtbar (gleiche Sichtbarkeitsfalle), und die Optionsliste ist inkompatibel zu CS-03 (englisch gegen deutsch). Nicht fragen, sondern aus company_profiles.industry übernehmen und einmal bestätigen lassen. |
| `CJ-20` | interviewmethodik | "Unternehmensgröße" — BEWIESEN in PROD nie sichtbar; identische Information liegt bereits in company_profiles.headcount (CS-09). Doppelerfassung derselben Zahl in zwei Taxonomien. |
| `CJ-25` | interviewmethodik | "Warum ist die Stelle vakant?" — Dublette zu IB-01 mit abweichenden Optionen ("Umstrukturierung" gegen "Ablösung"/"Elternzeit-Vertretung"). Eine Version behalten; die IB-01-Optionen sind die trennschärferen, weil sie den Vertraulichkeitsfall abbilden. |
| `IB-33` | interviewmethodik | "Was dürfen wir anonym verraten?" als abstrakte Chip-Liste — der Kunde kann nicht beurteilen, was die Kombination aus Branche, Größe und Region über ihn verrät. Umbauen zur Bestätigung: den fertigen anonymen Satz zeigen und fragen, ob er zu viel verrät. Zusätz |
| `PS-05` | conversational-ai | Branche wird im Studio erfasst, aber von buildRecord (JobIntakeStudio.tsx:332-354) nicht geschrieben — dauerhafter Datenverlust unabhängig von der fehlenden Migration. Entweder verdrahten oder nicht fragen; als Frage aus dem Firmenprofil ohnehin ableitbar. |
| `CJ-12` | it-headhunter | 'Erforderliche Skills, kommagetrennt' ist fuer IT aktiv schaedlich, nicht nur nutzlos. BEWIESEN (calculate-match-v3-1:1298, per node reproduziert): das Matching ist substring-basiert, damit zaehlt der eingetippte Job-Skill 'Java' als DIREKTER Volltreffer auf d |
| `IB-14` | sourcing-intelligence | Ersetzt durch S-05. Die Antwort setzt jobs.visa_sponsorship nicht, während der Matcher genau dieses Feld als Hard Kill auswertet. In der heutigen Form erzeugt die Frage die Illusion einer Entscheidung. |
| `IB-31` | sourcing-intelligence | Ersetzt durch S-02. Die Chips ('Direkte Wettbewerber', 'Branchenführer') liefern Kategorien statt Namen und sind nicht in eine Suche übersetzbar; zudem landet die Antwort in briefing_notes und erreicht den Recruiter nachweislich nie. |
| `IB-32` | sourcing-intelligence | Ersetzt durch S-03. Eine Tabuliste, die als Fließtext gespeichert und im Recruiter-View nicht angezeigt wird, ist eine nicht durchsetzbare Zusage – schlechter als keine Frage, weil sie beim Kunden Sicherheit suggeriert. |
| `CJ-06` | contracting | 'Gehalt (€) Min/Max' ist im Contracting-Pfad die falsche Einheit und zugleich ein Veroeffentlichungs-Gate: CreateJob.tsx:578-581 verlangt eine Gehaltsrange zum Einreichen, unabhaengig von employment_type. Ein Contracting-Einsatz kann heute im LIVE-Pfad nur ver |
| `PS-06 / PS-07` | contracting | Nicht die Felder sind das Problem, sondern die Nullsetzung dahinter: JobIntakeStudio.tsx:342-343 schreibt bei employment_type='freelance' salary_min und salary_max hart auf null. Solange keine Satzspalte existiert, ist das ein garantierter Totalverlust der Ver |
| `CJ-09` | contracting | Erfahrungslevel (Junior/Mid/Senior/Lead) ist bei Freiberuflern nahezu bedeutungslos: entscheidend ist die nachweisbare Vorleistung, nicht die Anzahl der Jahre. Im Contracting-Pfad durch C-14 ersetzen; die Spalte experience_level darf dort aus der Vorleistung a |
| `IB-18` | contracting | 'Was ist ueber das Gehalt hinaus verhandelbar?' mit den Optionen Sign-on-Bonus, Titel/Level und Equity ist auf Freiberufler nicht anwendbar. Die verhandelbaren Hebel im Contracting sind Satz, Auslastung, Onsite-Tage und Reisekosten — die deckt C-06 und C-09 ab |
| `IB-25` | contracting | 'Was macht diese Rolle besonders?' mit Chips wie Mission/Impact und Wachstum/Equity zielt auf Festanstellungskandidaten. Ein Freiberufler entscheidet ueber Satz, Laufzeit, Technologie und Planbarkeit. Im Contracting-Pfad streichen; C-01 und C-04 liefern die ta |
| `CJ-08 gegen CJ-15` | contracting | Zwei Felder fuer dieselbe Aussage: remote_type (onsite/hybrid/remote) und remote_policy (onsite_only/hybrid/full_remote), ohne jeden Abgleich. Im Contracting-Pfad genuegt eine Angabe plus C-06 (Onsite-Tage und Reisekosten). |
| `CS-01 / CS-13 / CS-14` | contracting | Tote Elemente im Firmenprofil: der Logo-Button ist permanent disabled und hat kein onClick (ClientSettings.tsx:232-238), die Team-Verwaltung ist ein Platzhalter ohne Eingabefeld (Zeile 408-424) — waehrend JobIntakeStudio.tsx:710-729 auf 'Einstellungen → Team'  |
| `IB-19 (Gegenbefund, nicht streichen sondern oeffnen)` | contracting | Kein Streichkandidat, sondern eine Luecke: onsite_days traegt appliesTo:'full-time' (IntakeBriefing.tsx:189) und applicableFor filtert es fuer Freelance heraus (Zeile 290). Zusammen mit der Contracting-Sektion in ProfileSections.tsx:138-181, die ebenfalls kein |
| `IB-03` | executive-search-partner | Doppelung zu CJ-29 und EX-02. BEWIESEN: Die Antwort landet nur als Fließtext in briefing_notes und setzt jobs.team_size nicht. EX-02 schreibt stattdessen team_size und department_structure typisiert. |
| `IB-06` | executive-search-partner | Der Hands-on-Anteil ist für Führungsmandate durch EX-02 (Führungsspanne inklusive der Frage, ob Führungskräfte geführt werden) und EX-04 (Veränderungsauftrag) präziser und ohne Selbsteinschätzung beantwortet. Für Fach- und Expertenrollen kann die Frage bleiben |
| `IB-09` | executive-search-partner | Fragt das Erfolgsbild, schreibt aber BEWIESEN jobs.success_profile nicht (die Spalte existiert in PROD, REST 200, wird nur vom CreateJob-Pfad aus intakeData befüllt). Ersetzt durch EX-05, das gezielt in success_profile schreibt. |
| `IB-26` | executive-search-partner | Doppelung zur Folgefrage in EX-01 (Berichtslinie). Zusätzlich De-Anonymisierungsrisiko: Die Chip-Option 'Erfahrene:r Lead (ex-Top-Firma)' und der Freitext können die Firma verraten, ohne dass im statischen Pfad ein Guardrail existiert. EX-01 fragt bewusst nach |
| `CJ-24` | executive-search-partner | In der heutigen Form streichen, nicht das Konzept. BEWIESEN: extract-intake-briefing liefert HTTP 404, analyzeBriefing wirft, und der gesamte getippte Text ist ersatzlos verloren — obwohl dies die einzige Stelle im Live-Pfad ist, an der ein Kunde Team, Kultur, |
| `CS-09` | dach-executive-search | Exakte Mitarbeiterzahl steht im Abschnitt 'wird Recruitern angezeigt' (ClientSettings.tsx:343-345) und identifiziert zusammen mit CS-10, CS-11 und der Branche den Kunden. Für den anonymen Beschreibungstext genügt ein Größenband; die exakte Zahl ersatzlos strei |
| `ON-03` | dach-executive-search | Handelsregisternummer ist im Onboarding leer absendbar (ClientOnboarding.tsx:289) und wird nirgends geprüft — sie erzeugt Reibung ohne Nutzen. Erst bei Vertrags- oder Rechnungsstellung erheben, dann aber verpflichtend. |
| `IB-01 bis IB-34 (gesamter statischer Briefing-Block, Sammelbegründung)` | finance-headhunter | BEWIESEN: Das einzige Ziel aller 34 Fragen ist jobs.briefing_notes, und jobs.briefing_notes ist in PRODUKTION nicht Teil von recruiter_jobs_view. REST auf jobs.briefing_notes liefert [] (Spalte existiert), REST auf recruiter_jobs_view.briefing_notes liefert 42 |
| `IB-15` | finance-headhunter | 'Echte Ausschlusskriterien' als Sammelfrage mit Freitext. Vermischt drei Dinge mit völlig verschiedenen Zielfeldern und Risikoprofilen: Zertifikate (FIN-09, Ziel required_certifications — hard kill), Wettbewerbskonflikte (FIN-15, Ziel fehlt in PROD) und Budget |
| `IB-31 und IB-32` | finance-headhunter | Ziel- und No-Go-Firmen. Beide Zielspalten existieren in PRODUKTION nicht (BEWIESEN, REST 42703 für jobs.target_companies und jobs.nogo_companies), die vorgesehene Vorbefüllquelle company_profiles.excluded_companies ebenfalls nicht (BEWIESEN, REST 42703) — und  |
| `IB-28 und IB-29` | finance-headhunter | Interview-Prozess und Entscheider. IB-29 ist inhaltlich doppelt zu CJ-27, und CJ-27 ist BEWIESEN wertlos (siehe dort). Beide liefern zudem keine strukturierte Stufendefinition, sondern Fließtext in briefing_notes. Für Finance ist die entscheidende Prozessfrage |
| `IB-22, IB-23, IB-24 gegen PS-10, PS-11, PS-12` | finance-headhunter | Projektdauer, Auslastung und Verlängerung werden je zweimal erhoben, mit unterschiedlichen Skalen (IB-24 vierstufig gegen PS-12 binär) und ohne Abgleich. Beide Seiten landen nirgends verwertbar: PS-10/11/12 in intake_payload (BEWIESEN 42703 in PROD), IB-22/23/ |
| `CJ-19, CJ-20, CJ-21, CJ-22, CJ-23` | finance-headhunter | In PRODUKTION unerreichbar. Das Akkordeon rendert nur bei (enrichmentData || industry || company_size_band || funding_stage), und alle vier Quellen hängen an parse-job-url, parse-job-pdf oder enrich-job-data — alle drei in PROD HTTP 404. Es gibt kein zweites E |
| `CS-01, CS-13, CS-14` | finance-headhunter | Kein Ziel und kein Nutzen. CS-01 Logo-Button ist permanent disabled und hat kein onClick; CS-13 Team-Mitglieder ist ein Platzhalter ohne Eingabefeld (und widerspricht JobIntakeStudio.tsx:710-729, das auf 'Einstellungen → Team' verweist, das es nicht gibt); CS- |
| `CS-09, CS-10, CS-11, CS-12` | finance-headhunter | Der Abschnitt trägt die Überschrift 'Partner Facts für Recruiter' mit dem Text 'Diese Informationen werden Recruitern angezeigt' (ClientSettings.tsx:343-345). Das ist BEWIESEN falsch: recruiter_jobs_view enthält kein client_id und keine company_profiles-Felder |

### Automatisch vorbefuellbar statt fragen — 107 Empfehlungen

| Frage-ID | Quelle der Vorbefuellung | Agent |
|---|---|---|
| `IB-01` | BEWIESEN: prefillFromBuilt setzt a.why_open aus b.vacancyReason (IntakeBriefing.tsx:33). Aus der Anzeige ableitbar — als Bestätigung zeigen: "In der Anzeige klingt es nach Wachstum — stimmt das?" | interviewmethodik |
| `IB-13` | BEWIESEN: Sprachanforderung wird aus must_haves per Regex erkannt (IntakeBriefing.tsx:26-32). Nur bestätigen lassen, nicht neu fragen — aber typisiert speichern (required_languages), nicht als Fließtext. | interviewmethodik |
| `IB-19` | BEWIESEN: onsite_days wird aus built.remoteDays berechnet (IntakeBriefing.tsx:36-39). Reine Ableitung, keine eigene Frage. | interviewmethodik |
| `IB-20` | BEWIESEN: start wird auf "ASAP" gesetzt, wenn hiringUrgency "urgent"/"hot" (IntakeBriefing.tsx:35). Dublette zur Dringlichkeit — als Bestätigung anbieten. | interviewmethodik |
| `IB-25` | BEWIESEN: x_factor wird aus usps[0] vorbefüllt (IntakeBriefing.tsx:40). Methodisch problematisch, weil dem Kunden seine eigene Werbesprache als Antwort vorgeschlagen wird — durch UE-13 ersetzen und die Vorbefüllung nur noch als Kontext für  | interviewmethodik |
| `IB-26` | BEWIESEN: manager wird aus b.reportsTo vorbefüllt (IntakeBriefing.tsx:34). Aus der Anzeige übernehmen, nur bestätigen. | interviewmethodik |
| `IB-21` | In DACH ist die dreimonatige Kündigungsfrist der Regelfall. Als Vorgabe "bis 3 Monate" setzen und nur bei Dringlichkeit "sehr dringend" aktiv bestätigen lassen. | interviewmethodik |
| `CJ-05` | company_profiles.company_name (BEWIESEN CS-02, ClientSettings.tsx:246-252). Der Firmenname darf pro Job nie neu getippt werden — abweichende Schreibweisen sind ein Enttarnungs- und Dublettenrisiko. | interviewmethodik |
| `CJ-07` | company_profiles.office_locations wird im Studio bereits gelesen (BEWIESEN JobIntakeStudio.tsx:151-165). Als Auswahl der hinterlegten Standorte anbieten statt als Freitext. | interviewmethodik |
| `CJ-19` | company_profiles.industry (CS-03). Voraussetzung: die beiden inkompatiblen Taxonomien vereinheitlichen — deutsch in ClientSettings.tsx:51-64 gegen englisch in CreateJob.tsx:1472-1484. | interviewmethodik |
| `CJ-20` | company_profiles.headcount (CS-09) auf das Größenband abbilden. Reine Rechnung, keine Frage. | interviewmethodik |
| `IB-32` | company_profiles.excluded_companies wird im Studio schon zur Vorbefüllung gelesen (BEWIESEN JobIntakeStudio.tsx:156-159). ACHTUNG BEWIESEN: die Spalte fehlt in PROD (42703) und es gibt kein UI, das sie füllt — die Vorbefüllung läuft heute i | interviewmethodik |
| `PS-16` | Der anonyme Firmenbeschreiber wird bereits aus Branche, Größe und Region vorgeschlagen (BEWIESEN JobIntakeStudio.tsx:162-166, 246-248). Nicht fragen, sondern den fertigen Satz zeigen und bestätigen lassen — das ist die einzige Form, in der  | interviewmethodik |
| `CJ-06` | Gehaltsspanne aus dem zuletzt veröffentlichten Job derselben Rollenfamilie desselben Kunden vorschlagen: "Beim letzten Vertriebsleiter lagen Sie bei 90.000 bis 110.000 — passt das hier auch?" | interviewmethodik |
| `ON-03` | Handelsregisternummer wird zweimal erhoben: client_verifications.company_registration_number (ON-03) und faktisch erneut über CS-08. Einmal erheben, an beiden Stellen anzeigen. | interviewmethodik |
| `ON-04` | BEWIESEN Doppelerfassung: client_verifications.vat_id (ON-04) und company_profiles.tax_id (CS-08), ohne jeden Abgleich. Zwei Felder für dieselbe Nummer erzeugen garantiert widersprüchliche Rechnungsdaten. | interviewmethodik |
| `CJ-04` | Stellenanzeige beziehungsweise Freitextbeschreibung über parse-job-url/parse-job-pdf (beide in PROD 404 — Deploy ist Vorbedingung). Danach nur noch in UE-01 bestätigen. | conversational-ai |
| `CJ-05` | company_profiles.company_name, ersatzweise client_verifications. Nie erneut fragen — und in Recruiter-Ansichten durch den Descriptor aus UE-13 ersetzen, statt im Klartext auszuliefern. | conversational-ai |
| `CJ-07` | company_profiles.office_locations[0] (JobIntakeStudio.tsx:147-153 macht das bereits) oder aus der Anzeige. Bestätigen statt fragen. | conversational-ai |
| `CJ-08` | company_profiles.remote_policy als Vorbelegung, in UE-01 als Bestätigungsfrage ausgespielt ('Ihr Standard ist hybrid mit zwei Bürotagen — gilt das auch hier?'). | conversational-ai |
| `CJ-09` | Aus Titel und geforderten Jahren der Anzeige abgeleitet. Nur bestätigen; der abgeleitete Wert darf ohne Bestätigung nicht in experience_min/max wandern. | conversational-ai |
| `CJ-10` | Anzeige beziehungsweise Freitext. Kein Eingabefeld für den Kunden, sondern Ergebnis der Extraktion. | conversational-ai |
| `CJ-11` | Anzeige. Wird zu must_haves/nice_to_haves normalisiert und nur über UE-06 auf Härte geprüft. | conversational-ai |
| `CJ-12` | Anzeige plus Normalisierung zu sauberen Skill-Tokens. Nachgefragt wird ausschließlich die Tiefe (UE-05), nie die Liste selbst. | conversational-ai |
| `CJ-16` | company_profiles.address. Wichtig: automatisch auf die Sperrliste in UE-13 setzen — die exakte Adresse steht im KI-Prompt selbst auf der red_list (index.ts:175), wird im CreateJob-Pfad aber ungeschützt am Job gespeichert. | conversational-ai |
| `CJ-19` | company_profiles.industry. Vorbedingung: die beiden unvereinbaren Taxonomien vereinheitlichen (ClientSettings.tsx:51-64 deutsch gegen CreateJob.tsx:1472-1484 englisch). | conversational-ai |
| `CJ-20` | company_profiles.headcount beziehungsweise team_size_range, gerundet auf ein Band. Nur als Band ausspielen, nie als exakte Zahl (De-Anonymisierung). | conversational-ai |
| `CJ-21` | Einmalig im Firmenprofil, danach an jeden Job vererbt. Freigabe zur Nennung regelt UE-13, nicht eine eigene Frage. | conversational-ai |
| `CJ-06` | Gehaltsband eines früheren Jobs derselben Rollenfamilie als Vorschlag. Gefragt wird nur noch die Decke (UE-10), nicht das Band. | conversational-ai |
| `CJ-25` | Aus der Anzeige extrahierbar (vacancy_reason wird von fromData bereits gelesen, JobIntakeStudio.tsx:42). Nur bestätigen; die Vorgänger-Nachfrage IB-02 feuert weiterhin bedingt. | conversational-ai |
| `IB-28` | Interview-Prozess des letzten Jobs desselben Kunden. Ab dem zweiten Job Vorschlag statt Frage — UE-15 wird dann zur Ein-Klick-Bestätigung. | conversational-ai |
| `IB-32` | company_profiles.excluded_companies (Spalte fehlt in PROD, 42703, und hat kein Eingabefeld — beides Vorbedingung). Danach in UE-12 nur bestätigen und ergänzen lassen. | conversational-ai |
| `PS-17` | Reveal-Zeitpunkt des ersten Jobs wird zum Kundenstandard und ab dem zweiten Job vorbelegt. UE-14 zeigt ihn als Vorbelegung. | conversational-ai |
| `PS-16` | Descriptor aus Branche, Größenband und Region automatisch vorschlagen (JobIntakeStudio.tsx:162-166 tut das bereits). Der Kunde bestätigt oder schärft nach — er formuliert nicht von null. | conversational-ai |
| `CS-03` | Einmal im Firmenprofil, danach an jeden Job vererbt. Im Job selbst nie erneut fragen. | conversational-ai |
| `CS-09` | Einmal im Firmenprofil, aber ausschließlich als Größenband weitergeben, nicht als exakte Mitarbeiterzahl. | conversational-ai |
| `CJ-05` | company_profiles.company_name (ClientSettings.tsx:246-252, Spalte in PROD vorhanden). Der Firmenname darf nie erneut abgefragt werden, sondern nur bestaetigt: 'Wir legen die Stelle fuer {Firmenname} an — richtig?' JobIntakeStudio.tsx:151 li | it-headhunter |
| `CJ-19` | company_profiles.industry (ClientSettings.tsx:256-269). Als Bestaetigungsfrage statt Neufrage. ACHTUNG: BEWIESEN inkompatible Taxonomien — ClientSettings verwendet deutsche Labels ('Technologie'), CreateJob.tsx:1472-1484 englische ('Technol | it-headhunter |
| `CJ-20` | company_profiles.headcount (ClientSettings.tsx:350-360) laesst sich verlustfrei auf die Baender von jobs.company_size_band abbilden. Nur bestaetigen. Zugleich Triple-Blind pruefen: headcount + annual_revenue + founded_year + USP zusammen id | it-headhunter |
| `CJ-07` | company_profiles.office_locations wird von JobIntakeStudio.tsx:151-165 bereits gelesen. Als Auswahl der vorhandenen Standorte anbieten statt als leeres Freitextfeld. Die exakte Buero-Adresse (CJ-16) dabei NICHT vorbefuellen — sie steht im K | it-headhunter |
| `CJ-15` | company_profiles.remote_policy (von JobIntakeStudio.tsx:151-166 gelesen). Als Abweichungsfrage formulieren, wie es der KI-Prompt bereits vorsieht (intake-questions/index.ts, Regel 8: 'Ihr Standard ist hybrid mit 2 Buerotagen — gilt das auch | it-headhunter |
| `CJ-09` | Aus dem Jobtitel ableitbar: 'Senior', 'Lead', 'Principal', 'Junior', 'Werkstudent' im Titel setzen jobs.experience_level vor. Nur bestaetigen. IT-05 (Entscheidungshoheit) ist die eigentliche Pruefung, ob das Level stimmt. | it-headhunter |
| `CJ-06` | Aus einem frueheren Job desselben Kunden in derselben Rollenfamilie und Seniority: salary_min/salary_max als Vorschlag anbieten ('Bei Ihrer letzten Senior-Entwicklerstelle lag das Band bei 80-95k — starten wir hier gleich?'). Kein Neuerhebe | it-headhunter |
| `IB-32` | GEPLANT WAR company_profiles.excluded_companies (JobIntakeStudio.tsx:156-159 liest es bereits). BEWIESEN ist die Spalte in PROD NICHT vorhanden (REST 42703) und es existiert kein UI, das sie fuellt. Die No-Go-Frage ist deshalb heute NICHT a | it-headhunter |
| `CJ-23` | jobs.tech_environment (string[], Spalte in PROD vorhanden, REST 200) sollte aus IT-03 und IT-09 befuellt werden statt aus enrich-job-data. BEWIESEN liefert enrich-job-data in PROD HTTP 404, deshalb bleibt die Spalte heute immer null und der | it-headhunter |
| `CJ-25` | Falls der Kunde fuer dieselbe Rolle schon einmal gesucht hat, ist vacancy_reason meist identisch ('Wachstum'). Als Vorbelegung anbieten. Bei der ersten Stelle eines Kunden normal fragen. | it-headhunter |
| `CJ-05` | company_profiles.company_name (CS-02). Der Firmenname darf pro Job nicht frei getippt werden – das ist BEWIESEN die Triple-Blind-Sollbruchstelle: recruiter_jobs_view liefert company_name (REST 200), und im CreateJob-Pfad existiert kein reve | sourcing-intelligence |
| `CJ-19` | company_profiles.industry (CS-03). Nicht fragen, sondern bestätigen. Achtung BEWIESEN: Die Optionslisten sind inkompatibel (deutsch in ClientSettings.tsx:51-64 vs. englisch in CreateJob.tsx:1472-1484) – vor der Übernahme muss eine Taxonomie | sourcing-intelligence |
| `CJ-20` | company_profiles.headcount (CS-09). Unternehmensgröße aus dem Profil ableiten und in ein Band überführen; heute in PROD ohnehin nie sichtbar (Rendering hängt an enrich-job-data, HTTP 404). | sourcing-intelligence |
| `CJ-16` | company_profiles.address (CS-07). Aus dem Profil übernehmen, aber NICHT recruiter-sichtbar machen: Die exakte Adresse steht im KI-Briefing selbst auf der roten Liste (intake-questions/index.ts:175) und ist der stärkste De-Anonymisierungsvek | sourcing-intelligence |
| `CJ-07` | company_profiles.office_locations. Standort aus dem Profil vorschlagen; nur bei Abweichung nachfragen. Für Sourcing zählt ohnehin nicht der Bürostandort, sondern der Radius (S-07). | sourcing-intelligence |
| `CJ-08` | company_profiles.remote_policy. Als Bestätigungsfrage stellen ('Ihr Standard ist hybrid mit zwei Bürotagen – gilt das auch hier?') statt als Neuerhebung; der Prompt von intake-questions sieht genau dieses Muster bereits vor (Regel 8). | sourcing-intelligence |
| `S-03` | company_profiles.excluded_companies. Die Tabuliste gehört an die Firma, nicht an den einzelnen Job – einmal pflegen, pro Job nur bestätigen. BEWIESEN: Spalte fehlt in PROD (42703) und hat kein UI; beides muss zusammen mit der Migration ents | sourcing-intelligence |
| `S-11` | company_profiles.unique_selling_point (CS-12). Als Vorbefüllung für das Wechselmotiv nutzen und nur schärfen. BEWIESEN: CS-12 wird heute in die Jobaufnahme gar nicht übernommen – JobIntakeStudio liest nur industry, company_name, office_loca | sourcing-intelligence |
| `PS-05` | company_profiles.industry. Das Freitextfeld 'Branche' im Studio wird BEWIESEN nie gespeichert (buildRecord schreibt industry nicht) – es gehört ersetzt durch einen vorbefüllten Bestätigungswert aus dem Firmenprofil, der tatsächlich nach job | sourcing-intelligence |
| `CJ-04` | Importierte Stellenanzeige über parse-job-url / parse-job-pdf. Titel, Beschreibung (CJ-10), Anforderungen (CJ-11) und Skills (CJ-12) sollen extrahiert und bestätigt werden, nicht getippt. BEWIESEN: Alle drei Importfunktionen liefern in PROD | sourcing-intelligence |
| `S-01` | Ableitbar aus jobs.title plus den Aufgaben durch das Sprachmodell. Titelvarianten sollen vorgeschlagen und abgehakt werden – Vorschlagen funktioniert hier nachweislich besser als Fragen, weil Kunden Synonyme wiedererkennen, aber nicht aktiv | sourcing-intelligence |
| `CJ-18` | Die Einstiegswahl 'Contracting / Interim' in NeueStelleBar.tsx:71 setzt employment_type bereits. Die Anstellungsart darf im Contracting-Pfad nicht ein zweites Mal als Select erscheinen (CreateJob.tsx:1427-1441). | contracting |
| `CJ-05` | company_profiles.company_name (ClientSettings.tsx:246-252). Wird heute BEWIESEN als Pflicht-Freitext neu abgefragt (CreATEJob.tsx:1195-1203) und blockiert sogar den Entwurf. Vorbefuellen und nur bestaetigen lassen. | contracting |
| `CJ-07` | company_profiles.address (ClientSettings.tsx:313-320) beziehungsweise office_locations, die JobIntakeStudio.tsx:151-165 ohnehin bereits liest. Standort vorschlagen, nicht fragen. | contracting |
| `CJ-19` | company_profiles.industry (ClientSettings.tsx:256-269). Achtung: BEWIESEN zwei unvereinbare Taxonomien (deutsch in CS-03, englisch in CreateJob.tsx:1472-1484) — vor dem Vorbefuellen vereinheitlichen. Zusaetzlich ist CJ-19 in PROD ohnehin un | contracting |
| `PS-16` | Der Reveal-Descriptor laesst sich aus company_profiles.industry + headcount + Region generieren; JobIntakeStudio.tsx:162-166 schlaegt genau das bereits vor. Nur bestaetigen lassen, nicht tippen lassen. | contracting |
| `C-07` | Firmen-Konstante: Auslandsarbeit ist eine Unternehmensregel, keine Einsatz-Eigenschaft. Einmal im Firmenprofil erfassen (company_profiles, neues Feld), pro Einsatz nur bestaetigen. | contracting |
| `C-10` | Firmen-Konstante: Beschaffungsweg und Zahlungsziel aendern sich nicht pro Einsatz. Einmal im Firmenprofil erfassen, danach vorbefuellen und nur bestaetigen. Bei Wiederholungskunden zusaetzlich aus dem letzten Contracting-Einsatz uebernehmen | contracting |
| `C-12` | Firmen-Konstante: Haftpflicht-, NDA- und Zertifikatsanforderungen sind Unternehmensstandards. Aus dem letzten Contracting-Einsatz desselben Kunden vorbefuellen; nur einsatzspezifische Zusaetze (Sicherheitsueberpruefung) neu fragen. | contracting |
| `C-13` | Aus dem letzten Contracting-Einsatz desselben Kunden: die Zugangsvorlaufzeit ist eine IT-Eigenschaft der Firma, keine des Einsatzes. | contracting |
| `C-04` | Laufzeit und Auslastung lassen sich aus einem importierten Anforderungsprofil ziehen (parse-job-url / parse-job-pdf), sobald diese Funktionen deployed sind — BEWIESEN liefern beide in PROD heute HTTP 404, weshalb alle drei Importwege tot si | contracting |
| `C-14` | Muss-Kriterien aus dem importierten Anforderungsprofil vorschlagen; der Kunde stuft dann nur noch ein (Wunsch / Mindestanforderung / Ausschluss). Setzt ebenfalls die Deployment der parse-Funktionen voraus. | contracting |
| `IB-32` | company_profiles.excluded_companies wird von JobIntakeStudio.tsx:156-159 bereits gelesen. BEWIESEN fehlt die Spalte jedoch in PROD (42703) und es gibt kein UI, das sie fuellt — erst Feld schaffen, dann vorbefuellen. | contracting |
| `ON-04 / CS-08` | Doppelerfassung derselben Umsatzsteuer-ID in client_verifications.vat_id (ClientOnboarding.tsx:276-282) und company_profiles.tax_id (ClientSettings.tsx:324-330) ohne jeden Abgleich. Eine Quelle festlegen, die andere nur anzeigen. | contracting |
| `CJ-05` | company_profiles.company_name (CS-02) — bereits Pflichtfeld in ClientSettings.tsx:246-252. Statt erneut abfragen: vorbefüllen und nur bestätigen lassen. Achtung: Der Klartext-Firmenname am Job ist die Triple-Blind-Sollbruchstelle; für vertr | executive-search-partner |
| `CJ-19` | company_profiles.industry (CS-03). Voraussetzung: Die beiden Taxonomien vereinheitlichen — CS-03 nutzt deutsche Labels (ClientSettings.tsx:51-64), CJ-19 englische (CreateJob.tsx:1472-1484). Solange beide Listen existieren, ist eine Übernahm | executive-search-partner |
| `CJ-20` | company_profiles.headcount (CS-09) — auf das Band von jobs.company_size_band abbilden. Für Führungsmandate zusätzlich prüfen, ob die Kombination aus Größe, Branche und Region den Kunden identifizierbar macht. | executive-search-partner |
| `CJ-07` | company_profiles.address (CS-07) beziehungsweise office_locations — als Bestätigungsfrage stellen ('Ihr Standard ist Frankfurt, gilt das auch hier?'), nicht als Leerfeld. | executive-search-partner |
| `CJ-16` | company_profiles.address (CS-07). Zusätzlich zwingend auf reveal_envelope.red_list: Die exakte Adresse steht im Prompt von intake-questions ausdrücklich auf der red_list (index.ts:175), wird im CreateJob-Pfad aber ungeschützt an jobs.office | executive-search-partner |
| `CJ-15` | company_profiles.remote_policy — als Bestätigungsfrage im Sinne von Regel 8 des KI-Prompts ('Ihr Standard ist hybrid mit 2 Bürotagen — gilt das auch hier?'). | executive-search-partner |
| `CJ-18` | Für die Rollenfamilie Führung & Executive auf 'full-time' vorbelegen. Interim-Mandate sind ein eigener Fork und sollten aktiv gewählt werden, statt die Freelance-Strecke versehentlich auszulösen. | executive-search-partner |
| `ON-04` | Doppelerfassung mit CS-08 (company_profiles.tax_id gegen client_verifications.vat_id, kein Abgleich). Einmal führend erfassen und in das jeweils andere Feld spiegeln. | executive-search-partner |
| `IB-32` | company_profiles.excluded_companies — Vorbefüllung ist in JobIntakeStudio.tsx:156-159 bereits vorgesehen. BEWIESEN blockiert: Die Spalte fehlt in PROD (REST 42703) und es existiert kein UI, um sie zu füllen. Erst nach Migration und Firmenpr | executive-search-partner |
| `IB-01` | Aus EX-09 ableitbar: 'Vakant' und 'Besetzt' ergeben Nachbesetzung, 'Neu geschaffen' ergibt new_position. Statt separat fragen: aus der Diskretionsantwort setzen und jobs.vacancy_reason direkt schreiben — heute wird IB-01 BEWIESEN nur nach b | executive-search-partner |
| `CS-12` | company_profiles.unique_selling_point ist bereits erfasst, wird aber BEWIESEN nicht in die Jobaufnahme übernommen (JobIntakeStudio liest nur industry, company_name, office_locations, excluded_companies, team_size_range/company_size, remote_ | executive-search-partner |
| `CJ-05` | company_profiles.company_name (BEWIESEN: existiert in PROD, REST 200). Der Firmenname darf nie erneut abgefragt werden — und gehört als Klartext ohnehin nur in das Reveal-Kuvert, nicht in den recruiter-sichtbaren Datensatz. | dach-executive-search |
| `CJ-07` | company_profiles.office_locations (BEWIESEN: REST 200) — Auswahlliste statt Freitext, Region als anonymer Anteil der Beschreibung aus K12. | dach-executive-search |
| `CJ-19` | company_profiles.industry (BEWIESEN: REST 200). Voraussetzung: eine gemeinsame Taxonomie — heute stehen sich zwei Listen gegenüber (deutsch in ClientSettings.tsx:51-64, englisch in CreateJob.tsx:1472-1484). | dach-executive-search |
| `CJ-20` | company_profiles.headcount (BEWIESEN: REST 200), umgerechnet in ein Band nach jobs.company_size_band (BEWIESEN: REST 200). Nie die exakte Zahl an Recruiter geben. | dach-executive-search |
| `CJ-08` | company_profiles.remote_policy (BEWIESEN: REST 200) als Vorbelegung, dem Kunden als Bestätigung anbieten ('Ihr Standard ist hybrid mit zwei Bürotagen — gilt das hier auch?'). | dach-executive-search |
| `CJ-17` | Abgeleitet aus dem bestätigten Arbeitsmodell; zusätzlich jobs.onsite_required (BEWIESEN: existiert, REST 200, und wird vom Live-Matcher als harter Ausschluss gelesen, calculate-match-v3-1/index.ts:944-951) automatisch setzen: onsite_require | dach-executive-search |
| `CJ-29` | company_profiles.team_size_range (BEWIESEN: REST 200) als Vorschlag; bei einem zweiten Mandat im selben Team aus dem vorherigen Job übernehmen. | dach-executive-search |
| `CJ-04` | Import der Stellenanzeige (parse-job-url / parse-job-pdf) — ACHTUNG, BEWIESEN: beide liefern in PROD HTTP 404; bis zum Deploy bleibt der Titel eine manuelle Eingabe. Ohne diesen Deploy ist jede 'wir bestätigen statt zu fragen'-Regel wirkung | dach-executive-search |
| `CJ-10` | Stellenanzeige (Beschreibung, Aufgabenblock). Statt einer offenen Frage nach den Wochenaufgaben nur die Zuspitzung stellen: 'Die Anzeige nennt acht Aufgaben — welche drei füllen wirklich den Großteil der Woche?' | dach-executive-search |
| `CJ-12` | Stellenanzeige + skill_taxonomy/skill_synonyms (BEWIESEN: beide Tabellen werden vom Live-Matcher gelesen, calculate-match-v3-1/index.ts:653-664). Skills extrahieren und bestätigen lassen, statt kommagetrennt tippen zu lassen. | dach-executive-search |
| `IB-13` | Vorbelegung aus Standort und Sprache der Anzeige (DACH-Standort und deutschsprachige Anzeige → Deutsch C1). K06 bestätigt diesen Vorschlag nur, schreibt ihn aber typisiert nach jobs.required_languages. | dach-executive-search |
| `IB-25` | company_profiles.unique_selling_point (BEWIESEN: REST 200) plus die Antworten aus K01 und K02. Heute wird dieses Feld nicht in die Jobaufnahme übernommen — genau die Information, die der Recruiter für die Ansprache braucht. | dach-executive-search |
| `PS-16` | Automatisch erzeugen aus Branche + Größenband + Region (alle drei aus company_profiles verfügbar) und in K12 nur bestätigen lassen. Der Kunde soll den anonymen Beschreibungstext prüfen, nicht erfinden. | dach-executive-search |
| `IB-32` | company_profiles.excluded_companies — ACHTUNG, BEWIESEN: diese Spalte fehlt in PROD (REST 42703) und es gibt kein UI, das sie füllt. Bis dahin ist K11 die einzige Erhebung; die Antwort braucht dringend ein eigenes Feld, weil eine Abwerbe-Zu | dach-executive-search |
| `ON-04` | Einmalig bei der Verifizierung erheben, danach aus client_verifications.vat_id vorbefüllen und im Firmenprofil (company_profiles.tax_id, REST 200) nur anzeigen — nie zweimal fragen. | dach-executive-search |
| `CJ-05` | company_profiles.company_name (existiert in PROD, REST []). Bereits einmal im Firmenprofil erfasst (CS-02, dort Pflichtfeld). Als Pflicht-Freitext beim Job erneut abzufragen ist doppelt und erzeugt Schreibweisen-Varianten desselben Unterneh | finance-headhunter |
| `CJ-19` | company_profiles.industry (existiert in PROD, REST []). ACHTUNG vor dem Vorbefüllen: Die Optionslisten sind inkompatibel — ClientSettings.tsx:51-64 führt deutsche Werte ('Technologie', 'Finanzen & Banking'), CreateJob.tsx:1472-1484 englisch | finance-headhunter |
| `CJ-20` | company_profiles.headcount (existiert in PROD, REST []) → jobs.company_size_band durch Bandbildung. Der Kunde hat die Zahl bereits genannt; sie erneut als Auswahl zu erfragen ist reine Wiederholung. Für Finance zusätzlich relevant, weil Unt | finance-headhunter |
| `CJ-07` | company_profiles.office_locations (existiert in PROD, REST []). Als Bestätigungsfrage stellen ('Ihr Standard ist {Ort} — gilt das auch für diese Stelle?'), nicht als Leerfeld. Der KI-Briefing-Prompt sieht genau diese Bestätigungslogik berei | finance-headhunter |
| `CJ-15` | company_profiles.remote_policy (existiert in PROD, REST []) als Vorschlag, dann Bestätigungsfrage. Wichtig für Finance: Die Bestätigung darf nicht das Ende sein — FIN-13 muss trotzdem gestellt werden, weil die Firmenregel und die Abschlussw | finance-headhunter |
| `CS-08` | client_verifications.vat_id (Spalte existiert in PROD, aus ON-04). CS-08 (company_profiles.tax_id) und ON-04 erfassen dieselbe Umsatzsteuer-ID an zwei Stellen ohne jeden Abgleich. Einmal erfassen, das zweite Feld daraus füllen und schreibge | finance-headhunter |
| `FIN-04` | Vorheriger Job desselben Kunden (jobs gefiltert auf client_id, Spalte existiert). Der Rechnungslegungsstandard eines Unternehmens ändert sich nicht von Stelle zu Stelle. Beim zweiten Finance-Job desselben Kunden als Bestätigung zeigen statt | finance-headhunter |
| `FIN-06` | Vorheriger Job desselben Kunden (jobs.skills, jobs.must_haves) sowie job_skill_requirements (Tabelle existiert in PROD). Die ERP-Landschaft ist eine Unternehmenseigenschaft, keine Stelleneigenschaft. Beim zweiten Finance-Job nur noch die Mo | finance-headhunter |
| `FIN-05` | company_profiles.headcount und company_profiles.annual_revenue (beide existieren in PROD, REST []) — aber ausdrücklich nur als Vorschlag für eine Bestätigungsfrage, nie als Antwort. Die Anzahl der Gesellschaften lässt sich aus Umsatz und Ko | finance-headhunter |
| `FIN-13` | jobs.onsite_days_required aus einem früheren Job desselben Kunden (Spalte existiert in PROD, liegt in recruiter_jobs_view) als Ausgangswert. Die Abschlusswochen-Nachfrage bleibt trotzdem stehen — sie ist nicht aus dem Vorwert ableitbar. | finance-headhunter |
| `IB-32 (Gegenbeispiel: NICHT vorbefüllbar)` | company_profiles.excluded_companies EXISTIERT IN PRODUKTION NICHT (BEWIESEN, REST 42703). JobIntakeStudio.tsx:156-159 liest die Spalte trotzdem zur Vorbefüllung, und es gibt kein UI, das sie je füllen würde. Diese Vorbefüllung kann heute st | finance-headhunter |